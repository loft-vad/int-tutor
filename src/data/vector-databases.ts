import type { Question } from '@/types/content';

/**
 * Vector databases and similarity search.
 *
 * Matrix rows: "Vector Databases", "Vector Querying", "Vector Performance" (Intermediate),
 * "Storing Embeddings in Vector Databases" (indexing strategies, high-dimensional embeddings,
 * batch insertion), "Graph DBs: Concepts / Querying" (Advanced), and the SQL/NoSQL
 * embedding-storage rows.
 */

export const vectorDatabasesQuestions: Question[] = [
  // ── Flashcards ────────────────────────────────────────────────
  {
    id: "vdb-fc-001",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "beginner",
    tags: ["basics", "pgvector"],
    front: "What is a vector database and what does it actually add over a normal database?",
    back: "A database whose primary index is built for **approximate nearest-neighbour (ANN) search** over high-dimensional vectors.\n\nIt adds:\n- **ANN indexes** (HNSW, IVF, PQ) — sub-linear similarity search instead of a full scan.\n- **Metadata filtering** integrated with the vector search (pre- or post-filter).\n- **Hybrid search** — vector + lexical in one query.\n- Operational plumbing: sharding, replication, snapshots, upserts, namespaces/tenants.\n\nYou do not always need a dedicated one: `pgvector` on Postgres you already run is often the right first choice.",
    explanation: "The senior answer is the second half. Adding a whole new stateful system to your architecture for 500k vectors is over-engineering; pgvector handles that comfortably and keeps your embeddings transactionally consistent with the rows they describe.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-002",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "beginner",
    tags: ["similarity", "cosine", "distance-metrics"],
    front: "Cosine similarity, dot product, and Euclidean distance — which do you use?",
    back: "- **Cosine similarity** — angle only, ignores magnitude. The default for text embeddings, because length reflects token count more than meaning. Range −1..1.\n- **Dot product** — angle *and* magnitude. **Identical to cosine when vectors are normalized**, and cheaper to compute — which is why many indexes normalize on write and use dot product internally.\n- **Euclidean (L2)** — straight-line distance. Also equivalent in ranking to cosine on normalized vectors.\n\nThe rule: **use the metric the embedding model was trained with.** Model cards state it. Mismatching the metric silently degrades recall.",
    explanation: "The 'equivalent when normalized' fact is the one interviewers probe. And the failure mode to name: choosing L2 in your index config for a model trained with cosine produces a system that works just badly enough that nobody notices for months.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-003",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "intermediate",
    tags: ["hnsw", "ann", "indexing"],
    front: "How does the HNSW index work?",
    back: "**Hierarchical Navigable Small World** — a multi-layer proximity graph.\n\n- The top layer is sparse with long-range links; each layer down is denser.\n- Search starts at the top, greedily walks to the nearest node, then descends and repeats — coarse-to-fine, like a skip list for vectors.\n\nParameters:\n- **M** — links per node. Higher = better recall, more memory.\n- **efConstruction** — candidate list size at build time. Higher = better graph, slower build.\n- **efSearch** — candidate list size at query time. **The runtime recall/latency dial.**\n\nTrade-off: excellent recall and latency, but the whole graph must sit in **RAM**, and deletions are handled as tombstones requiring periodic compaction.",
    explanation: "HNSW is the default in most vector DBs, so know its knobs. The interview-critical point: `efSearch` is tunable **per query**, so you can serve a fast, lower-recall path for autocomplete and a slow, high-recall path for RAG from the same index.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-004",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "advanced",
    tags: ["ivf", "product-quantization", "indexing"],
    front: "How do IVF and Product Quantization work, and when do you prefer them to HNSW?",
    back: "**IVF (Inverted File)** — k-means clusters the vectors into `nlist` cells; a query probes only the `nprobe` nearest cells. Cheap to build, tunable via `nprobe`, but recall suffers when the true neighbour sits just across a cell boundary.\n\n**PQ (Product Quantization)** — split each vector into sub-vectors, replace each with a codebook centroid ID. A 1536-d float32 vector (6 KB) compresses to ~96 bytes — **~64× smaller** — at the cost of approximation error.\n\n**IVF-PQ** combines both and is how billion-scale indexes fit in memory.\n\nPrefer them over HNSW when **memory is the binding constraint** or the index is enormous; prefer HNSW when recall and latency matter most and the data fits in RAM.",
    explanation: "'Vector Performance — understanding indexing techniques (e.g., HNSW, IVF)' is a named matrix row. The framing that lands: HNSW optimises for speed at a memory cost, PQ optimises for memory at a recall cost, and you pick based on which resource you're actually short of.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-005",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "advanced",
    tags: ["recall", "benchmarking", "tuning"],
    front: "What is the recall/latency trade-off in ANN search, and how do you measure it?",
    back: "ANN is *approximate* — it may miss true neighbours. **Recall@k** = the fraction of the true top-k that the index returned.\n\nMeasure it by brute-forcing a sample: compute exact top-k over the full corpus for ~1,000 queries, then compare against index results at various settings.\n\nThen plot **recall vs. QPS** and pick the operating point:\n- Semantic autocomplete → 90% recall, 5 ms.\n- RAG over legal documents → 99% recall, 100 ms.\n\nDial with `efSearch` (HNSW) or `nprobe` (IVF).",
    explanation: "Interviewers want to hear that recall is a *measured, tuned* property, not a hope. Teams that never brute-force a ground truth have no idea whether their index is at 99% recall or 70% — and 70% recall silently caps their whole RAG pipeline.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-006",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "intermediate",
    tags: ["filtering", "metadata", "security"],
    front: "Pre-filtering vs post-filtering on metadata — what's the difference and which is safer?",
    back: "- **Post-filter** — ANN search returns top-k, then filter by metadata. Simple, but if only 2 of the 10 results match your filter you get 2 results, not 10. With a selective filter you can get **zero**.\n- **Pre-filter** — restrict the search space to matching vectors first. Always returns k results, but a naive implementation degenerates into a brute-force scan over the filtered subset.\n\nModern engines do **filtered ANN**: evaluate the metadata predicate *during* graph traversal, keeping the search efficient while guaranteeing k results.\n\nFor anything correctness-critical — **permissions and tenancy** — you must pre-filter.",
    explanation: "The security angle is the important one: post-filtering an ACL means the vector index searched documents the user isn't allowed to see, and result counts and latency can leak their existence. Pre-filter for access control, always.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-007",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "advanced",
    tags: ["multi-tenancy", "security", "namespaces"],
    front: "How do you design multi-tenancy in a vector database?",
    back: "Three patterns, increasing isolation:\n\n1. **Shared index + `tenant_id` filter** — cheapest, one index. Requires a mandatory pre-filter on every query; one missing filter is a cross-tenant leak.\n2. **Namespace / partition per tenant** — supported natively by most vector DBs. Good isolation, no filter to forget, but thousands of tiny namespaces waste memory.\n3. **Index (or cluster) per tenant** — strongest isolation, needed for data-residency or per-tenant encryption. Operationally heavy.\n\nCommon production shape: namespaces for most tenants, dedicated indexes for the largest and most regulated ones.",
    explanation: "The failure mode to name is the one-missing-filter leak, and the mitigation: enforce the tenant filter in a shared data-access layer that makes an unscoped query impossible to write, rather than trusting every call site.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-008",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "intermediate",
    tags: ["batch", "indexing", "performance"],
    front: "Why does batch insertion matter, and how do you build a large index efficiently?",
    back: "Per-vector inserts are dominated by round-trip and index-maintenance overhead. Batching 100–1,000 vectors per request is routinely an order of magnitude faster.\n\nFor a first large build:\n1. **Batch-embed** the corpus (batch embedding endpoints are cheaper and far higher throughput).\n2. **Bulk-load** vectors with indexing deferred.\n3. **Build the ANN index once** over the full set — an HNSW graph built in bulk is much faster and better-connected than one grown by incremental insertion.\n4. Then switch to incremental upserts for ongoing changes.\n\nAlso: parallelise across shards, checkpoint progress, and handle 429s with backoff + jitter.",
    explanation: "'Batch Insertion and Retrieval of Embeddings' appears as a matrix row for vector, SQL, *and* NoSQL storage — the graders clearly care. The build-once-then-incremental detail is what shows you've actually indexed tens of millions of vectors.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-009",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "intermediate",
    tags: ["pgvector", "sql", "postgres"],
    front: "How do you store embeddings in a relational database?",
    back: "With `pgvector` on Postgres:\n\n```sql\nCREATE EXTENSION vector;\nCREATE TABLE chunks (\n  id          bigserial PRIMARY KEY,\n  document_id uuid NOT NULL REFERENCES documents(id) ON DELETE CASCADE,\n  tenant_id   uuid NOT NULL,\n  content     text NOT NULL,\n  embedding   vector(1536)\n);\nCREATE INDEX ON chunks USING hnsw (embedding vector_cosine_ops);\nCREATE INDEX ON chunks (tenant_id, document_id);\n\nSELECT id, content, 1 - (embedding <=> $1) AS score\nFROM chunks\nWHERE tenant_id = $2\nORDER BY embedding <=> $1\nLIMIT 10;\n```\n\nBig wins: **one system**, ACID transactions, joins to your business tables, and `ON DELETE CASCADE` making orphaned vectors impossible.",
    explanation: "That cascade is the strongest practical argument for pgvector: in a separate vector store, keeping deletions in sync with the source of truth is a permanent background chore, and it's the one people get wrong. Operators `<=>` cosine, `<->` L2, `<#>` inner product.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-010",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "intermediate",
    tags: ["indexing", "sql", "nosql"],
    front: "When should you index SQL/NoSQL columns alongside embeddings?",
    back: "Vector search is only half the query. You also need conventional indexes on:\n\n- **`document_id`** — for delete/update by source document (the operation you run constantly).\n- **`tenant_id` / ACL columns** — for the mandatory pre-filter.\n- **Timestamps** — for recency filtering and index-lag monitoring.\n- **`content_hash`** — for idempotent upserts and deduplication.\n\nWithout a `document_id` index, deleting one document's 400 chunks becomes a full table scan — and that runs on every source update.",
    explanation: "'Indexing SQL Tables for Efficient Retrieval — e.g., indexing the document ID column to speed up queries' is almost verbatim a matrix row. The reason it's on the list is that it's the ingestion pipeline's hot path, not the query path people think about.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-011",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "advanced",
    tags: ["quantization", "matryoshka", "memory"],
    front: "How do you handle high-dimensional embeddings efficiently?",
    back: "1,536-d float32 = **6 KB per vector**; 100M vectors ≈ 600 GB before any index overhead.\n\nReduction levers:\n- **Matryoshka embeddings (MRL)** — models trained so a truncated prefix is still a valid embedding. Take the first 512 of 1,536 dims for a 3× saving with modest recall loss.\n- **Scalar quantization** — float32 → int8, 4× smaller, small recall loss. Almost always worth it.\n- **Binary quantization** — 1 bit per dim, 32× smaller, with Hamming distance for a fast first pass, then rescore the shortlist with full vectors.\n- **Product quantization** — up to ~64× via codebooks.\n- **Dimensionality reduction (PCA)** — works, but adds a fitted transform you must version and re-apply forever.\n\nCommon production stack: **binary/int8 for the coarse pass, full-precision rescoring on the top ~100.**",
    explanation: "That two-stage quantize-then-rescore pattern is the current state of the art and a strong signal. It gets you most of the memory saving while keeping final ranking quality close to exact.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-012",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "intermediate",
    tags: ["tooling", "selection", "faiss"],
    front: "Which vector database would you choose, and on what criteria?",
    back: "| Option | Best when |\n|---|---|\n| **pgvector** | You already run Postgres and have < ~10M vectors. Default choice. |\n| **Qdrant** | Self-hosted, strong filtered search, Rust, good ergonomics |\n| **Milvus** | Billion-scale, multiple index types, heavier ops |\n| **Pinecone / managed** | You don't want to operate a stateful system |\n| **Weaviate** | Built-in hybrid search and module ecosystem |\n| **Elasticsearch / OpenSearch** | You already run it and need lexical + vector together |\n| **FAISS** | A library, not a database — in-process, no persistence or filtering layer |\n\nDecide on: corpus size, filtering needs, hybrid search, ops appetite, cost, data residency.",
    explanation: "Naming FAISS as a *library* rather than a database is a small detail that shows you've used it — it has no server, no metadata store, and no durability, which makes it perfect for prototypes and wrong for production without a lot of scaffolding.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-013",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "beginner",
    tags: ["faiss", "ann"],
    front: "What is FAISS and where does it fit?",
    back: "Facebook AI Similarity Search — a **library** of ANN index implementations (Flat, IVF, HNSW, PQ, and GPU variants).\n\nStrengths: extremely fast, all the index types in one place, GPU support, the reference implementation for benchmarking.\n\nWhat it does **not** give you: a server, persistence beyond `write_index`/`read_index`, metadata filtering, ACLs, replication, or concurrent writes.\n\nFit: notebooks, offline evaluation, embedded in-process search, and as the engine inside something else. The Novice matrix row asks for exactly this — build the simplest naive RAG with FAISS.",
    explanation: "`IndexFlatL2` is your exact-search baseline for computing ground-truth recall when benchmarking an approximate index — that's a genuinely useful thing to mention rather than treating FAISS purely as a prototype tool.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-014",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "advanced",
    tags: ["graph-db", "neo4j", "graphrag"],
    front: "What is a graph database, and how does it differ from a vector database?",
    back: "A graph DB (Neo4j, Neptune) stores **nodes**, **edges**, and properties, and makes traversal a first-class, cheap operation.\n\n| | Vector DB | Graph DB |\n|---|---|---|\n| Question | \"What is *similar* to this?\" | \"What is *connected* to this, and how?\" |\n| Query | ANN over embeddings | Traversal (Cypher, Gremlin, SPARQL) |\n| Strength | Fuzzy semantic match | Multi-hop, explicit relationships |\n| Weakness | No notion of relationships | No notion of similarity |\n\nIn GraphRAG they're combined: vector search finds entry-point entities, graph traversal expands to related facts, and both feed the prompt.",
    explanation: "'Graph DBs: Concepts' and 'Graph DBs: Querying' are Advanced matrix rows. The complementary framing — similarity versus connectivity — is the answer; they're not competitors.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-015",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "advanced",
    tags: ["cypher", "graph-db", "querying"],
    front: "Write a Cypher query that finds second-degree connections.",
    back: "```cypher\nMATCH (p:Person {email: $email})-[:WORKS_WITH]->(colleague)\n      -[:WORKS_WITH]->(second)\nWHERE second <> p\n  AND NOT (p)-[:WORKS_WITH]->(second)\nRETURN second.name, count(*) AS shared\nORDER BY shared DESC\nLIMIT 10;\n```\n\nThe pattern `(a)-[:REL]->(b)-[:REL]->(c)` is the whole point of a graph DB: expressing it in SQL needs a self-join per hop, and variable-depth traversal (`-[:REL*1..5]->`) needs a recursive CTE that performs badly.",
    explanation: "Matrix row 'Graph DBs: Querying' expects you to write, not just describe. Being able to produce a two-hop Cypher query with an exclusion clause is a reasonable Advanced bar, and it also explains *why* graph DBs exist.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-016",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "advanced",
    tags: ["monitoring", "observability", "operations"],
    front: "How do you monitor a vector search system in production?",
    back: "**Quality**\n- Recall@k against a periodically refreshed ground-truth sample.\n- Score distribution drift — a sudden shift often means a silent embedding-model change.\n- Zero-result and low-score rates (users asking things the corpus can't answer).\n\n**Performance**\n- p50/p95/p99 query latency, split by filtered vs unfiltered.\n- Index memory usage vs available RAM — HNSW degrades sharply on swap.\n- Insert/upsert throughput and queue depth.\n\n**Correctness**\n- Index lag: source-change timestamp → indexed timestamp.\n- Vector count vs source-document count (drift means orphans or ingestion gaps).\n- Tombstone ratio; trigger compaction before it hurts.",
    explanation: "The vector-count-vs-source-count reconciliation check is the one that catches the largest class of silent RAG bugs — deletions that never propagated and documents that failed to ingest. It's cheap and almost nobody has it.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-017",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "advanced",
    tags: ["hnsw", "deletion", "compaction"],
    front: "What happens to an HNSW index when you delete a lot of vectors?",
    back: "Most implementations do a **soft delete**: the node stays in the graph, marked as a tombstone, and is skipped in results.\n\nConsequences as tombstones accumulate:\n- Memory is still consumed by deleted vectors.\n- Search traverses dead nodes, so latency rises.\n- **Recall degrades** — tombstones can occupy the candidate list, crowding out live results, so you get fewer than k.\n\nFix: periodic **compaction / index rebuild**. Monitor the tombstone ratio and schedule a rebuild before it becomes a recall problem.\n\nThis is one reason a high-churn corpus may be better served by IVF or by periodic full rebuilds.",
    explanation: "This bites teams whose documents update frequently: retrieval quality slowly decays over months with no code change and no obvious cause. Knowing that deletions are tombstones, not removals, is what lets you diagnose it.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-018",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "advanced",
    tags: ["sharding", "replication", "scaling"],
    front: "How would you shard and replicate a vector index?",
    back: "**Sharding** (for capacity/throughput): partition vectors across nodes, query all shards in parallel, merge the top-k.\n- **Random/hash sharding** — even load; every query must fan out to every shard.\n- **Semantic/tenant sharding** — queries touch one shard, so throughput scales well, but load can be badly skewed.\n\nMerging is straightforward for distance-based scores: take each shard's top-k and re-sort globally.\n\n**Replication** (for availability/read throughput): copies of each shard behind a load balancer. Vector indexes are usually read-heavy and rebuilt from a source of truth, so eventual consistency between replicas is normally acceptable.\n\nKeep the source of truth (documents + embeddings) in durable storage so any index can be rebuilt.",
    explanation: "'understanding and managing vector database scaling (e.g., sharding, replication)' is a named matrix row. The last sentence is the design principle: treat the vector index as a **derived, rebuildable artifact**, not as primary data.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-fc-019",
    type: "flashcard",
    topic: "vector-databases",
    difficulty: "intermediate",
    tags: ["architecture", "when-not-to"],
    front: "When is a vector database the wrong tool?",
    back: "- **Exact lookup** — you know the ID. Use a key-value store.\n- **Structured filtering only** — 'all invoices in Q3 over $10k' is SQL, not similarity.\n- **Small corpora** — a few thousand vectors brute-force in milliseconds with NumPy. An ANN index adds only complexity.\n- **Rapidly changing data with strict consistency** — index lag makes it stale.\n- **Exact keyword matching** — BM25 wins.\n- **Aggregations and analytics** — vector DBs are not OLAP engines.\n\nAnd the general case: if you already run Postgres and have < ~10M vectors, `pgvector` is very likely enough.",
    explanation: "Interviewers ask this to test whether you reach for infrastructure by default. A brute-force NumPy dot-product over 5,000 vectors is exact, sub-millisecond, and has zero operational cost — an entirely respectable production answer at that scale.",
    createdAt: "2026-08-26",
  },
  // ── Multiple choice ───────────────────────────────────────────
  {
    id: "vdb-mc-001",
    type: "multiple-choice",
    topic: "vector-databases",
    difficulty: "intermediate",
    tags: ["distance-metrics", "normalization"],
    prompt: "Your embedding model was trained with cosine similarity, but your index is configured for L2 distance on unnormalized vectors. What happens?",
    options: [
      { id: "a", text: "Ranking is subtly wrong — longer texts are penalised or favoured by magnitude rather than meaning" },
      { id: "b", text: "Nothing — all distance metrics produce identical rankings" },
      { id: "c", text: "The index refuses to build" },
      { id: "d", text: "Recall improves because L2 is more precise" },
    ],
    correctOptionId: "a",
    timeLimitSeconds: 50,
    explanation: "On *normalized* vectors, cosine, dot product, and L2 rank identically. Unnormalized, L2 is influenced by vector magnitude, which for text embeddings largely tracks length — so the index quietly ranks by a property you don't care about. It never errors, which is exactly why it goes unnoticed.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-mc-002",
    type: "multiple-choice",
    topic: "vector-databases",
    difficulty: "intermediate",
    tags: ["filtering", "post-filter"],
    prompt: "You apply a highly selective metadata filter (matching ~0.1% of documents) with post-filtering and top-k=10. What is the likely result?",
    options: [
      { id: "a", text: "A slow but correct query" },
      { id: "b", text: "The filter is silently ignored" },
      { id: "c", text: "Zero or very few results, because the pre-filter top-10 rarely contains a matching document" },
      { id: "d", text: "Exactly 10 results, drawn from the filtered subset" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 50,
    explanation: "Post-filtering searches first and filters second, so a 0.1%-selective predicate usually eliminates the entire result set. This is the classic 'my vector search returns nothing' bug, and it's why selective filters require pre-filtering or a filtered-ANN implementation.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-mc-003",
    type: "multiple-choice",
    topic: "vector-databases",
    difficulty: "advanced",
    tags: ["quantization", "memory", "scaling"],
    prompt: "You need to store 100M 1536-d embeddings but have 128 GB of RAM. What is the most practical approach?",
    options: [
      { id: "a", text: "Store the vectors in S3 and scan them per query" },
      { id: "b", text: "Quantize (int8 or binary) for the in-memory index and rescore the shortlist with full-precision vectors from disk" },
      { id: "c", text: "Use HNSW with float32 vectors and add more nodes until it fits" },
      { id: "d", text: "Reduce top-k to lower memory usage" },
    ],
    correctOptionId: "b",
    timeLimitSeconds: 60,
    explanation: "100M × 1536 × 4 bytes ≈ 600 GB in float32, so it simply cannot fit. Binary or int8 quantization brings the searchable index into RAM, and rescoring the top ~100 candidates with full vectors recovers nearly all ranking quality. top-k has no bearing on index size, and scanning S3 per query is not a search system.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-mc-004",
    type: "multiple-choice",
    topic: "vector-databases",
    difficulty: "intermediate",
    tags: ["hnsw", "tuning"],
    prompt: "Which parameter do you tune at query time to trade latency for recall in HNSW?",
    options: [
      { id: "a", text: "efConstruction" },
      { id: "b", text: "The number of shards" },
      { id: "c", text: "efSearch" },
      { id: "d", text: "M" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 40,
    explanation: "`M` and `efConstruction` are build-time properties — changing them means rebuilding the index. `efSearch` sets the candidate list size per query, so you can serve a fast low-recall path and a slow high-recall path from exactly the same index.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-mc-005",
    type: "multiple-choice",
    topic: "vector-databases",
    difficulty: "intermediate",
    tags: ["upsert", "freshness", "idempotency"],
    prompt: "A document is updated and re-ingested, producing 12 new chunks where there were 15. What must the pipeline do to stay correct?",
    options: [
      { id: "a", text: "Update the first 12 vectors in place and leave the remaining 3" },
      { id: "b", text: "Rebuild the entire index" },
      { id: "c", text: "Delete all vectors for that document_id, then insert the new 12 in one transaction or idempotent batch" },
      { id: "d", text: "Insert the 12 new chunks; the old ones will be outranked naturally" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 50,
    explanation: "Anything short of delete-then-insert leaves 3 stale chunks that keep surfacing in retrieval as authoritative content. This is why an index on `document_id` is essential and why the upsert must be keyed on a stable document identity rather than on chunk position.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-mc-006",
    type: "multiple-choice",
    topic: "vector-databases",
    difficulty: "beginner",
    tags: ["pgvector", "architecture"],
    prompt: "For a corpus of 50,000 chunks in an app that already runs Postgres, what is the most defensible choice?",
    options: [
      { id: "a", text: "A self-hosted Milvus cluster" },
      { id: "b", text: "pgvector, keeping embeddings transactionally consistent with the source rows" },
      { id: "c", text: "A dedicated managed vector database for future-proofing" },
      { id: "d", text: "FAISS loaded into each application process" },
    ],
    correctOptionId: "b",
    timeLimitSeconds: 45,
    explanation: "At 50k vectors the ANN index is barely necessary, and pgvector gives you joins, transactions, and ON DELETE CASCADE so orphaned vectors are impossible. Adding a second stateful system buys operational cost and a permanent sync problem in exchange for scale you don't need.",
    createdAt: "2026-08-26",
  },
  {
    id: "vdb-mc-007",
    type: "multiple-choice",
    topic: "vector-databases",
    difficulty: "advanced",
    tags: ["hnsw", "compaction", "operations"],
    prompt: "Retrieval quality has slowly degraded over six months with no code changes, in a corpus with heavy document churn. Most likely cause?",
    options: [
      { id: "a", text: "HNSW tombstones from deletions have accumulated, crowding candidate lists and reducing effective recall" },
      { id: "b", text: "The embedding model has drifted over time" },
      { id: "c", text: "Cosine similarity becomes less accurate as data ages" },
      { id: "d", text: "The context window has shrunk" },
    ],
    correctOptionId: "a",
    timeLimitSeconds: 60,
    explanation: "Deletes in HNSW are soft, so a high-churn index fills with tombstones that consume memory and candidate-list slots. Recall decays gradually with nothing in the logs to explain it. The fix is scheduled compaction or a periodic rebuild, driven by a tombstone-ratio metric.",
    createdAt: "2026-08-26",
  },
];
