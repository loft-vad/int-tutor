import type { Question } from '@/types/content';

/**
 * AI fundamentals — terms and concepts.
 *
 * Covers the GenAI evaluation matrix rows "AI fundamentals" (Novice) and
 * "Architectures (AI/ML)" (Advanced), plus Sheet1 rows N-1.1 and N-1.2.
 * See .claude/knowledge/ai-skills-matrix.md
 */

export const aiFundamentalsQuestions: Question[] = [
  // ── Flashcards ────────────────────────────────────────────────
  {
    id: "ai-fc-001",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["vocabulary", "ai-vs-ml", "n-1.2"],
    front: "How do AI, ML, Deep Learning, and GenAI relate to each other?",
    back: "They are **nested sets**, widest first:\n\n- **AI** — any system that performs tasks needing human-like intelligence (including hand-written rules).\n- **ML** — the subset of AI that *learns* patterns from data instead of being explicitly programmed.\n- **Deep Learning** — the subset of ML using multi-layer neural networks.\n- **GenAI** — the subset of deep learning that *generates* new content (text, image, audio, code) rather than only classifying or predicting.\n\n```\nAI ⊃ ML ⊃ Deep Learning ⊃ Generative AI ⊃ LLMs\n```",
    explanation: "The trap is calling everything 'AI'. Interviewers listen for whether you can place a technology precisely: a spam filter is ML but not GenAI; a rules engine is AI but not ML; an LLM is all four.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-002",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["llm", "transformer", "n-1.1"],
    front: "What is a Large Language Model, mechanically?",
    back: "A **transformer neural network** trained to predict the next token given the preceding tokens.\n\n1. Text is split into **tokens** (sub-word units).\n2. Each token becomes a vector (**embedding**).\n3. Stacked **self-attention** + feed-forward layers mix information across positions.\n4. The final layer emits a probability distribution over the whole vocabulary.\n5. A token is **sampled** from that distribution, appended, and the loop repeats.\n\nEverything an LLM appears to *do* — reasoning, translating, coding — is a side effect of that one objective at scale.",
    explanation: "Say 'next-token prediction' out loud. Candidates who describe the LLM as a database of facts or a search engine fail this question. The follow-up is usually 'so why does it hallucinate?' — because sampling a plausible token is not the same as retrieving a true one.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-003",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["tokens", "cost", "n-1.1"],
    front: "What is a token, and why do tokens (not words) matter?",
    back: "A **token** is the atomic unit an LLM reads and writes — typically a sub-word fragment of ~3–4 characters in English.\n\nWhy it matters:\n- **Billing** is per token, input and output priced separately.\n- **Context windows** are measured in tokens.\n- **Rate limits** are usually tokens-per-minute, not requests-per-minute.\n- Non-English text and code tokenize *less* efficiently — the same sentence in Thai or Japanese can cost 2–4× more tokens.\n\nRough English rule of thumb: **1 token ≈ 0.75 words**, so 1,000 tokens ≈ 750 words.",
    explanation: "A common practical follow-up: 'why can't the model count the letters in strawberry?' Because it never sees letters — it sees tokens. Tokenization also explains why prompt-length estimates in characters are unreliable for cost forecasting.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-004",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["context-window", "n-1.1"],
    front: "What is the context window, and what happens when you exceed it?",
    back: "The **context window** is the maximum number of tokens the model can attend to in one request — **prompt + generated output combined**.\n\nExceeding it does not silently truncate in a helpful way:\n- Most APIs return a hard error (`context_length_exceeded`).\n- If your framework truncates for you, it usually drops the *oldest* messages — silently losing the system prompt or early conversation.\n\nMitigations: summarize older turns, use RAG to fetch only relevant chunks, or move to a larger-context model.",
    explanation: "The senior answer adds that a large context window is not free: attention cost grows with sequence length, latency and price go up, and retrieval accuracy degrades in the middle of long contexts ('lost in the middle'). Big context is not a substitute for good retrieval.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-005",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["hallucination", "grounding", "n-1.1"],
    front: "What is a hallucination and why do LLMs hallucinate?",
    back: "A **hallucination** is fluent, confident output that is factually wrong or unsupported by any source.\n\nCause: the model optimises for the *most probable* continuation, not the *true* one. It has no internal notion of 'I don't know' unless training taught it to express uncertainty.\n\nReduction strategies (in order of effectiveness):\n1. **Grounding** — supply the facts in the prompt (RAG), and instruct the model to answer only from them.\n2. **Citations** — require span-level attribution so claims can be verified.\n3. **Lower temperature** for factual tasks.\n4. **Verification pass** — a second call, or a deterministic check, that validates claims.",
    explanation: "Note that lowering temperature reduces variance, not falsehood — a confidently wrong answer at temperature 0 is still wrong, just reproducibly so. Interviewers like candidates who make that distinction.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-006",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["grounding", "rag", "n-1.1"],
    front: "What does *grounding* mean?",
    back: "**Grounding** is constraining a model's output to verifiable external context supplied at inference time, rather than to its parametric memory.\n\nIn practice it means:\n- Retrieve relevant documents and put them in the prompt (RAG).\n- Instruct: *'Answer only from the provided context. If the context does not contain the answer, say so.'*\n- Return citations pointing back to the source spans.\n\nGrounded systems can be **audited**: you can point at the sentence that produced the claim.",
    explanation: "Grounding is the single biggest lever on factual reliability in production LLM apps, which is why the evaluation matrix pairs it with RAG. It also converts a model-quality problem into a retrieval-quality problem — which is an engineering problem you can actually measure and fix.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-007",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["inference", "training", "n-1.1"],
    front: "What is inference, and how does it differ from training?",
    back: "- **Training** — adjusting model weights by gradient descent over a huge corpus. Costs millions of dollars, takes weeks, happens once.\n- **Inference** — running a *frozen* model forward to produce output. Costs fractions of a cent, takes milliseconds to seconds, happens billions of times.\n\nAs an application engineer you almost always work at inference time. Your levers are the **prompt**, the **context**, the **sampling parameters**, and the **model choice** — not the weights.",
    explanation: "The practical consequence: 'the model doesn't know about our product' is almost never solved by training. It is solved by retrieval. Reaching for fine-tuning first is a classic junior instinct that interviewers probe for.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-008",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "intermediate",
    tags: ["fine-tuning", "rag", "lora", "n-1.1"],
    front: "What is fine-tuning, and when is it the right tool?",
    back: "**Fine-tuning** continues training a pre-trained model on a smaller domain-specific dataset, updating weights.\n\nUse it for **behaviour and format**, not for **facts**:\n\n| Goal | Right tool |\n|---|---|\n| Model must know your company's current data | RAG |\n| Model must reply in a specific tone / schema | Fine-tuning (or a good prompt) |\n| Model must handle a narrow classification task cheaply | Fine-tuning a small model |\n| Model must cite sources | RAG |\n\n**Order of escalation:** prompt → few-shot examples → RAG → fine-tuning. Fine-tuning is last because it is the most expensive to build *and* to maintain — the data drifts and you retrain.",
    explanation: "PEFT methods like LoRA make fine-tuning much cheaper by training a small number of adapter weights instead of the whole model. Mentioning LoRA/QLoRA signals you have gone past the marketing layer.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-009",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["embeddings", "semantic-search", "n-1.2"],
    front: "What are embeddings?",
    back: "An **embedding** is a fixed-length vector of floats that represents the *meaning* of a piece of content, such that semantically similar items land close together in vector space.\n\n- Produced by an embedding model (a different, usually much smaller model than the LLM).\n- Typical dimensionality: 384 (MiniLM) → 1536 (OpenAI `text-embedding-3-small`) → 3072 (large).\n- Similarity is measured with **cosine similarity** (or dot product on normalized vectors).\n\nThey power semantic search, clustering, deduplication, classification, and the retrieval half of RAG.",
    explanation: "Two things interviewers check: (1) embeddings from different models are not comparable — you cannot mix them in one index; (2) changing the embedding model means re-embedding the entire corpus, which is a real migration cost.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-010",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["semantic-search", "hybrid-search", "n-1.2"],
    front: "What is semantic search and how does it differ from keyword search?",
    back: "**Keyword (lexical) search** — matches literal terms, ranked by something like BM25. Fast, exact, explainable; fails when the user's words differ from the document's words.\n\n**Semantic search** — embeds the query, finds nearest-neighbour document vectors. Handles synonyms and paraphrase; fails on rare exact tokens (product SKUs, error codes, names).\n\n**Hybrid search** combines both and is what most production systems actually ship.",
    explanation: "The right answer to 'which should we use?' is almost always 'both, fused'. Being able to name a fusion method — Reciprocal Rank Fusion is the standard one — moves you from Novice to Intermediate on this row.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-011",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["multimodal", "n-1.2"],
    front: "What does *multimodal* mean?",
    back: "A **multimodal** model accepts and/or produces more than one type of data — text, images, audio, video — in a shared representation space.\n\nPractical implications:\n- Images are also billed **in tokens** (a 1024×1024 image can cost well over 1,000 tokens).\n- Vision enables document understanding without a separate OCR stage — screenshots, charts, scanned PDFs.\n- 'Multimodal' says nothing about *output*: many models read images but only write text.",
    explanation: "For RAG this matters a lot: a multimodal model lets you index page images directly instead of running a lossy OCR + layout-parsing pipeline, which is one of the hardest parts of document ingestion.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-012",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["rag", "n-1.2"],
    front: "What is Retrieval-Augmented Generation (RAG), in one paragraph?",
    back: "RAG **retrieves relevant context at query time and injects it into the prompt** so the model answers from supplied evidence rather than from memory.\n\nMinimal flow:\n```\nquery → embed → vector search → top-k chunks\n      → prompt(context + question) → LLM → answer + citations\n```\n\nIt gives you fresh data, private data, citations, and a much lower hallucination rate — without touching model weights.",
    explanation: "This is the single most-asked AI-engineering question. Have a crisp 30-second version plus the failure modes ready: bad chunking, missing documents, retrieved-but-ignored context, and irrelevant top-k.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-013",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["agents", "n-1.2"],
    front: "What is an AI agent, as distinct from a chatbot or a chain?",
    back: "An **agent** is an LLM that decides *what to do next* in a loop, rather than executing a fixed script.\n\n| | Control flow | Example |\n|---|---|---|\n| **Chain / workflow** | Fixed, written by you | summarize → translate → format |\n| **Agent** | Dynamic, decided by the model | choose a tool, read the result, decide again |\n\nThe loop: **observe → reason → act (call a tool) → observe result → repeat until done**.\n\nThe defining ingredients are **tool calling**, **memory**, and a **termination condition**.",
    explanation: "The engineering point interviewers wait for: agents trade determinism for flexibility. If a fixed chain solves the problem, use the chain — it is cheaper, faster, testable, and debuggable. Reach for an agent only when the required steps genuinely cannot be known in advance.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-014",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["tool-calling", "agents", "n-1.2"],
    front: "What is tool calling (function calling)?",
    back: "The model is given **schemas** of functions it may invoke. Instead of prose, it can emit a structured call:\n\n```json\n{ \"name\": \"get_weather\", \"arguments\": { \"city\": \"Berlin\" } }\n```\n\n**Your code** executes the function and returns the result as a new message; the model then continues.\n\nCritical: the model **never runs anything itself**. It only *proposes* a call. Execution, authorization, and validation are entirely your responsibility.",
    explanation: "That last point is the security answer too — a prompt-injected model can propose `delete_all_users()`, and the only thing standing between that and disaster is your authorization layer. Never let tool permissions exceed the permissions of the end user on whose behalf you're acting.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-015",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "intermediate",
    tags: ["mcp", "agents", "n-1.2"],
    front: "What is MCP (Model Context Protocol)?",
    back: "An open protocol that standardises how AI applications connect to external **tools, data sources, and prompts** — 'USB-C for AI apps'.\n\n- **MCP server** exposes capabilities: *tools* (callable functions), *resources* (readable data), *prompts* (reusable templates).\n- **MCP host/client** (an IDE assistant, a chat app, an agent) discovers and calls them.\n- Transport is JSON-RPC over stdio or HTTP.\n\nThe value: write one MCP server for your internal system, and every MCP-capable client can use it — instead of writing a bespoke integration per assistant.",
    explanation: "MCP appears explicitly in the evaluation matrix under 'AI Agents - A2A, MCP, ToolCalling'. The interview follow-up is the security one: an MCP server runs with real credentials, so its tools need scoping, auditing, and confirmation on destructive actions.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-016",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "advanced",
    tags: ["a2a", "agents", "multi-agent"],
    front: "What is A2A (Agent-to-Agent) communication?",
    back: "A protocol for **agents built by different teams or vendors to discover and delegate to each other** as peers, rather than one agent wrapping another as a tool.\n\nCore pieces:\n- An **agent card** advertising identity, capabilities, and endpoints.\n- A **task** lifecycle (submitted → working → input-required → completed/failed).\n- Structured **artifacts** as results, with streaming updates.\n\nWhere MCP connects an agent *down* to tools and data, A2A connects agents *across* to peers.",
    explanation: "The distinction MCP-is-vertical / A2A-is-horizontal is the crisp way to answer this. Multi-agent systems in the matrix's Advanced tier assume you can reason about both.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-017",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "intermediate",
    tags: ["agentic-workflow", "orchestration", "n-1.2"],
    front: "What is an agentic workflow?",
    back: "A workflow where an LLM has **some** autonomy over control flow, on a spectrum:\n\n1. **Single call** — one prompt, one answer.\n2. **Chain** — fixed sequence of calls.\n3. **Router** — model picks a branch, then a fixed path runs.\n4. **Orchestrator-worker** — model decomposes a task and dispatches sub-tasks.\n5. **Autonomous agent** — model loops freely with tools until it decides it is done.\n\nCost, latency, and unpredictability all rise as you go down the list.",
    explanation: "The strong answer names the design principle: push as much structure into code as you can, and give the model autonomy only where the problem genuinely requires it. Most 'agent' products in the wild are levels 2–4, not level 5.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-018",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "intermediate",
    tags: ["orchestration", "n-1.2"],
    front: "What is orchestration in an LLM application?",
    back: "The layer that coordinates everything around the model calls: prompt assembly, retrieval, tool execution, routing, retries, fallbacks, caching, streaming, state, and observability.\n\nFrameworks (LangChain, LlamaIndex, Semantic Kernel, LangGraph) provide it; plenty of production systems write it by hand because the framework abstractions leak.\n\nWhatever you choose, the orchestrator is where **reliability engineering** lives — the model itself has no retries, no timeouts, and no idempotency.",
    explanation: "A good senior answer notes that framework choice is reversible if you keep provider calls behind your own interface, and that the hard parts (evaluation, observability, cost control) are not solved by any framework.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-019",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "advanced",
    tags: ["transformer", "attention", "architecture"],
    front: "How does the transformer's self-attention mechanism work, conceptually?",
    back: "Each token is projected into three vectors: **Query**, **Key**, and **Value**.\n\n1. Compare every token's Query against every other token's Key → attention scores.\n2. Softmax the scores → weights that sum to 1.\n3. Output = weighted sum of the Value vectors.\n\nSo each position builds a representation that is a *mixture of every other position*, weighted by relevance. **Multi-head** attention runs this several times in parallel with different projections, letting different heads track different relationships (syntax, coreference, topic).\n\nCost is **O(n²)** in sequence length — which is why long context is expensive.",
    explanation: "The O(n²) point is the one with engineering consequences: it explains long-context pricing, why FlashAttention and sliding-window/sparse attention exist, and why doubling your prompt can more than double your latency.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-020",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "advanced",
    tags: ["transformer", "architecture", "bert"],
    front: "Why are most modern LLMs *decoder-only*, and what are the alternatives?",
    back: "Three transformer families:\n\n- **Encoder-only** (BERT) — bidirectional, sees the whole input at once. Great for classification, NER, and **embeddings**. Cannot generate fluently.\n- **Decoder-only** (GPT, Llama, Claude) — causal/masked attention, each token sees only the past. Generates autoregressively.\n- **Encoder-decoder** (T5, BART) — encode the input fully, decode the output. Natural fit for translation and summarization.\n\nDecoder-only won for general-purpose models because a single next-token objective scales cleanly and unifies every task as text-in/text-out.",
    explanation: "Note the practical split still in use: your generation model is decoder-only, but your *embedding* model is usually encoder-only. Knowing why is a nice signal in an AI-engineering interview.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-021",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "advanced",
    tags: ["rlhf", "dpo", "alignment"],
    front: "What is RLHF, and what are DPO / Constitutional AI?",
    back: "**RLHF** (Reinforcement Learning from Human Feedback) aligns a pre-trained model with human preferences:\n1. **SFT** — supervised fine-tuning on demonstration data.\n2. **Reward model** — trained on human rankings of candidate outputs.\n3. **RL** (typically PPO) — optimise the policy against the reward model.\n\nAlternatives that avoid the RL stage:\n- **DPO** (Direct Preference Optimization) — optimise directly on preference pairs; simpler and more stable.\n- **Constitutional AI / RLAIF** — an AI critiques and revises outputs against a written set of principles, reducing the human labelling burden.\n\nThis alignment stage is what turns a raw next-token predictor into something that follows instructions and refuses harmful requests.",
    explanation: "You rarely do this yourself as an application engineer, but you must know it exists — it explains why models refuse things, why 'jailbreak' prompts target the alignment layer specifically, and why base models behave so differently from chat models.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-022",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "advanced",
    tags: ["moe", "architecture", "inference-cost"],
    front: "What is a Mixture-of-Experts (MoE) model?",
    back: "An architecture where the feed-forward layer is split into many **expert** sub-networks, and a lightweight **router** activates only a few (often 2) per token.\n\nConsequence: **total parameters** are huge but **active parameters** per token are small — so you get large-model quality at small-model inference cost.\n\nTrade-offs: all experts must be held in memory, load balancing across experts is a training headache, and latency can be less predictable.",
    explanation: "When a spec sheet says '8×7B', that is MoE notation — not 56B of compute per token. Being able to separate total from active parameters is a good signal when discussing self-hosting cost.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-023",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "intermediate",
    tags: ["reasoning-models", "cost", "routing"],
    front: "What is a *reasoning* model and when should you use one?",
    back: "A model trained to generate an extended internal chain of thought before its final answer, spending far more inference compute on hard problems ('test-time compute').\n\nUse when: multi-step maths, complex code, planning, hard debugging, or anything where a wrong intermediate step ruins the answer.\n\nAvoid when: latency matters, the task is simple extraction/classification/formatting, or cost per call is tight — reasoning tokens are billed and can dwarf the visible output.",
    explanation: "The practical architecture answer is a **router**: send hard requests to a reasoning model and easy ones to a fast cheap model. That kind of cost-aware routing is exactly what the matrix's 'cost/performance optimization' rows are testing.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-024",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["ml-basics", "supervised-learning", "n-1.2"],
    front: "Supervised, unsupervised, and reinforcement learning — what is the difference?",
    back: "- **Supervised (SL)** — labelled data; learn input → output. Classification, regression. (Spam detection.)\n- **Unsupervised** — unlabelled data; find structure. Clustering, dimensionality reduction, **and the pre-training objective of LLMs** (self-supervised: the label is the next token, extracted from the data itself).\n- **Reinforcement (RL)** — an agent takes actions in an environment and learns from a reward signal. Used in LLMs at the alignment stage (RLHF).\n\n**Semi-supervised** and **self-supervised** sit between the first two.",
    explanation: "The matrix lists 'SL' explicitly in the AI-fundamentals row. The interesting nuance to raise: LLM pre-training is best described as self-supervised, because the supervision signal is generated from the raw text rather than annotated by humans.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-025",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["neural-networks", "n-1.2"],
    front: "What is a neural network, in the terms a GenAI engineer needs?",
    back: "Layers of units, each computing `activation(W·x + b)`, stacked so that each layer transforms the previous layer's representation.\n\n- **Weights (W)** and **biases (b)** are the learned parameters.\n- **Backpropagation** computes the gradient of a loss with respect to every parameter.\n- **Gradient descent** nudges parameters to reduce the loss.\n- **Activation functions** (ReLU, GELU, SoftMax) introduce the non-linearity that makes depth useful.\n\nAn LLM is this, with attention layers, at a scale of billions of parameters.",
    explanation: "You will not implement backprop in an application role, but you need the vocabulary to read a model card, understand what a 'parameter count' means for memory, and follow a discussion about fine-tuning.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-026",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "intermediate",
    tags: ["nlp", "cost", "n-1.2"],
    front: "What is NLP, and which classic NLP tasks still matter in a GenAI stack?",
    back: "**NLP** is the field of making machines process human language. LLMs subsumed most of it, but several classic components are still load-bearing in production:\n\n- **Tokenization** — BPE / SentencePiece; determines cost and context usage.\n- **NER** — extracting entities for metadata filtering and PII redaction.\n- **Sentence segmentation** — a prerequisite for good chunking.\n- **Lemmatization / stemming** — still used by BM25 in hybrid search.\n- **Classification** — a small fine-tuned encoder is far cheaper than an LLM for routing and moderation.\n\nThe lesson: use an LLM where it earns its cost, and a small specialised model everywhere else.",
    explanation: "'Use the cheapest model that solves the task' is a recurring theme in senior AI-engineering interviews. Reaching for a frontier LLM to do sentiment classification on a million rows is a cost-design failure.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-027",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["sampling", "temperature", "n-1.1"],
    front: "Temperature, top-p, top-k, and seed — what does each control?",
    back: "All four shape **sampling** from the model's output distribution:\n\n- **Temperature** — scales the logits. `0` ≈ greedy/deterministic, `1` = the model's natural distribution, `>1` flatter and more random.\n- **Top-k** — sample only from the k highest-probability tokens.\n- **Top-p (nucleus)** — sample from the smallest set of tokens whose cumulative probability ≥ p. Adapts to the distribution's shape, unlike top-k.\n- **Seed** — fixes the pseudo-random draw so a run is reproducible.\n\nRule of thumb: adjust **one** of temperature or top-p, not both.",
    explanation: "Interviewers like the caveat: even at temperature 0 with a fixed seed, output is not guaranteed identical across runs — floating-point non-determinism on GPUs, batching, and silent model updates all break reproducibility. Design tests that assert on semantics, not exact strings.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-028",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "intermediate",
    tags: ["determinism", "testing", "evaluation", "n-1.1"],
    front: "What does *determinism* mean for LLM applications, and how do you test them?",
    back: "LLMs are **not deterministic** in the way normal code is. Even `temperature=0` plus a seed only reduces variance — GPU floating-point non-associativity, request batching, and provider-side model updates all cause drift.\n\nTesting strategies that survive this:\n- Assert on **structure** (valid JSON, required fields) not exact text.\n- Assert on **semantics** via an embedding-similarity threshold or an LLM judge.\n- **Golden datasets** with accepted-range metrics, run as an eval suite, not as unit tests.\n- **Record/replay** cached responses for deterministic CI.\n- Pin **model versions**, never floating aliases like `-latest`, in production.",
    explanation: "That last point is the one that bites teams in production: a provider silently rotating what `-latest` points to has broken more prompt-tuned pipelines than any code change. Version pinning plus an eval suite is the professional answer.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-029",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "advanced",
    tags: ["parameters", "quantization", "self-hosting"],
    front: "What are parameters, and what do they tell you about running a model?",
    back: "**Parameters** are the learned weights. Count is the headline size figure (7B, 70B, 405B).\n\nMemory rule of thumb for inference:\n- FP16/BF16 → **~2 GB per billion** parameters\n- INT8 → ~1 GB per billion\n- INT4 → ~0.5 GB per billion\n\nSo a 70B model needs ~140 GB in FP16 — multiple A100/H100s — but ~35 GB at INT4, which fits a single 48 GB card. **Quantization** trades a little quality for a lot of memory.\n\nAnd you still need headroom for the **KV cache**, which grows with batch size and context length.",
    explanation: "This is the question that separates 'I call an API' from 'I could deploy this'. Even if you only ever use hosted models, the arithmetic explains the pricing you're charged and why long contexts cost disproportionately more.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-030",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "intermediate",
    tags: ["model-selection", "cost", "routing"],
    front: "How do you choose between a frontier model, a small model, and a fine-tuned model?",
    back: "Decide on four axes: **quality, latency, cost, control**.\n\n| Situation | Choice |\n|---|---|\n| Hard reasoning, low volume, quality decisive | Frontier / reasoning model |\n| High volume, narrow well-defined task | Small model, possibly fine-tuned |\n| Strict data residency or air-gap | Self-hosted open-weights |\n| Prototype, requirements still moving | Best hosted model, optimise later |\n\nThe mature pattern is **cascade / routing**: try the cheap model first, escalate to the expensive one only when a confidence check or validator fails.",
    explanation: "Always mention that you would measure this rather than guess: build the eval set first, then benchmark candidate models against it on quality, p95 latency, and cost per 1k requests. 'We chose it because it was best on our evals' is the answer interviewers want.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-fc-031",
    type: "flashcard",
    topic: "ai-fundamentals",
    difficulty: "advanced",
    tags: ["architecture", "rag", "agents"],
    front: "What are the main AI/ML architecture patterns for a GenAI application?",
    back: "From simplest to most involved:\n\n1. **Direct prompting** — one call, prompt-engineered. Start here.\n2. **RAG** — retrieval grounds the answer in your data.\n3. **Tool-using agent** — the model calls APIs to read and act.\n4. **Router / cascade** — classify the request, dispatch to the right model or pipeline.\n5. **Multi-agent** — specialised agents with an orchestrator.\n6. **Fine-tuned / distilled model** — behaviour baked into weights for cost or latency.\n\nThey compose: a production system is often a router in front of a RAG pipeline whose generation step is a tool-using agent.",
    explanation: "The matrix's Advanced tier asks you to 'evaluate and compare architectural approaches'. The evaluation criteria to name: accuracy on your eval set, p95 latency, cost per request, failure modes, and how hard it is to debug when it goes wrong.",
    createdAt: "2026-08-26",
  },
  // ── Multiple choice ───────────────────────────────────────────
  {
    id: "ai-mc-001",
    type: "multiple-choice",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["hallucination", "grounding", "rag"],
    prompt: "An LLM confidently states a version number for your internal library that does not exist. What is the *most* effective fix?",
    options: [
      { id: "a", text: "Set temperature to 0" },
      { id: "b", text: "Increase the context window size" },
      { id: "c", text: "Add 'Do not hallucinate' to the system prompt" },
      { id: "d", text: "Add the library docs to the prompt via retrieval and instruct the model to answer only from that context" },
    ],
    correctOptionId: "d",
    timeLimitSeconds: 45,
    explanation: "Temperature 0 makes a wrong answer reproducible, not correct. A bigger window is empty capacity. Instructing a model not to hallucinate has almost no effect, because it cannot tell that it is doing so. Supplying the actual facts and constraining the model to them is the only structural fix.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-mc-002",
    type: "multiple-choice",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["context-window", "tokens"],
    prompt: "Your prompt is 8,000 tokens and you request a maximum of 2,000 output tokens against a model with an 8,192-token context window. What happens?",
    options: [
      { id: "a", text: "It works, but the prompt is silently truncated to 6,192 tokens" },
      { id: "b", text: "It works and the output is truncated to 192 tokens" },
      { id: "c", text: "The request fails — prompt plus requested output exceeds the window" },
      { id: "d", text: "It works; the output window is separate from the input window" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 45,
    explanation: "The context window covers input and output together. Most providers validate up front and reject the request rather than truncating, because silent truncation would corrupt the system prompt. Budget explicitly: max_output = window − prompt_tokens − safety margin.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-mc-003",
    type: "multiple-choice",
    topic: "ai-fundamentals",
    difficulty: "intermediate",
    tags: ["fine-tuning", "rag"],
    prompt: "Which task is a fine-tuned model a *better* fit for than RAG?",
    options: [
      { id: "a", text: "Making the model always reply in your company's specific JSON schema and terse house style" },
      { id: "b", text: "Answering questions about documents added to your wiki this morning" },
      { id: "c", text: "Citing the source paragraph for each claim" },
      { id: "d", text: "Reducing hallucination about product pricing" },
    ],
    correctOptionId: "a",
    timeLimitSeconds: 45,
    explanation: "Fine-tuning teaches form and behaviour; RAG supplies facts. Fresh data, citations, and factual accuracy about mutable business data all belong to retrieval. A style or schema constraint that must hold across every call is the classic fine-tuning win — though a good system prompt often gets you most of the way for free.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-mc-004",
    type: "multiple-choice",
    topic: "ai-fundamentals",
    difficulty: "intermediate",
    tags: ["embeddings", "migration"],
    prompt: "You switch your embedding model from `text-embedding-3-small` (1536-d) to a 768-d open-source model. What must you do?",
    options: [
      { id: "a", text: "Only re-embed documents added after the switch" },
      { id: "b", text: "Pad the new 768-d vectors with zeros to 1536 dimensions" },
      { id: "c", text: "Re-embed and re-index the entire corpus" },
      { id: "d", text: "Nothing — the vector database normalizes dimensions automatically" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 50,
    explanation: "Embedding spaces are model-specific; vectors from two models are not comparable even at equal dimensionality, and zero-padding produces meaningless distances. Changing the embedding model is a full re-index — treat it as a data migration with a shadow index and a cutover, not a config tweak.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-mc-005",
    type: "multiple-choice",
    topic: "ai-fundamentals",
    difficulty: "beginner",
    tags: ["tool-calling", "agents", "security"],
    prompt: "What does the model actually do when it 'calls a tool'?",
    options: [
      { id: "a", text: "Emits a structured request naming a function and its arguments, which your code must execute" },
      { id: "b", text: "Executes the function inside the model provider's sandbox and returns the result" },
      { id: "c", text: "Makes an outbound HTTP request from the model to your API" },
      { id: "d", text: "Rewrites its own weights to include the function" },
    ],
    correctOptionId: "a",
    timeLimitSeconds: 45,
    explanation: "The model only proposes. Your application parses the call, authorizes it, executes it, and feeds the result back as a message. This is the whole basis of agent security: a prompt-injected model can propose a destructive call, and your authorization layer is the only thing that stops it.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-mc-006",
    type: "multiple-choice",
    topic: "ai-fundamentals",
    difficulty: "advanced",
    tags: ["moe", "inference-cost"],
    prompt: "A `8x7B` Mixture-of-Experts model activates 2 experts per token. Roughly what inference compute does each token cost?",
    options: [
      { id: "a", text: "The same as a 56B dense model" },
      { id: "b", text: "The same as a 7B dense model, with only 7B needing to be in memory" },
      { id: "c", text: "The same as a 47B dense model" },
      { id: "d", text: "Close to a ~13B dense model, while total weights are ~47B and must all fit in memory" },
    ],
    correctOptionId: "d",
    timeLimitSeconds: 60,
    explanation: "Compute tracks *active* parameters (roughly two experts plus the shared layers), but memory tracks *total* parameters, since the router may pick any expert on any token. That gap — cheap to run, expensive to host — is the whole point of MoE and the thing to get right when sizing hardware.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-mc-007",
    type: "multiple-choice",
    topic: "ai-fundamentals",
    difficulty: "advanced",
    tags: ["transformer", "attention", "cost"],
    prompt: "Why does self-attention make long contexts disproportionately expensive?",
    options: [
      { id: "a", text: "Longer prompts require more model parameters to be loaded" },
      { id: "b", text: "The tokenizer slows down linearly with input size" },
      { id: "c", text: "Long contexts force the provider to use a larger model" },
      { id: "d", text: "Attention compares every token with every other token, so cost grows with the square of sequence length" },
    ],
    correctOptionId: "d",
    timeLimitSeconds: 50,
    explanation: "The n² term in attention is why 100k-token prompts cost far more than 10× a 10k-token prompt in latency terms, and why FlashAttention, sliding-window attention, and prompt caching exist. It is also a good argument for retrieving 5 relevant chunks instead of dumping 200 pages into the prompt.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-mc-008",
    type: "multiple-choice",
    topic: "ai-fundamentals",
    difficulty: "intermediate",
    tags: ["agents", "architecture"],
    prompt: "Which of these is the clearest sign you should build a fixed chain rather than an autonomous agent?",
    options: [
      { id: "a", text: "The task requires calling more than one external API" },
      { id: "b", text: "The task needs data from a database" },
      { id: "c", text: "The output must be JSON" },
      { id: "d", text: "The sequence of steps is known in advance and identical for every request" },
    ],
    correctOptionId: "d",
    timeLimitSeconds: 45,
    explanation: "Agents earn their unpredictability only when the required steps genuinely cannot be known ahead of time. Multiple APIs, database access, and structured output are all perfectly well served by a deterministic chain — which is cheaper, faster, testable, and debuggable.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-mc-009",
    type: "multiple-choice",
    topic: "ai-fundamentals",
    difficulty: "intermediate",
    tags: ["model-selection", "cost", "distillation"],
    prompt: "You need to classify 5 million support tickets into 12 categories. What is the most defensible architecture?",
    options: [
      { id: "a", text: "Send all 5 million tickets to a frontier LLM with a good prompt" },
      { id: "b", text: "Use a reasoning model for maximum accuracy" },
      { id: "c", text: "Build a RAG pipeline over the ticket history" },
      { id: "d", text: "Label a sample with a frontier model, fine-tune or train a small classifier, and run that at volume" },
    ],
    correctOptionId: "d",
    timeLimitSeconds: 60,
    explanation: "This is the distillation pattern: use the expensive model once to create labels, then serve with a small model that costs orders of magnitude less per item. RAG is the wrong tool — there is nothing to retrieve — and a reasoning model on 5M items is a budget incident, not an architecture.",
    createdAt: "2026-08-26",
  },
  {
    id: "ai-mc-010",
    type: "multiple-choice",
    topic: "ai-fundamentals",
    difficulty: "intermediate",
    tags: ["temperature", "determinism", "testing"],
    prompt: "Which statement about temperature 0 is correct?",
    options: [
      { id: "a", text: "It makes the model fully deterministic and reproducible" },
      { id: "b", text: "It improves factual accuracy because the model becomes more careful" },
      { id: "c", text: "It disables sampling entirely, so the seed parameter becomes required" },
      { id: "d", text: "It reduces output variance but does not guarantee identical output across runs or make answers more truthful" },
    ],
    correctOptionId: "d",
    timeLimitSeconds: 50,
    explanation: "Greedy decoding still runs on GPUs with non-associative floating-point arithmetic and variable batching, and providers update models behind stable-looking names. Temperature 0 buys you consistency of *style*, not truth and not bit-for-bit reproducibility — which is why LLM tests assert on structure and semantics.",
    createdAt: "2026-08-26",
  },
];
