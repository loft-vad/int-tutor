import type { Question } from '@/types/content';

export const typescriptQuestions: Question[] = [
  {
    id: 'ts-001',
    type: 'flashcard',
    topic: 'typescript',
    difficulty: 'beginner',
    tags: ['type-inference', 'basics'],
    front: 'What is TypeScript\'s type inference?',
    back: 'TypeScript **automatically infers types** from values without explicit annotations:\n\n```ts\nlet name = "Alice";       // inferred: string\nlet count = 0;            // inferred: number\nconst arr = [1, 2, 3];    // inferred: number[]\n\nfunction add(a: number, b: number) {\n  return a + b; // inferred return: number\n}\n```\n\nTypeScript is "structurally typed" — if it looks like a duck, it is a duck.',
    explanation: 'Rely on inference where possible; annotate function parameters and return types for public APIs.',
    createdAt: '2024-01-01',
  },
  {
    id: 'ts-002',
    type: 'multiple-choice',
    topic: 'typescript',
    difficulty: 'intermediate',
    tags: ['generics', 'type-parameters'],
    prompt: 'What does this function signature mean?\n```ts\nfunction identity<T>(arg: T): T\n```',
    options: [
      { id: 'a', text: 'The function takes and returns any value' },
      { id: 'b', text: 'The return type is always the same as the input type, preserving type information' },
      { id: 'c', text: 'T must be a string' },
      { id: 'd', text: 'The function only works with objects' },
    ],
    correctOptionId: 'b',
    timeLimitSeconds: 30,
    explanation: 'Generics let you write type-safe code that works with any type. Unlike `any`, generics preserve the specific type so TypeScript knows the return type matches the input.',
    createdAt: '2024-01-01',
  },
  {
    id: 'ts-003',
    type: 'flashcard',
    topic: 'typescript',
    difficulty: 'intermediate',
    tags: ['utility-types', 'Partial', 'Required', 'Pick', 'Omit'],
    front: 'What are the common TypeScript utility types?',
    back: '- `Partial<T>` — all properties optional\n- `Required<T>` — all properties required\n- `Pick<T, K>` — keep only keys K\n- `Omit<T, K>` — remove keys K\n- `Record<K, V>` — object type with keys K and values V\n- `Readonly<T>` — all properties readonly\n- `ReturnType<T>` — extract return type of a function\n- `Parameters<T>` — extract parameter types as tuple\n- `NonNullable<T>` — remove null/undefined',
    explanation: 'Utility types let you transform existing types rather than redefining them, reducing duplication.',
    createdAt: '2024-01-01',
  },
  {
    id: 'ts-004',
    type: 'multiple-choice',
    topic: 'typescript',
    difficulty: 'intermediate',
    tags: ['type-narrowing', 'type-guards'],
    prompt: 'Which is the correct way to narrow a union type `string | number` to `string`?',
    options: [
      { id: 'a', text: '`if (value.isString())`' },
      { id: 'b', text: '`if (value as string)`' },
      { id: 'c', text: '`if (typeof value === "string")`' },
      { id: 'd', text: '`if (value instanceof String)`' },
    ],
    correctOptionId: 'c',
    timeLimitSeconds: 30,
    explanation: 'TypeScript uses control flow analysis to narrow types. `typeof` checks narrow primitive unions. `instanceof` narrows class instances. Type assertions (`as`) bypass narrowing without checking.',
    createdAt: '2024-01-01',
  },
  {
    id: 'ts-005',
    type: 'flashcard',
    topic: 'typescript',
    difficulty: 'advanced',
    tags: ['discriminated-unions', 'pattern-matching'],
    front: 'What is a discriminated union in TypeScript?',
    back: 'A union of types that each have a **common literal property** (the discriminant) allowing TypeScript to narrow the type:\n\n```ts\ntype Shape =\n  | { kind: "circle"; radius: number }\n  | { kind: "square"; side: number };\n\nfunction area(s: Shape): number {\n  switch (s.kind) {\n    case "circle": return Math.PI * s.radius ** 2;\n    case "square": return s.side ** 2;\n  }\n}\n```\n\nTypeScript knows `s.radius` is only available when `kind === "circle"`.',
    explanation: 'Discriminated unions are the TypeScript-idiomatic way to model algebraic data types. Prefer them over class hierarchies for data modeling.',
    createdAt: '2024-01-01',
  },
  {
    id: 'ts-006',
    type: 'coding-challenge',
    topic: 'typescript',
    difficulty: 'advanced',
    tags: ['generics', 'mapped-types', 'utility-types'],
    mode: 'complete',
    prompt: 'Implement a generic `DeepReadonly<T>` type that makes all nested properties readonly.',
    starterCode: `// Make all nested properties readonly
type DeepReadonly<T> = // your implementation here

// Test:
type Config = {
  server: { host: string; port: number };
  db: { url: string; name: string };
};

type ReadonlyConfig = DeepReadonly<Config>;
// ReadonlyConfig.server.host should be readonly`,
    solutionCode: `type DeepReadonly<T> = {
  readonly [K in keyof T]: T[K] extends object ? DeepReadonly<T[K]> : T[K];
};

type Config = {
  server: { host: string; port: number };
  db: { url: string; name: string };
};

type ReadonlyConfig = DeepReadonly<Config>;
// All nested properties are now readonly`,
    language: 'typescript',
    hints: [
      'Use a mapped type: `{ readonly [K in keyof T]: ... }`',
      'For object properties, recursively apply DeepReadonly',
      'Use a conditional type to check if a property is an object: `T[K] extends object ? ... : T[K]`',
    ],
    explanation: 'Mapped types let you transform every property of a type. Recursive conditional types enable deep transformations.',
    createdAt: '2024-01-01',
  },
  {
    id: 'ts-007',
    type: 'multiple-choice',
    topic: 'typescript',
    difficulty: 'advanced',
    tags: ['infer', 'conditional-types'],
    prompt: 'What does `infer` do in TypeScript conditional types?',
    options: [
      { id: 'a', text: 'Forces TypeScript to infer a type automatically' },
      { id: 'b', text: 'Declares a type variable to be captured within a conditional type pattern' },
      { id: 'c', text: 'Marks a type as inferred from runtime values' },
      { id: 'd', text: 'Only works with function types' },
    ],
    correctOptionId: 'b',
    timeLimitSeconds: 30,
    explanation: '`infer R` in `T extends (...args: any) => infer R ? R : never` captures the return type into variable R. It\'s used to extract parts of a type pattern.',
    createdAt: '2024-01-01',
  },
  {
    id: 'ts-008',
    type: 'flashcard',
    topic: 'typescript',
    difficulty: 'intermediate',
    tags: ['satisfies', 'type-checking'],
    front: 'What does the `satisfies` operator do in TypeScript 4.9+?',
    back: 'The `satisfies` operator validates that an expression matches a type **without widening** the type:\n\n```ts\ntype Color = "red" | "green" | "blue";\ntype Palette = Record<string, Color | [number, number, number]>;\n\n// satisfies checks the type but keeps narrow inference\nconst palette = {\n  red: [255, 0, 0],\n  green: "green",\n} satisfies Palette;\n\npalette.red[0]; // ✅ still typed as number[] (not widened)\npalette.green.toUpperCase(); // ✅ still typed as string\n```',
    explanation: '`satisfies` is preferable to type annotation when you want type-checking without losing precise literal/tuple types.',
    createdAt: '2024-01-01',
  },
  {
    id: 'ts-009',
    type: 'multiple-choice',
    topic: 'typescript',
    difficulty: 'intermediate',
    tags: ['interface', 'type-alias', 'difference'],
    prompt: 'Which is **only possible with `interface`** and not `type` alias?',
    options: [
      { id: 'a', text: 'Extending another type' },
      { id: 'b', text: 'Declaration merging (extending across files)' },
      { id: 'c', text: 'Describing function types' },
      { id: 'd', text: 'Using generics' },
    ],
    correctOptionId: 'b',
    timeLimitSeconds: 30,
    explanation: 'Interfaces support declaration merging — multiple declarations with the same name are merged. This is used by libraries to let users augment types. Type aliases cannot be merged.',
    createdAt: '2024-01-01',
  },
  {
    id: 'ts-010',
    type: 'coding-challenge',
    topic: 'typescript',
    difficulty: 'intermediate',
    tags: ['generics', 'type-safety'],
    mode: 'fix',
    prompt: 'Fix the type errors in this generic function that extracts a property from an array of objects.',
    starterCode: `function pluck(arr, key) {
  return arr.map(item => item[key]);
}

const users = [{ name: "Alice", age: 30 }, { name: "Bob", age: 25 }];
const names = pluck(users, "name"); // should be string[]`,
    solutionCode: `function pluck<T, K extends keyof T>(arr: T[], key: K): T[K][] {
  return arr.map(item => item[key]);
}

const users = [{ name: "Alice", age: 30 }, { name: "Bob", age: 25 }];
const names = pluck(users, "name");   // string[]
const ages = pluck(users, "age");     // number[]
// pluck(users, "invalid"); // ❌ TypeScript error`,
    language: 'typescript',
    hints: [
      'Use two type parameters: T for the array element type, K for the key',
      'Constrain K with `extends keyof T` to only allow valid keys',
      'The return type is `T[K][]` — an array of the property value type',
    ],
    explanation: 'Using `K extends keyof T` creates a constraint that ensures only valid property names are accepted, and `T[K]` gives you the exact return type.',
    createdAt: '2024-01-01',
  },
  {
    id: 'ts-011',
    type: 'flashcard',
    topic: 'typescript',
    difficulty: 'advanced',
    tags: ['template-literal-types'],
    front: 'What are template literal types in TypeScript?',
    back: 'Template literal types let you create new string types by combining existing ones:\n\n```ts\ntype Greeting = `Hello, ${string}`;\ntype EventName = "click" | "focus" | "blur";\ntype Handler = `on${Capitalize<EventName>}`;\n// "onClick" | "onFocus" | "onBlur"\n\ntype CSSProperty = `${string}-${string}`;\n// matches "background-color", "font-size", etc.\n```',
    explanation: 'Template literal types are powerful for creating type-safe string unions, useful for event names, CSS properties, route paths, etc.',
    createdAt: '2024-01-01',
  },
  // ── Flashcards ────────────────────────────────────────────────
  {
    id: "ts-fc-001",
    type: "flashcard",
    topic: "typescript",
    difficulty: "beginner",
    tags: ["types", "basics", "gfg"],
    front: "What data types does TypeScript provide?",
    back: "**Primitives:** `string`, `number` (all numbers are floats), `boolean`, `bigint`, `symbol`, `null`, `undefined`.\n\n**Special:**\n- `any` — opt out of type checking entirely.\n- `unknown` — the type-safe `any`; must be narrowed before use.\n- `never` — a value that never occurs (a function that always throws, or an impossible union branch).\n- `void` — a function returns nothing meaningful.\n\n**Object types:** `object`, arrays (`string[]` / `Array<string>`), **tuples** (`[string, number]`), `enum`, and any interface/class/type alias.\n\nTypeScript adds only the *type layer*; at runtime everything is plain JavaScript.",
    explanation: "'All numbers are floats' matters in practice — there is no `int`, so integer overflow and precision bugs are JavaScript's, not TypeScript's. `bigint` is the escape hatch for large integers.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-002",
    type: "flashcard",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["any", "unknown", "never", "gfg"],
    front: "`any` vs `unknown` vs `never` — when do you use each?",
    back: "```ts\nlet a: any = JSON.parse(s);\na.foo.bar();              // compiles; may explode at runtime\n\nlet u: unknown = JSON.parse(s);\nu.foo;                    // Error — must narrow first\nif (typeof u === 'object' && u !== null && 'foo' in u) { /* ok */ }\n\nfunction fail(msg: string): never { throw new Error(msg); }\n```\n\n- **`any`** — disables checking, and it's **contagious**: anything derived from it is also `any`. Use it as a last resort, and prefer a `// TODO` comment explaining why.\n- **`unknown`** — the correct type for values from outside your program: `JSON.parse`, `fetch`, user input, `catch` clauses. Forces you to validate.\n- **`never`** — the empty type. Used for exhaustiveness checking and for functions that never return.",
    explanation: "The exhaustiveness idiom is the most valuable use of `never`: `const _exhaustive: never = value` in a `switch` default makes the compiler fail when someone adds a variant to a union. That's a genuine safety net, not a type-system curiosity.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-003",
    type: "flashcard",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["interfaces", "type-aliases", "gfg"],
    front: "Interfaces vs type aliases — what are the real differences?",
    back: "```ts\ninterface User { name: string }\ninterface User { age: number }        // declaration merging → { name, age }\n\ntype Point = { x: number };\n// type Point = { y: number }         // Error: duplicate identifier\n```\n\n| | `interface` | `type` |\n|---|---|---|\n| Declaration merging | Yes | No |\n| Extends | `extends` (faster to check) | intersections `&` |\n| Unions / primitives / tuples | No | Yes |\n| Mapped & conditional types | No | Yes |\n| Error messages | Usually clearer | Can expand into large structural types |\n\n**Guidance:** `interface` for object shapes and public APIs (merging enables module augmentation, and it's slightly faster for the compiler); `type` for unions, tuples, and anything computed.",
    explanation: "Declaration merging is not just trivia — it's how you augment third-party types (`declare module 'express' { interface Request { user?: User } }`). That's a common real task and it only works with interfaces.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-004",
    type: "flashcard",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["unions", "narrowing", "gfg"],
    front: "How do union types and type narrowing work?",
    back: "```ts\ntype Result = { ok: true; data: string } | { ok: false; error: Error };\n\nfunction handle(r: Result) {\n  if (r.ok) {\n    r.data;      // narrowed to the success branch\n  } else {\n    r.error;     // narrowed to the failure branch\n  }\n}\n```\n\nNarrowing mechanisms: `typeof`, `instanceof`, `in`, truthiness, equality, **discriminated unions** (a shared literal-typed field), and user-defined **type predicates** (`x is Foo`).\n\nA **discriminated union** is the single most useful TypeScript pattern — it turns runtime branching into compiler-verified exhaustiveness.",
    explanation: "When asked 'what's your favourite TypeScript feature', discriminated unions plus exhaustiveness checking is a strong answer, because it prevents a whole class of bug: adding a new state and forgetting to handle it somewhere.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-005",
    type: "flashcard",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["type-guards", "narrowing", "gfg"],
    front: "What is a type predicate, and what is an assertion function?",
    back: "```ts\n// Type predicate — narrows in the true branch\nfunction isUser(v: unknown): v is User {\n  return typeof v === 'object' && v !== null && 'id' in v;\n}\n\n// Assertion function — narrows for the rest of the scope\nfunction assertUser(v: unknown): asserts v is User {\n  if (!isUser(v)) throw new Error('not a user');\n}\n\nconst data: unknown = await res.json();\nassertUser(data);\ndata.id;                      // narrowed from here on\n```\n\nBoth are **unchecked promises to the compiler** — TypeScript trusts your logic without verifying it. A buggy predicate silently reintroduces unsoundness.\n\nNote: an assertion function must be declared with an explicit type annotation, not inferred (`const f = (v): asserts v is T => ...` fails).",
    explanation: "For validating external data, prefer a schema library (Zod, Valibot) that *derives* the type from the runtime validator. Then the predicate can't drift from the check, which is the failure mode of hand-written type guards.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-006",
    type: "flashcard",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["generics", "constraints", "gfg"],
    front: "What are generics and generic constraints?",
    back: "```ts\nfunction first<T>(arr: T[]): T | undefined { return arr[0]; }\n\n// Constraint: T must have a length\nfunction longest<T extends { length: number }>(a: T, b: T): T {\n  return a.length >= b.length ? a : b;\n}\n\n// keyof constraint — the classic type-safe property getter\nfunction get<T, K extends keyof T>(obj: T, key: K): T[K] {\n  return obj[key];\n}\nget({ a: 1, b: 'x' }, 'b');   // string\nget({ a: 1 }, 'c');           // Error: 'c' is not assignable to 'a'\n\n// Default type parameter\ninterface Box<T = string> { value: T }\n```\n\nGenerics preserve the relationship between input and output types instead of collapsing everything to `any`.",
    explanation: "The `K extends keyof T` pattern is the one to have memorised — it's how every typed ORM, form library, and object utility works, and interviewers use it to check whether you understand generics or just recognise them.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-007",
    type: "flashcard",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["utility-types", "gfg"],
    front: "Which built-in utility types should you know?",
    back: "```ts\nPartial<T>          // all properties optional\nRequired<T>         // all properties required\nReadonly<T>         // all properties readonly\nPick<T, K>          // keep only keys K\nOmit<T, K>          // remove keys K\nRecord<K, V>        // { [key in K]: V }\nExclude<U, X>       // remove members X from union U\nExtract<U, X>       // keep members of U assignable to X\nNonNullable<T>      // remove null | undefined\nParameters<F>       // tuple of a function's parameters\nReturnType<F>       // a function's return type\nAwaited<T>          // unwrap a Promise (recursively)\nInstanceType<C>     // instance type of a constructor\nNoInfer<T>          // block inference at this position (5.4+)\n```\n\n`Omit` is the one to watch: it is **not** key-checked, so `Omit<User, 'emial'>` compiles silently and does nothing.",
    explanation: "That `Omit` gotcha is a good detail. If you need safety, write `Omit<T, Extract<keyof T, K>>` or a key-checked helper — otherwise a typo in a refactor leaves the property in place with no error.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-008",
    type: "flashcard",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["conditional-types", "infer", "gfg"],
    front: "How do conditional types and `infer` work?",
    back: "```ts\ntype IsString<T> = T extends string ? true : false;\n\ntype ElementOf<T> = T extends (infer U)[] ? U : never;\ntype A = ElementOf<number[]>;          // number\n\ntype Unwrap<T> = T extends Promise<infer U> ? Unwrap<U> : T;   // recursive\ntype B = Unwrap<Promise<Promise<string>>>;                     // string\n```\n\n**Distributive** behaviour: when the checked type is a naked type parameter, the conditional distributes over a union:\n```ts\ntype ToArray<T> = T extends any ? T[] : never;\ntype C = ToArray<string | number>;      // string[] | number[]\n```\nWrap in a tuple to prevent distribution: `[T] extends [any] ? ... : ...`.",
    explanation: "Distributivity surprises people — `Exclude` and `NonNullable` are built on it, and the `[T] extends [U]` trick to disable it is the standard workaround when you want to compare a union as a whole rather than member by member.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-009",
    type: "flashcard",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["mapped-types", "template-literals"],
    front: "What are mapped types and key remapping?",
    back: "```ts\ntype Optional<T> = { [K in keyof T]?: T[K] };\ntype Mutable<T>  = { -readonly [K in keyof T]: T[K] };     // remove modifier\ntype Strict<T>   = { [K in keyof T]-?: T[K] };             // remove optionality\n\n// Key remapping with `as` (4.1+)\ntype Getters<T> = {\n  [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K]\n};\ntype G = Getters<{ name: string }>;   // { getName: () => string }\n\n// Filter keys by mapping unwanted ones to never\ntype StringKeys<T> = { [K in keyof T as T[K] extends string ? K : never]: T[K] };\n```\n\nThe `+`/`-` modifiers add or remove `readonly` and `?`.",
    explanation: "Key remapping combined with template literal types is how modern libraries generate typed APIs from a schema. Mapping a key to `never` to filter it out is the idiom to remember — there's no `if` in a mapped type, so that's how you express exclusion.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-010",
    type: "flashcard",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["satisfies", "modern-ts"],
    front: "What is the `satisfies` operator and why is it better than a type annotation?",
    back: "```ts\nconst config = {\n  host: 'localhost',\n  port: 3000,\n} satisfies Record<string, string | number>;\n\nconfig.port.toFixed();     // OK — inferred as number, not string | number\nconfig.host.toUpperCase(); // OK — inferred as string\n```\n\nCompare:\n- `const config: Record<string, string|number> = {...}` — **widens**: `config.port` is `string | number`, and typos in keys are allowed.\n- `const config = {...}` — no validation at all.\n- `satisfies` — **validates against the constraint while preserving the narrow inferred type.**\n\nThe canonical use is a config or route object that must conform to a shape but whose exact literal values you still want to use.",
    explanation: "This is the single most useful TypeScript 4.9+ addition and a reliable signal that a candidate is current. The 'validate without widening' framing is the crisp way to explain it.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-011",
    type: "flashcard",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["as-const", "literal-types"],
    front: "What does `as const` do?",
    back: "A **const assertion** makes a literal deeply readonly and narrows it to its literal type:\n\n```ts\nconst a = ['x', 'y'];              // string[]\nconst b = ['x', 'y'] as const;     // readonly [\"x\", \"y\"]\n\nconst c = { mode: 'dark' };        // { mode: string }\nconst d = { mode: 'dark' } as const; // { readonly mode: \"dark\" }\n\n// Derive a union from a runtime array — no duplication\nconst ROLES = ['admin', 'user', 'guest'] as const;\ntype Role = typeof ROLES[number];  // 'admin' | 'user' | 'guest'\n```\n\nThat last pattern is the important one: one source of truth that exists at both runtime (iterable) and compile time (a union).",
    explanation: "The `typeof ARR[number]` idiom is worth memorising. The alternative — declaring the union *and* the array separately — is a duplication that silently drifts, which is exactly the bug this prevents.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-012",
    type: "flashcard",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["template-literals", "type-level"],
    front: "What are template literal types?",
    back: "Types built from string literals by interpolation:\n\n```ts\ntype Event = `on${Capitalize<'click' | 'focus'>}`;   // 'onClick' | 'onFocus'\n\ntype Route = `/users/${string}`;\nconst r: Route = '/users/42';        // OK\nconst s: Route = '/posts/1';         // Error\n\n// Parse a string type with infer\ntype Split<S extends string, D extends string> =\n  S extends `${infer H}${D}${infer T}` ? [H, ...Split<T, D>] : [S];\ntype P = Split<'a.b.c', '.'>;        // ['a','b','c']\n```\n\nIntrinsic helpers: `Uppercase`, `Lowercase`, `Capitalize`, `Uncapitalize`.\n\nReal uses: typed event names, typed CSS properties, typed route params, and typed object paths for `get(obj, 'a.b.c')`.",
    explanation: "Typed route parameters are the killer application — extracting `:id` from `'/users/:id'` at the type level so a router can type its params object. That's how modern frameworks give you end-to-end typed routing with no code generation.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-013",
    type: "flashcard",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["exhaustiveness", "never", "discriminated-unions"],
    front: "How do you make an exhaustive `switch` that fails to compile when a case is added?",
    back: "```ts\ntype Shape =\n  | { kind: 'circle'; r: number }\n  | { kind: 'square'; s: number };\n\nfunction area(shape: Shape): number {\n  switch (shape.kind) {\n    case 'circle': return Math.PI * shape.r ** 2;\n    case 'square': return shape.s ** 2;\n    default: {\n      const _exhaustive: never = shape;   // ← the guard\n      throw new Error(`Unhandled: ${JSON.stringify(shape)}`);\n    }\n  }\n}\n```\n\nWhen all cases are handled, `shape` narrows to `never` in `default` and the assignment compiles. Add `{ kind: 'triangle' }` to `Shape` and the assignment fails — pointing you at every place that needs updating.",
    explanation: "This turns 'add a variant and hunt for every switch' into a compiler-driven task list. It's the strongest practical argument for discriminated unions over string enums or class hierarchies, and it comes up constantly in real code review.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-014",
    type: "flashcard",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["tsconfig", "compiler-options", "gfg"],
    front: "Which `tsconfig.json` options actually matter?",
    back: "```jsonc\n{\n  \"compilerOptions\": {\n    \"strict\": true,                        // turn on all the strict family\n    \"noUncheckedIndexedAccess\": true,      // arr[i] is T | undefined\n    \"exactOptionalPropertyTypes\": true,    // ?: x means absent, not undefined\n    \"noImplicitOverride\": true,\n    \"noFallthroughCasesInSwitch\": true,\n    \"verbatimModuleSyntax\": true,          // import type is preserved exactly\n    \"isolatedModules\": true,               // safe for esbuild/swc/babel\n    \"target\": \"ES2022\",\n    \"module\": \"NodeNext\",\n    \"moduleResolution\": \"NodeNext\",\n    \"skipLibCheck\": true                   // don't type-check .d.ts deps\n  }\n}\n```\n\n`strict` bundles `strictNullChecks`, `noImplicitAny`, `strictFunctionTypes`, `strictBindCallApply`, `strictPropertyInitialization`, `useUnknownInCatchVariables`, and `alwaysStrict`.",
    explanation: "`noUncheckedIndexedAccess` is the highest-value non-default flag: without it `arr[999]` is typed `T` rather than `T | undefined`, so array access is a lie. It's noisy to adopt in an existing codebase but it catches real crashes.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-015",
    type: "flashcard",
    topic: "typescript",
    difficulty: "beginner",
    tags: ["noimplicitany", "strict", "gfg"],
    front: "What is `noImplicitAny` and why does it matter?",
    back: "Part of `strict`. Without it, a parameter with no annotation is silently `any`:\n\n```ts\nfunction greet(name) { ... }   // noImplicitAny: false → name is any\nfunction greet(name) { ... }   // noImplicitAny: true  → Error\n```\n\nWhy it matters: `any` is **contagious**. One un-annotated parameter makes everything derived from it unchecked, and the hole spreads silently through the codebase — you get the compile-time cost of TypeScript with none of the safety.\n\nMigration path for an existing JS codebase: enable `noImplicitAny` last, after `allowJs` + `checkJs`, and use `// @ts-expect-error` (not `@ts-ignore`) so the suppressions fail once the underlying issue is fixed.",
    explanation: "The `@ts-expect-error` over `@ts-ignore` recommendation is a genuinely useful detail: `@ts-expect-error` itself errors when the line stops having an error, so your suppressions self-clean instead of accumulating forever.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-016",
    type: "flashcard",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["enums", "gfg", "modern-ts"],
    front: "How do TypeScript enums work, and what should you use instead?",
    back: "```ts\nenum Status { Active, Inactive }        // numeric: 0, 1\nenum Role { Admin = 'ADMIN' }           // string enum\nconst enum Dir { Up, Down }             // inlined, no runtime object\n```\n\nProblems with numeric enums:\n- They emit a **runtime object** (so they're not erasable — a problem for `isolatedModules` and Node's type-stripping).\n- **Reverse mapping** means `Status[0] === 'Active'`, which surprises people.\n- Any number is assignable to a numeric enum in older versions.\n- `const enum` doesn't work across module boundaries with isolated transpilation.\n\nModern alternative:\n```ts\nconst Status = { Active: 'active', Inactive: 'inactive' } as const;\ntype Status = typeof Status[keyof typeof Status];\n```\nZero runtime cost beyond a plain object, fully type-safe, and JSON-friendly.",
    explanation: "The erasability point is increasingly relevant — Node's `--experimental-strip-types` and TS 5.8's `erasableSyntaxOnly` reject enums precisely because they emit code. The `as const` object pattern is where the ecosystem is heading.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-017",
    type: "flashcard",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["decorators", "gfg"],
    front: "What are decorators in TypeScript?",
    back: "Functions that modify classes and their members. TypeScript 5.0 ships the **TC39 Stage 3** standard form, which is different from the legacy `experimentalDecorators` form Angular and NestJS use.\n\n```ts\n// Standard (TS 5.0+)\nfunction logged<T, A extends any[], R>(\n  target: (this: T, ...args: A) => R,\n  ctx: ClassMethodDecoratorContext,\n) {\n  return function (this: T, ...args: A): R {\n    console.log(`calling ${String(ctx.name)}`);\n    return target.call(this, ...args);\n  };\n}\n\nclass Api { @logged fetch(url: string) { /* ... */ } }\n```\n\nThe two forms are **not** interchangeable — the legacy form receives `(target, key, descriptor)` and supports parameter decorators and `emitDecoratorMetadata`, which the standard form (as of now) does not.",
    explanation: "That incompatibility is the practically important part: an Angular or NestJS codebase must keep `experimentalDecorators: true`, because those frameworks depend on parameter decorators and reflect-metadata for dependency injection.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-018",
    type: "flashcard",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["this", "classes", "gfg"],
    front: "How do you type `this` in a function?",
    back: "```ts\ninterface Handler { count: number }\n\n// `this` parameter — erased at runtime, purely a type annotation\nfunction increment(this: Handler, by: number) {\n  this.count += by;\n}\n\n// Enable checking with the noImplicitThis flag (part of strict)\nclass Counter {\n  n = 0;\n  // Arrow property: `this` is lexically bound, safe to pass as a callback\n  inc = () => { this.n++; };\n  // Method: `this` depends on the call site\n  dec() { this.n--; }\n}\n\nconst c = new Counter();\nconst f = c.dec;\nf();                          // `this` is undefined — runtime error\n```\n\n`strictBindCallApply` also type-checks `bind`, `call`, and `apply` arguments.",
    explanation: "The lost-`this` bug when passing a method as a callback is a classic. TypeScript can catch it with a `this` parameter, and the arrow-property form avoids it entirely at the cost of one function per instance rather than one on the prototype.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-019",
    type: "flashcard",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["classes", "access-modifiers", "gfg"],
    front: "What are access modifiers and parameter properties in TypeScript classes?",
    back: "```ts\nclass Account {\n  constructor(\n    public readonly id: string,     // parameter property: declares + assigns\n    private balance: number,\n    protected owner: string,\n  ) {}\n\n  #secret = 'x';                    // true JS private field — runtime-enforced\n}\n```\n\n- **`public`** (default), **`private`**, **`protected`** — checked at **compile time only**. `(acct as any).balance` reads it fine at runtime.\n- **`readonly`** — assignable only in the constructor; also compile-time only.\n- **`#field`** — the ECMAScript private field: genuinely inaccessible outside the class at runtime, and not visible via `Object.keys` or `JSON.stringify`.\n\n**Parameter properties** are TypeScript-only sugar that declares and assigns a field from a constructor parameter in one step.",
    explanation: "The compile-time-only nature of `private` is the interview point: it documents intent and stops accidents, but it is not a security boundary. Use `#` when the encapsulation must actually hold at runtime.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-020",
    type: "flashcard",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["classes", "interfaces", "gfg"],
    front: "When should you use a class and when an interface?",
    back: "**Interface** — describes a *shape*. No runtime output, so it's free. Use for data structures, function signatures, and public API contracts.\n\n**Class** — a *runtime construct*: it creates a value (a constructor function), can hold implementation, state, and inheritance, and can be instantiated with `new`.\n\nRules of thumb:\n- Plain data → `interface` or `type`. A class with only public fields and no methods is usually a plain object in disguise.\n- Behaviour + state bound together, or you need `instanceof` → class.\n- A class can `implements` an interface — that's the standard way to enforce a contract on an implementation.\n- In a DI framework (Angular, NestJS) classes double as injection tokens, so they're used for services regardless.",
    explanation: "'A class with no methods should be an interface' is a good heuristic to state — classes cost runtime code and force `new` on consumers, which makes serialization and structural typing harder for no benefit.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-021",
    type: "flashcard",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["declaration-files", "module-augmentation", "gfg"],
    front: "How do you write ambient declarations and augment a module?",
    back: "```ts\n// global.d.ts — declare something that exists at runtime but has no types\ndeclare global {\n  interface Window { __APP_CONFIG__: { apiUrl: string } }\n}\n\ndeclare module '*.svg' {\n  const content: string;\n  export default content;\n}\n\n// Module augmentation — add to a third-party type\ndeclare module 'express' {\n  interface Request { user?: { id: string } }\n}\n\nexport {};   // makes this file a module; required for `declare global`\n```\n\n`.d.ts` files contain **types only** — no implementation, no emitted JavaScript. `declare` tells the compiler 'this exists, trust me'.",
    explanation: "The `export {}` requirement trips people up: without at least one import or export, the file is treated as a global script and `declare global` is an error. Express request augmentation is the most common real-world instance of this.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-022",
    type: "flashcard",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["import-type", "modules", "verbatimmodulesyntax"],
    front: "What is `import type`, and what does `verbatimModuleSyntax` do?",
    back: "```ts\nimport type { User } from './types';        // erased entirely at compile time\nimport { type User, createUser } from './api';  // inline type modifier\n```\n\nWhy it matters:\n- **Avoids runtime imports** for things that only exist in the type system — preventing accidental circular imports and side effects from a module you only needed a type from.\n- Required by **`isolatedModules`** (esbuild, swc, Babel), because a single-file transpiler can't tell whether `User` is a type or a value.\n\n**`verbatimModuleSyntax`** makes the emit rule dead simple: anything with a `type` modifier is dropped, everything else is kept exactly as written. It replaces the confusing `importsNotUsedAsValues` and `preserveValueImports` flags.",
    explanation: "The circular-import angle is the practical one. Importing a type from a module purely for annotation can drag that whole module into the runtime bundle and create a cycle — `import type` makes the dependency compile-time only.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-023",
    type: "flashcard",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["branded-types", "nominal-typing"],
    front: "What is a branded (nominal) type and why would you use one?",
    back: "TypeScript is **structurally** typed, so any two `string`s are interchangeable — including a `UserId` and an `OrderId`.\n\n```ts\ndeclare const brand: unique symbol;\ntype Brand<T, B> = T & { readonly [brand]: B };\n\ntype UserId  = Brand<string, 'UserId'>;\ntype OrderId = Brand<string, 'OrderId'>;\n\nconst asUserId = (s: string) => s as UserId;\n\nfunction getUser(id: UserId) { /* ... */ }\ngetUser(asUserId('u1'));            // OK\ngetUser('u1');                      // Error\ngetUser(orderId);                   // Error — different brand\n```\n\nThe brand exists only in the type system; at runtime it's still a plain string with zero overhead.\n\nUse for IDs, validated values (`Email`, `SanitizedHtml`, `PositiveInt`), and units (`Meters` vs `Feet`).",
    explanation: "The `SanitizedHtml` case is the compelling one: you can make it impossible to pass unsanitised input into a rendering function without going through the sanitizer, enforced by the compiler at zero runtime cost.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-024",
    type: "flashcard",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["using", "resource-management", "modern-ts"],
    front: "What are `using` and `await using` (explicit resource management)?",
    back: "TypeScript 5.2 implements the TC39 explicit resource management proposal — deterministic cleanup at scope exit:\n\n```ts\nclass FileHandle {\n  [Symbol.dispose]() { this.close(); }\n}\n\nfunction read() {\n  using file = open('data.txt');\n  return file.read();\n}   // file[Symbol.dispose]() runs here, even if read() throws\n\nasync function query() {\n  await using conn = await pool.connect();   // Symbol.asyncDispose\n  return conn.query('...');\n}\n```\n\nIt's `try/finally` without the boilerplate, and it composes — several `using` declarations dispose in reverse order.\n\nRequires `Symbol.dispose` polyfills on older runtimes and `lib: ESNext.Disposable`.",
    explanation: "This is Python's `with` and C#'s `using` arriving in JavaScript. It's the right answer to 'how do you guarantee a database connection or file handle is released?' — and knowing it exists marks you as current on TS 5.x.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-025",
    type: "flashcard",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["overloads", "conditional-types", "generics"],
    front: "How do you type a function that returns different types based on its input?",
    back: "**Preferred — generics with a conditional return:**\n```ts\nfunction parse<T extends 'json' | 'text'>(\n  res: Response, as: T\n): Promise<T extends 'json' ? unknown : string> {\n  return (as === 'json' ? res.json() : res.text()) as any;\n}\n```\n\n**Alternative — overloads**, when the signatures are genuinely unrelated:\n```ts\nfunction el(tag: 'a'): HTMLAnchorElement;\nfunction el(tag: 'img'): HTMLImageElement;\nfunction el(tag: string): HTMLElement;\nfunction el(tag: string): HTMLElement { return document.createElement(tag); }\n```\n\nRules: the implementation signature is **not** callable from outside; order overloads most-specific first; and the implementation must be compatible with all of them.",
    explanation: "The honest caveat interviewers like: both approaches usually need a type assertion inside the implementation, because TypeScript can't verify that the body satisfies the conditional return type. That's a known limitation, not a mistake in your code.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-026",
    type: "flashcard",
    topic: "typescript",
    difficulty: "beginner",
    tags: ["compiler", "tsc", "gfg"],
    front: "How do you compile TypeScript, and what does the compiler actually emit?",
    back: "```bash\ntsc                      # compile per tsconfig.json\ntsc --noEmit             # type-check only (the CI mode)\ntsc --watch              # incremental rebuild\ntsc file.ts --outFile bundle.js   # only with module: amd|system\n```\n\nWhat `tsc` does: **type-check**, then **erase types** and downlevel syntax to the `target`.\n\nCrucial: **types have zero runtime effect.** They're deleted. That's why you cannot check a type at runtime and why validation needs a runtime library.\n\nIn modern toolchains `tsc` often does *only* type checking (`--noEmit`), while esbuild/swc/Vite handle the transpile because they're far faster — but they don't type-check, so you need both.",
    explanation: "That last division of labour is worth stating: fast transpilers strip types without understanding them, so a build can succeed with type errors. `tsc --noEmit` in CI is what actually enforces type safety in most modern setups.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-027",
    type: "flashcard",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["declaration-files", "publishing", "gfg"],
    front: "What is `.d.ts` and how do you ship types with a library?",
    back: "A **declaration file** contains types only, no implementation. `tsc --declaration` generates them from your source.\n\n```jsonc\n// package.json\n{\n  \"types\": \"./dist/index.d.ts\",\n  \"exports\": {\n    \".\": {\n      \"types\": \"./dist/index.d.ts\",\n      \"import\": \"./dist/index.mjs\",\n      \"require\": \"./dist/index.cjs\"\n    }\n  }\n}\n```\n\nThe `types` condition **must come first** inside each `exports` entry — resolution picks the first match.\n\nFor untyped third-party packages, install community types from `@types/*` (DefinitelyTyped), or write a minimal local `.d.ts` with `declare module 'pkg'`.",
    explanation: "The types-first ordering in `exports` is a real, easily-missed bug: put it after `import` and TypeScript silently fails to find your declarations, and consumers get `any` with no error to explain why.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-028",
    type: "flashcard",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["mixins", "classes", "gfg"],
    front: "What are mixins in TypeScript?",
    back: "A pattern for composing behaviour without single-inheritance limits, built on class expressions:\n\n```ts\ntype Ctor<T = {}> = new (...args: any[]) => T;\n\nfunction Timestamped<TBase extends Ctor>(Base: TBase) {\n  return class extends Base {\n    createdAt = new Date();\n  };\n}\n\nfunction Serializable<TBase extends Ctor>(Base: TBase) {\n  return class extends Base {\n    serialize() { return JSON.stringify(this); }\n  };\n}\n\nclass User { constructor(public name: string) {} }\nconst Enhanced = Serializable(Timestamped(User));\nnew Enhanced('Ada').serialize();\n```\n\nEach mixin is a function taking a base class and returning an extended one. TypeScript infers the combined type through the intersection of the return types.",
    explanation: "Mixins get asked about, but be ready with the caveat: they make types harder to read, break `instanceof` intuition, and composition via plain interfaces plus delegation is usually clearer. Know them, prefer composition.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-029",
    type: "flashcard",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["readonly", "immutability", "gfg"],
    front: "How do you create immutable object properties in TypeScript?",
    back: "Layered, weakest to strongest:\n\n```ts\n// 1. readonly — compile-time only\ninterface Point { readonly x: number }\n\n// 2. Readonly<T> — shallow, compile-time only\ntype P = Readonly<{ x: number; nested: { y: number } }>;\n// p.nested.y = 1  ← still allowed! Readonly is shallow.\n\n// 3. as const — deeply readonly, but only for literals\nconst cfg = { a: { b: 1 } } as const;\n\n// 4. Object.freeze — actual RUNTIME immutability, but shallow\nconst frozen = Object.freeze({ x: 1 });\n\n// 5. ReadonlyArray<T> / readonly T[] — no push, pop, splice\n```\n\nThe key distinction: `readonly` is erased at compile time and prevents *your code* from assigning; `Object.freeze` actually stops assignment at runtime.",
    explanation: "The shallowness of both `Readonly<T>` and `Object.freeze` is the trap. Deep immutability needs a recursive mapped type for the compile-time half and a recursive freeze (or a library like Immer) for the runtime half.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-fc-030",
    type: "flashcard",
    topic: "typescript",
    difficulty: "beginner",
    tags: ["fundamentals", "gfg"],
    front: "What are the advantages and disadvantages of TypeScript?",
    back: "**Advantages**\n- Catches type errors at compile time rather than in production.\n- Editor tooling: autocomplete, safe rename, find-references, inline docs.\n- Types as documentation that cannot go stale.\n- Refactoring large codebases becomes tractable.\n- Modern JS features with downlevel compilation.\n\n**Disadvantages**\n- Learning curve, especially advanced generics and conditional types.\n- A build step, and slower feedback on very large codebases.\n- Type definitions for third-party libraries can be missing, wrong, or outdated.\n- **It is not sound** — `any`, type assertions, and unchecked predicates all create holes.\n- Verbosity when types are over-engineered.\n\nNet: the cost is roughly proportional to project size; the benefit is superlinear.",
    explanation: "The unsoundness point is worth raising unprompted — TypeScript deliberately trades soundness for pragmatism (array covariance, `as`, and `any` are all deliberate holes). Understanding that it's a *productivity* tool rather than a proof system is a mature framing.",
    createdAt: "2026-08-26",
  },
  // ── Multiple choice ───────────────────────────────────────────
  {
    id: "ts-mc-001",
    type: "multiple-choice",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["satisfies", "modern-ts"],
    prompt: "What does `satisfies` do that a type annotation does not?",
    options: [
      { id: "a", text: "It allows extra properties that the type does not declare" },
      { id: "b", text: "It validates against the constraint while preserving the narrow inferred literal types" },
      { id: "c", text: "It performs a runtime check that the value matches the type" },
      { id: "d", text: "It makes the object deeply readonly" },
    ],
    correctOptionId: "b",
    timeLimitSeconds: 45,
    explanation: "A type annotation widens the value to the annotated type, so `config.port` becomes `string | number` and typos in keys pass. `satisfies` checks conformance without widening, so you keep both validation and precise inference. It has no runtime effect at all.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-mc-002",
    type: "multiple-choice",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["arrays", "strict", "unsoundness"],
    prompt: "With `strict: true` but `noUncheckedIndexedAccess: false`, what is the type of `arr[999]` for `const arr: string[]`?",
    options: [
      { id: "a", text: "string | undefined" },
      { id: "b", text: "undefined" },
      { id: "c", text: "never" },
      { id: "d", text: "string — TypeScript assumes the index is in bounds, which is a lie" },
    ],
    correctOptionId: "d",
    timeLimitSeconds: 50,
    explanation: "This is one of TypeScript's deliberate unsoundness holes: array index access is typed as the element type regardless of bounds. `noUncheckedIndexedAccess` fixes it by adding `| undefined`, which is noisy to adopt but catches genuine runtime crashes.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-mc-003",
    type: "multiple-choice",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["utility-types", "omit"],
    prompt: "Why does `Omit<User, 'emial'>` compile without error?",
    options: [
      { id: "a", text: "Omit only checks keys at runtime" },
      { id: "b", text: "It does error — Omit is key-checked" },
      { id: "c", text: "Omit's key parameter is not constrained to keyof T, so a typo silently removes nothing" },
      { id: "d", text: "TypeScript automatically corrects the typo" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 50,
    explanation: "`Omit<T, K extends keyof any>` deliberately accepts arbitrary keys so it can compose with unions and index signatures. The cost is that typos pass silently. Use `Omit<T, Extract<keyof T, K>>` or a key-checked helper when you want the safety.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-mc-004",
    type: "multiple-choice",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["classes", "privacy"],
    prompt: "Which construct gives you runtime-enforced privacy in a TypeScript class?",
    options: [
      { id: "a", text: "readonly field" },
      { id: "b", text: "protected field" },
      { id: "c", text: "#field — an ECMAScript private field" },
      { id: "d", text: "private field — TypeScript's access modifier" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 45,
    explanation: "`private`, `protected`, and `readonly` are erased at compile time — `(obj as any).secret` reads them fine, and they appear in `Object.keys` and `JSON.stringify`. `#field` is a JavaScript language feature enforced by the runtime and genuinely inaccessible from outside the class.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-mc-005",
    type: "multiple-choice",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["exhaustiveness", "never"],
    prompt: "You add a third variant to a discriminated union. What makes every unhandled switch fail to compile?",
    options: [
      { id: "a", text: "Using an enum instead of string literals" },
      { id: "b", text: "Assigning the narrowed value to a `never`-typed variable in the default branch" },
      { id: "c", text: "Marking the function return type as never" },
      { id: "d", text: "Enabling noFallthroughCasesInSwitch" },
    ],
    correctOptionId: "b",
    timeLimitSeconds: 50,
    explanation: "When all cases are handled the value narrows to `never` in `default`, so the assignment succeeds. Add a variant and the value is no longer `never`, so the assignment errors — turning a manual search into a compiler-generated task list. `noFallthroughCasesInSwitch` catches missing `break`, a different problem.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-mc-006",
    type: "multiple-choice",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["import-type", "isolatedmodules"],
    prompt: "Why is `import type` required when using esbuild or swc with `isolatedModules`?",
    options: [
      { id: "a", text: "A single-file transpiler cannot tell whether an imported name is a type or a value, so it must be told explicitly" },
      { id: "b", text: "It makes compilation faster by skipping the import" },
      { id: "c", text: "Types cannot be imported across module boundaries otherwise" },
      { id: "d", text: "It prevents circular dependencies at the type level" },
    ],
    correctOptionId: "a",
    timeLimitSeconds: 55,
    explanation: "These transpilers process one file at a time with no cross-file type information, so they can't know whether to erase an import or keep it. The `type` modifier removes the ambiguity. Avoiding runtime imports of type-only modules is a genuine secondary benefit, but the isolation requirement is the reason it's mandatory.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-mc-007",
    type: "multiple-choice",
    topic: "typescript",
    difficulty: "intermediate",
    tags: ["as-const", "literal-types"],
    prompt: "What is the type of `typeof ROLES[number]` where `const ROLES = ['a','b'] as const`?",
    options: [
      { id: "a", text: "readonly ['a','b']" },
      { id: "b", text: "string[]" },
      { id: "c", text: "'a' | 'b'" },
      { id: "d", text: "string" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 45,
    explanation: "`as const` narrows the array to `readonly ['a','b']`, and indexing a tuple type with `number` yields the union of its element types. This is the standard idiom for deriving a union from a runtime array so the two cannot drift apart.",
    createdAt: "2026-08-26",
  },
  {
    id: "ts-mc-008",
    type: "multiple-choice",
    topic: "typescript",
    difficulty: "advanced",
    tags: ["conditional-types", "distributive"],
    prompt: "`type ToArray<T> = T extends any ? T[] : never`. What is `ToArray<string | number>`?",
    options: [
      { id: "a", text: "string[] | number[] — the conditional distributes over the union" },
      { id: "b", text: "(string | number)[]" },
      { id: "c", text: "never" },
      { id: "d", text: "string[] & number[]" },
    ],
    correctOptionId: "a",
    timeLimitSeconds: 55,
    explanation: "When the checked type is a naked type parameter, conditional types distribute over unions — each member is evaluated separately and the results are unioned. Wrapping in a tuple (`[T] extends [any]`) disables distribution and gives `(string | number)[]` instead.",
    createdAt: "2026-08-26",
  },
];
