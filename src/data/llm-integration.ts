import type { Question } from '@/types/content';

/**
 * LLM APIs, chain frameworks, and cloud GenAI services.
 *
 * Matrix rows: "LLM APIs" (Novice + Advanced), "Chain Framework: LangChain OR LlamaIndex OR
 * SemanticKernel", "LangChain - Chains / Memory Management / Parallel Chain Execution /
 * Branching Chain Execution / Integrating External APIs / Error Handling and Debugging /
 * Custom Components", and the AWS / Azure / GCP GenAI Services blocks.
 */

export const llmIntegrationQuestions: Question[] = [
  // ── Flashcards ────────────────────────────────────────────────
  {
    id: "llm-fc-001",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "beginner",
    tags: ["api-basics", "chat-completions"],
    front: "What does a chat completions request actually contain?",
    back: "```python\nclient.chat.completions.create(\n    model=\"...\",                  # PIN the exact version, never an alias\n    messages=[\n        {\"role\": \"system\",    \"content\": \"...\"},\n        {\"role\": \"user\",      \"content\": \"...\"},\n        {\"role\": \"assistant\", \"content\": \"...\"},   # prior turns\n    ],\n    tools=[...],                  # function schemas\n    temperature=0,\n    max_tokens=1024,              # cost cap AND runaway guard\n    stop=[\"</answer>\"],\n    stream=True,\n    seed=42,\n)\n```\n\nThe API is **stateless** — every turn you resend the whole conversation. That is why cost grows quadratically with conversation length unless you compact or cache.",
    explanation: "The statelessness point catches people out. A 30-turn chat resends 29 turns on turn 30, so a naive chatbot's cost curve is quadratic. Naming that, plus prompt caching as the mitigation, is a strong Novice→Intermediate signal.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-002",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "beginner",
    tags: ["rate-limits", "retries", "backoff"],
    front: "How do you handle rate limits properly?",
    back: "Providers limit on **two axes**: requests per minute (RPM) and **tokens per minute (TPM)**. TPM is usually what you hit first, and it counts input + output.\n\n```python\nfor attempt in range(6):\n    try:\n        return call()\n    except RateLimitError as e:\n        wait = float(e.retry_after or (2 ** attempt))\n        sleep(wait + random.uniform(0, 0.5))   # jitter is essential\nraise\n```\n\nBeyond retries:\n- **Respect `Retry-After`** rather than guessing.\n- **Jitter** — without it, all your workers retry in lockstep and re-trigger the limit.\n- **Client-side token-bucket** to stay under the limit rather than discovering it.\n- **Queue + concurrency cap** — bound in-flight requests instead of retrying a stampede.\n- **Batch API** for non-urgent work — separate, much higher quota and roughly half the price.",
    explanation: "'Understanding and managing API rate limits and costs' is a Novice matrix row, and the TPM-not-RPM distinction is the detail that shows real usage. Also worth saying: retries alone are not a rate-limit strategy — they're what you do after your concurrency control failed.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-003",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "intermediate",
    tags: ["error-handling", "retries"],
    front: "Which errors do you retry, and which do you not?",
    back: "| Status | Meaning | Retry? |\n|---|---|---|\n| 400 | Bad request / schema error | **No** — fix the call |\n| 401 / 403 | Auth | **No** |\n| 404 | Unknown model | **No** |\n| 408 | Timeout | Yes |\n| 422 | Content-length / validation | No |\n| 429 | Rate limited | Yes, with `Retry-After` |\n| 500 / 502 / 503 / 529 | Provider-side | Yes, exponential backoff |\n\nAlso non-obvious: `finish_reason == \"length\"` is **not an error** — it means truncation. Detect it and either raise `max_tokens`, continue the generation, or fail loudly. Silently returning truncated JSON is a nasty class of bug.",
    explanation: "The `finish_reason` check is the one candidates forget, and it produces the worst failures — half-written JSON that parses as valid until the missing field matters. Always branch on finish reason, not just on HTTP status.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-004",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "intermediate",
    tags: ["streaming", "sse", "latency"],
    front: "Why stream responses, and what changes in your architecture?",
    back: "**Why:** time-to-first-token is a fraction of time-to-last-token, so perceived latency drops dramatically. It also lets users cancel early, which saves real money.\n\n**What changes:**\n- Transport must support it: **SSE** (simple, one-way, auto-reconnect) or WebSocket (bidirectional).\n- You can't validate the whole response before sending it — so guardrails must run incrementally, or you buffer and lose the benefit.\n- Errors can occur **mid-stream**, after you've already sent a 200 and half an answer. Your protocol needs an in-band error event.\n- Token accounting arrives at the end, in a final usage chunk.\n- Client cancellation must actually abort the upstream request (`AbortSignal`), or you keep paying for tokens nobody reads.",
    explanation: "That last point is a real and common cost leak: users navigate away, the browser connection drops, and the server keeps streaming from the provider because nothing propagated the cancellation. Wiring abort end-to-end is worth mentioning explicitly.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-005",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "intermediate",
    tags: ["cost", "optimization", "caching"],
    front: "How do you estimate and control LLM cost?",
    back: "**Estimate**\n```\ncost = (input_tokens/1M × input_price) + (output_tokens/1M × output_price)\n```\nOutput is typically 3–5× the input price. Count tokens with the provider's tokenizer, not `len(text)/4`, when the number matters.\n\n**Control**\n- **Cache** the stable prompt prefix (~90% off the cached portion).\n- **Route** — cheap model by default, expensive on escalation.\n- **Cap `max_tokens`** on every call.\n- **Compact** conversation history rather than resending it whole.\n- **Batch API** for offline work (~50% cheaper).\n- **Semantic cache** for repeated questions, scoped by tenant.\n- **Budget guards** per user, per tenant, per run — enforced in code.\n\n**Attribute**: log tokens and cost per request with user/feature tags, or you can't find the expensive path.",
    explanation: "Interviewers want the attribution point. 'Our LLM bill is $40k/month' is useless without per-feature breakdown; teams that tag every call can usually find one badly-designed prompt responsible for most of it.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-006",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "beginner",
    tags: ["langchain", "lcel", "chains"],
    front: "What is a chain, in LangChain terms?",
    back: "A composable pipeline of steps — prompt → model → parser — wired with the **Runnable** interface and LCEL:\n\n```python\nchain = prompt | model | StrOutputParser()\nchain.invoke({\"question\": \"...\"})\n```\n\nEvery Runnable supports the same methods: `invoke`, `batch`, `stream`, and async variants — so composition preserves streaming and batching automatically.\n\nThe Novice matrix row asks for: what a chain is, memory and context, install/setup, the docs, the role of the LLM, a single-step chain, and basic prompt templates.",
    explanation: "Know LCEL's pipe syntax and that `|` is just `Runnable.__or__`. The reason it matters practically: because everything is a Runnable, you get streaming and batching through a five-stage chain without writing any plumbing.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-007",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "intermediate",
    tags: ["langchain", "parallel", "branching"],
    front: "How do you run chain steps in parallel and branch conditionally?",
    back: "**Parallel** — independent steps run concurrently:\n```python\nchain = RunnableParallel(\n    summary=summary_chain,\n    sentiment=sentiment_chain,\n    entities=entity_chain,\n) | combine_prompt | model\n```\nWall-clock is the slowest branch, not the sum. A dict literal inside a chain is coerced to `RunnableParallel` automatically.\n\n**Branching** — pick a path at runtime:\n```python\nchain = RunnableBranch(\n    (lambda x: x[\"type\"] == \"code\",  code_chain),\n    (lambda x: x[\"type\"] == \"sql\",   sql_chain),\n    default_chain,\n)\n```\nPrefer a plain `RunnableLambda` returning a chain when the routing logic is non-trivial — it's easier to read and test.",
    explanation: "'Parallel Chain Execution' and 'Branching Chain Execution' are two separate Intermediate matrix rows. The point worth adding: parallelism only helps if the branches are genuinely independent, and you still share one rate limit across all of them.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-008",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "intermediate",
    tags: ["langchain", "memory", "context"],
    front: "How does memory work in a chain framework?",
    back: "The API is stateless, so 'memory' means *what you choose to resend*:\n\n- **Buffer** — full history. Accurate, unbounded cost.\n- **Window (last k turns)** — bounded, but loses earlier facts.\n- **Summary** — an LLM condenses old turns; cheap, lossy, and costs a call.\n- **Summary + buffer** — summary of old turns plus the last k verbatim. The usual production choice.\n- **Vector-backed** — store turns, retrieve only the relevant ones. Good for long-running relationships.\n- **Entity memory** — structured facts per entity, updated over time.\n\nModern LangChain routes this through `RunnableWithMessageHistory` / LangGraph checkpointers, with a session key so histories are per-conversation and persisted.",
    explanation: "'LangChain - Memory Management ... e.g., maintaining the context of a conversation in a chatbot application' is a named matrix row. The framing that lands: memory is a *retention policy*, and the interesting decision is what to forget, not what to keep.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-009",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "advanced",
    tags: ["langchain", "custom-components"],
    front: "How do you write a custom LangChain component?",
    back: "Anything callable can join a chain:\n\n```python\ndef redact(x: dict) -> dict:\n    return {**x, \"text\": PII.sub(\"[REDACTED]\", x[\"text\"])}\n\nchain = RunnableLambda(redact) | prompt | model\n```\n\nFor richer components, subclass the relevant base class — `BaseRetriever` (implement `_get_relevant_documents`), `BaseOutputParser` (`parse`), `BaseCallbackHandler` for logging/metrics, or a `BaseTool` for agents.\n\nCustom components are the right move when you need **domain retrieval logic**, **preprocessing**, **validation**, or **instrumentation** that the built-ins don't cover.",
    explanation: "'LangChain - Custom Components (e.g., creating components for preprocessing input data)' is a named Advanced row. The practical advice worth adding: prefer a plain function in a `RunnableLambda` over a class hierarchy unless you need the framework's lifecycle hooks — it's far easier to unit test.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-010",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "advanced",
    tags: ["langchain", "tools", "external-apis"],
    front: "How do you integrate an external API into a chain?",
    back: "Wrap it as a tool or a Runnable, and put **your** reliability engineering around it:\n\n```python\n@tool\ndef get_inventory(sku: str) -> dict:\n    '''Return current stock for a SKU. Use for availability questions.'''\n    r = session.get(f\"{BASE}/inventory/{sku}\", timeout=5)\n    r.raise_for_status()\n    return {\"sku\": sku, \"qty\": r.json()[\"quantity\"]}\n```\n\nNon-negotiables:\n- **Timeouts** on every call — a hanging API hangs the chain.\n- **Retries with backoff** for transient failures, inside the tool.\n- **Circuit breaker** so a dead dependency fails fast instead of burning the step budget.\n- **Trim the response** before it enters context — return the three fields you need, not 40 KB of JSON.\n- **Secrets from the environment**, never from model-supplied arguments.",
    explanation: "'LangChain - Integrating External APIs' is a named row. The response-trimming point is the one with the biggest practical impact — unfiltered API responses are the number-one cause of context bloat in tool-using systems.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-011",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "advanced",
    tags: ["langchain", "debugging", "tracing"],
    front: "How do you debug a chain that produces the wrong output?",
    back: "Chains are opaque by default — the fix is tracing.\n\n1. **Enable tracing** (LangSmith / Langfuse / OpenTelemetry). You need the *rendered* prompt at every step, not the template.\n2. **Inspect intermediate outputs** — `chain.invoke` with callbacks, or decompose and run steps individually.\n3. **Check the rendered prompt** — the majority of 'model' bugs are template bugs: an unfilled `{variable}`, a document that didn't get injected, an escaped brace.\n4. **Verify parsers** — an output parser silently swallowing a mismatch is common.\n5. **Pin and log versions** — model, prompt, and chain code, so you can attribute a regression.\n6. **Reproduce with a fixture**, then add it to the eval set.",
    explanation: "'LangChain - Error Handling and Debugging' is a named Advanced row. The single highest-value habit is logging the fully rendered prompt: it turns 'the LLM is being weird' into 'we shipped a template with an unfilled placeholder', which is a five-minute fix.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-012",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "intermediate",
    tags: ["frameworks", "langchain", "llamaindex"],
    front: "LangChain vs LlamaIndex vs Semantic Kernel vs no framework?",
    back: "| | Sweet spot |\n|---|---|\n| **LangChain / LangGraph** | Broad orchestration, agents, huge integration catalogue; LangGraph for stateful/durable flows |\n| **LlamaIndex** | Data-centric RAG — ingestion, indexing, and advanced retrieval strategies are its core |\n| **Semantic Kernel** | .NET/enterprise-first, plugin/planner model, strong Azure integration |\n| **No framework** | Production systems with a narrow, well-understood flow |\n\nThe matrix says *one of* — depth in one beats a shallow tour of all three.\n\nHonest trade-off: frameworks accelerate the first 80% and can obstruct the last 20%, where you need exact control over prompts, retries, and context. Keep provider calls behind your own interface so the choice stays reversible.",
    explanation: "The 'first 80% / last 20%' framing is the mature answer. Many production teams prototype in a framework and progressively replace its core loop while keeping the integrations — saying that shows you've shipped rather than just followed tutorials.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-013",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "intermediate",
    tags: ["aws", "bedrock", "cloud"],
    front: "What does AWS Bedrock provide, and what are its key features?",
    back: "A managed, multi-vendor model API on AWS — Anthropic, Meta, Mistral, Amazon Nova, Cohere and others behind one interface, with data staying in your AWS account and no separate vendor contract.\n\nKey features to name:\n- **Knowledge Bases** — managed RAG (ingestion, chunking, vector store via OpenSearch/Aurora).\n- **Agents** — managed tool-using agents with action groups.\n- **Guardrails** — content filters, denied topics, PII redaction, and contextual grounding checks, applied independently of the model.\n- **Provisioned Throughput** — reserved capacity for predictable high volume, vs on-demand.\n- **Model evaluation** and **Model Distillation**.\n- IAM for authorization, CloudWatch/CloudTrail for logs, VPC endpoints for private networking.",
    explanation: "'AWS GenAI Services: Service Awareness (Bedrock, SageMaker)' and 'Bedrock API Usage' are matrix rows. The differentiators worth calling out are Guardrails as a *separate* enforcement layer and the on-demand vs provisioned-throughput cost decision.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-014",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "intermediate",
    tags: ["aws", "bedrock", "sagemaker"],
    front: "Bedrock vs SageMaker — when do you use which?",
    back: "- **Bedrock** — serverless API to *someone else's* models. No infrastructure, per-token pricing, fastest path to production. Use for the overwhelming majority of GenAI applications.\n- **SageMaker** — the full ML platform: training, fine-tuning, and hosting **your own** models on instances you pay for by the hour. **JumpStart** provides pre-built open-weight models you can deploy or fine-tune.\n\nChoose SageMaker when you need a model Bedrock doesn't offer, custom fine-tuning beyond Bedrock's support, classical ML alongside GenAI, or full control of the serving stack.\n\nCost shape differs fundamentally: Bedrock is per-token (scales to zero); SageMaker endpoints bill per hour whether or not you send traffic.",
    explanation: "That last line is the practical decision driver and a classic interview trap. A SageMaker endpoint idling overnight still bills; a Bedrock integration with no traffic costs nothing. For spiky workloads that difference dominates.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-015",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "intermediate",
    tags: ["azure", "gcp", "vertex-ai", "cloud"],
    front: "What do Azure OpenAI and Google Vertex AI offer for GenAI apps?",
    back: "**Azure OpenAI Service** — OpenAI models inside Azure with enterprise controls: private networking, Entra ID auth, regional data residency, content filters, and **fine-tuning**. Deployments are named resources with their own quota (TPM), so capacity planning is explicit. **Azure AI Studio/Foundry** adds a broader model catalogue, prompt flow, and evaluation.\n\n**Google Vertex AI** — Gemini plus **Model Garden** (open and third-party models), Generative AI Studio for prototyping, grounding with Google Search or your own data, model tuning, and **Agent Engine** for deploying agents. Integrates with BigQuery and Vertex Vector Search.\n\nAll three clouds converge on the same shape: model catalogue + managed RAG + agents + guardrails + evaluation.",
    explanation: "The matrix has parallel Advanced rows for AWS, Azure, and GCP, so recognising the common shape lets you answer for whichever one comes up. The Azure-specific detail worth knowing is that quota is per *deployment*, which is a frequent source of surprise throttling.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-016",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "advanced",
    tags: ["architecture", "abstraction", "portability"],
    front: "How do you design a provider-agnostic LLM layer?",
    back: "Put an interface between your application and any SDK:\n\n```typescript\ninterface LLMProvider {\n  complete(req: CompletionRequest): Promise<CompletionResponse>;\n  stream(req: CompletionRequest): AsyncIterable<Chunk>;\n}\n```\n\nNormalise across providers: message roles, tool-call format, token usage fields, finish reasons, and error taxonomy.\n\nThen you can:\n- **Fail over** when a provider degrades.\n- **A/B test** models on live traffic.\n- **Route** by cost or capability.\n- **Negotiate** — being able to switch is leverage.\n\nCost: you lose provider-specific features unless you add escape hatches. Keep a typed `providerOptions` passthrough.",
    explanation: "This mirrors the `StorageAdapter` seam this very app uses. The honest caveat matters: full abstraction is impossible because prompts are model-coupled, so pair the interface with per-model prompt variants selected by config.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-017",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "advanced",
    tags: ["observability", "monitoring", "opentelemetry"],
    front: "What does production observability for an LLM feature require?",
    back: "Per request, logged and queryable:\n- Prompt version, model version, parameters.\n- Input/output token counts and computed cost.\n- Latency: **time to first token** and total.\n- Finish reason, error class, retry count.\n- Tool calls made, and retrieved chunk IDs for RAG.\n- User/tenant/feature tags for attribution.\n- A feedback signal — thumbs, acceptance, correction, or task success.\n\nAggregated: cost per feature, p95 TTFT, error rate by class, and a quality score from a scheduled eval run.\n\nStandardise on **OpenTelemetry GenAI semantic conventions** so you're not locked into one vendor's SDK.",
    explanation: "Time-to-first-token as a distinct metric from total latency is the LLM-specific detail. For a streaming UI, TTFT is what users actually experience, and a change that improves total latency while worsening TTFT feels slower to everyone.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-018",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "advanced",
    tags: ["reliability", "circuit-breaker", "fallback"],
    front: "How do you handle provider outages and degradation?",
    back: "- **Timeouts** on every call, tuned per model (a reasoning model legitimately takes 60s+).\n- **Circuit breaker** — trip after N consecutive failures, fail fast, probe periodically. Prevents queue collapse.\n- **Fallback chain** — same model in another region → a different provider → a cached/degraded response. Test the fallback path regularly or it will be broken when you need it.\n- **Graceful degradation** — disable the AI feature and show the deterministic path rather than erroring the whole page.\n- **Idempotency** on anything that writes, since retries duplicate.\n- **Health checks and alerting** on error rate and latency, not just availability.\n\nDesign assumption: **the provider will have an outage**, and the question is only what your users see.",
    explanation: "The untested-fallback warning is worth stating. Fallback paths that have never run in production are, empirically, broken — a stale API key, a missing model deployment, a prompt that was never adapted for the backup model.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-019",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "intermediate",
    tags: ["batch-api", "cost", "rate-limits"],
    front: "What is the Batch API and when should you use it?",
    back: "Submit a file of requests, get results back within a window (typically up to 24h), at roughly **half price** and against a **separate, much larger quota**.\n\nIdeal for: bulk classification, embedding a corpus, synthetic data generation, offline evaluation runs, backfills, and periodic report generation.\n\nNot for: anything a user is waiting on.\n\nDesign notes: each line carries a `custom_id` you must use to rejoin results to inputs; partial failures come back per line, so handle them individually; and because it uses separate quota, batch work will not throttle your interactive traffic — which is often the bigger win.",
    explanation: "That quota-isolation point is underappreciated. Running a large backfill through the synchronous API can rate-limit your production chat feature; moving it to batch removes the contention entirely, independent of the price saving.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-fc-020",
    type: "flashcard",
    topic: "llm-integration",
    difficulty: "advanced",
    tags: ["deployment", "canary", "versioning"],
    front: "How do you version and roll out a prompt or model change safely?",
    back: "Treat it like a code deploy, because it changes behaviour:\n\n1. **Offline eval** — run the candidate against the full eval set; compare quality, cost, and p95 latency.\n2. **Shadow** — run the candidate alongside production on real traffic, log both, compare, serve the incumbent.\n3. **Canary** — 1% → 5% → 25% → 100%, watching quality signals and error rates.\n4. **Kill switch** — prompt and model version are config, so rollback is instant and doesn't require a deploy.\n5. **Log the versions** with every request so you can attribute changes after the fact.\n\nNever change the prompt and the model in the same rollout — you won't know which caused the delta.",
    explanation: "That last rule is the one people break constantly. Changing two variables at once makes the result uninterpretable, and with non-deterministic systems you can't just reason it out afterwards.",
    createdAt: "2026-08-26",
  },
  // ── Multiple choice ───────────────────────────────────────────
  {
    id: "llm-mc-001",
    type: "multiple-choice",
    topic: "llm-integration",
    difficulty: "intermediate",
    tags: ["cost", "context", "chat"],
    prompt: "Your chat feature's cost grows much faster than user count. Most likely cause?",
    options: [
      { id: "a", text: "Streaming responses are billed at a premium" },
      { id: "b", text: "The API is stateless, so full conversation history is resent every turn — cost grows quadratically with turns" },
      { id: "c", text: "The provider raised prices" },
      { id: "d", text: "Output tokens cost more than input tokens" },
    ],
    correctOptionId: "b",
    timeLimitSeconds: 50,
    explanation: "A 30-turn conversation resends 29 turns on the last call. Total cost across the session is quadratic in turn count. The fixes are prompt caching on the stable prefix and history compaction — a summary of old turns plus the last k verbatim.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-mc-002",
    type: "multiple-choice",
    topic: "llm-integration",
    difficulty: "intermediate",
    tags: ["error-handling", "finish-reason"],
    prompt: "A response comes back with `finish_reason: \"length\"` and truncated JSON. What is the correct handling?",
    options: [
      { id: "a", text: "Treat it as a failure: raise max_tokens, continue the generation, or surface an error — never parse it" },
      { id: "b", text: "Parse what arrived and fill missing fields with defaults" },
      { id: "c", text: "Retry the identical request, since it is a transient error" },
      { id: "d", text: "Lower the temperature and retry" },
    ],
    correctOptionId: "a",
    timeLimitSeconds: 50,
    explanation: "Truncation is deterministic, so an identical retry truncates identically. Filling in defaults is worse — you silently fabricate data. The output was cut mid-structure, so the only safe options are giving it more room, continuing the generation, or failing loudly.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-mc-003",
    type: "multiple-choice",
    topic: "llm-integration",
    difficulty: "intermediate",
    tags: ["batch-api", "rate-limits", "cost"],
    prompt: "You must embed 5 million documents by tomorrow morning, and a live chat feature shares the same account. Best approach?",
    options: [
      { id: "a", text: "Use the Batch API — roughly half price and, critically, a separate quota that won't throttle live traffic" },
      { id: "b", text: "Run it synchronously with high concurrency and retry on 429s" },
      { id: "c", text: "Spread it over a week using the synchronous API at low concurrency" },
      { id: "d", text: "Request a temporary rate-limit increase and run it synchronously" },
    ],
    correctOptionId: "a",
    timeLimitSeconds: 55,
    explanation: "Batch is designed for exactly this: cheaper, higher throughput, and quota-isolated from interactive traffic. Hammering the synchronous API rate-limits your paying users, and stretching it over a week misses the deadline. A quota increase helps throughput but still shares the pool with production.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-mc-004",
    type: "multiple-choice",
    topic: "llm-integration",
    difficulty: "advanced",
    tags: ["architecture", "abstraction"],
    prompt: "Which is the strongest argument for putting an interface between your app and the provider SDK?",
    options: [
      { id: "a", text: "Failover, A/B testing, and cost-based routing become possible without touching application code" },
      { id: "b", text: "It makes the code shorter" },
      { id: "c", text: "Provider SDKs are unstable and change frequently" },
      { id: "d", text: "It removes the need for prompt engineering" },
    ],
    correctOptionId: "a",
    timeLimitSeconds: 50,
    explanation: "The seam buys operational capability, not tidiness: you can fail over during an outage, run two models on live traffic, and route by cost. The caveat to state alongside it is that prompts remain model-coupled, so the abstraction covers transport and not behaviour.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-mc-005",
    type: "multiple-choice",
    topic: "llm-integration",
    difficulty: "advanced",
    tags: ["reliability", "circuit-breaker"],
    prompt: "Under high load your service hangs and the queue backs up because the provider is degraded. What prevents this?",
    options: [
      { id: "a", text: "Increasing the worker pool size" },
      { id: "b", text: "Switching to streaming responses" },
      { id: "c", text: "A circuit breaker that trips after repeated failures and fails fast, plus per-call timeouts" },
      { id: "d", text: "More aggressive retries with longer backoff" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 55,
    explanation: "Retries against a degraded provider add load and hold workers longer, deepening the collapse; more workers just enlarges the pile-up. A circuit breaker converts slow failures into fast ones so capacity is freed and a fallback path can run.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-mc-006",
    type: "multiple-choice",
    topic: "llm-integration",
    difficulty: "intermediate",
    tags: ["aws", "sagemaker", "bedrock"],
    prompt: "You need a fine-tuned open-weights model served with strict VPC isolation on AWS. Which service?",
    options: [
      { id: "a", text: "Lambda with the model bundled in the deployment package" },
      { id: "b", text: "Bedrock Knowledge Bases" },
      { id: "c", text: "SageMaker — deploy and fine-tune your own model on managed endpoints" },
      { id: "d", text: "Bedrock on-demand — it serves any open-weights model you upload" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 55,
    explanation: "Bedrock serves a curated catalogue rather than arbitrary uploads (Custom Model Import is limited to specific architectures), and Knowledge Bases is managed RAG, not model hosting. SageMaker is the platform for training, fine-tuning, and hosting your own weights — with the trade-off that endpoints bill per hour, not per token.",
    createdAt: "2026-08-26",
  },
  {
    id: "llm-mc-007",
    type: "multiple-choice",
    topic: "llm-integration",
    difficulty: "advanced",
    tags: ["deployment", "experimentation"],
    prompt: "You want to change both the prompt and the model to improve quality. What is the correct rollout?",
    options: [
      { id: "a", text: "Change the model first in production, then tune the prompt against live traffic" },
      { id: "b", text: "Change one at a time, each with its own offline eval and canary, so the delta is attributable" },
      { id: "c", text: "Change both together to reach the improved state faster" },
      { id: "d", text: "Change both in a shadow deployment, then ship if the combined metrics improve" },
    ],
    correctOptionId: "b",
    timeLimitSeconds: 50,
    explanation: "With a non-deterministic system you cannot reason backwards about which variable caused a change — and a combined rollout can hide one regression behind one improvement. Sequential canaries take longer but leave you knowing what actually works, which matters when you need to roll back.",
    createdAt: "2026-08-26",
  },
];
