import type { Question } from '@/types/content';

/**
 * Reusable Stencil web components integrated into Angular-based applications (e.g. EAA).
 *
 * Covers the Web Components standards (custom elements, Shadow DOM, slots), Stencil 4
 * authoring and output targets, and the Angular integration surface: wrappers, forms,
 * change detection, styling, SSR, accessibility, and versioning.
 *
 * See .claude/knowledge/angular-stencil.md
 */

export const webComponentsQuestions: Question[] = [
  // ── Flashcards ────────────────────────────────────────────────
  {
    id: "wc-fc-001",
    type: "flashcard",
    topic: "web-components",
    difficulty: "beginner",
    tags: ["standards", "custom-elements", "shadow-dom"],
    front: "What are the four Web Components standards?",
    back: "1. **Custom Elements** — define a new HTML tag backed by a class:\n   `customElements.define('eaa-button', EaaButton)`. Tag names **must** contain a hyphen.\n2. **Shadow DOM** — an encapsulated DOM subtree with its own scoped styles, attached via\n   `this.attachShadow({ mode: 'open' })`.\n3. **HTML Templates** — `<template>` and `<slot>` for inert, reusable markup and content projection.\n4. **ES Modules** — the distribution mechanism.\n\nTogether these give you a component model the **browser** understands, so the same component works in Angular, React, Vue, or a plain HTML page. That framework-independence is the entire reason a shared design system is built this way.",
    explanation: "The hyphen requirement is a spec rule, not a convention — it guarantees no collision with future built-in elements. And 'the browser is the framework' is the one-line pitch for why an org with Angular, React, and legacy apps builds its design system as custom elements.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-002",
    type: "flashcard",
    topic: "web-components",
    difficulty: "beginner",
    tags: ["stencil", "compiler"],
    front: "What is Stencil, and how does it differ from a framework?",
    back: "Stencil is a **compiler**, not a runtime framework. You author in TSX with decorators; it emits standards-based Custom Elements plus a very small runtime.\n\n```tsx\n@Component({ tag: 'eaa-button', styleUrl: 'button.css', shadow: true })\nexport class EaaButton {\n  @Prop() variant: 'primary' | 'secondary' = 'primary';\n  @Prop() disabled = false;\n  @Event() eaaClick: EventEmitter<void>;\n\n  render() {\n    return (\n      <button class={this.variant} disabled={this.disabled}\n              onClick={() => this.eaaClick.emit()}>\n        <slot />\n      </button>\n    );\n  }\n}\n```\n\nWhat you ship is `<eaa-button>` — a real HTML element. Nothing in the consuming app needs to know Stencil exists.",
    explanation: "The compiler-not-framework distinction is the interview answer. Practical consequences: the output has no framework lock-in, the runtime is a few KB rather than a full framework, and components are lazy-loaded by default in the `dist` output.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-003",
    type: "flashcard",
    topic: "web-components",
    difficulty: "intermediate",
    tags: ["stencil", "decorators"],
    front: "What are Stencil's core decorators?",
    back: "| Decorator | Purpose |\n|---|---|\n| `@Component` | `tag`, `styleUrl`, `shadow`/`scoped`, `formAssociated` |\n| `@Prop()` | Public API input; reflected to an attribute with `{ reflect: true }` |\n| `@State()` | Internal reactive state; changing it re-renders |\n| `@Event()` | Declares a `CustomEvent` emitter (the component's public output) |\n| `@Listen()` | Subscribe to DOM events, including `window`/`document` |\n| `@Method()` | Public imperative API — **must be `async`** |\n| `@Element()` | Reference to the host element |\n| `@Watch('prop')` | React to a prop or state change |\n\n`@Method()` is async because the component may not be lazily loaded yet when you call it — the promise resolves once it is.",
    explanation: "That `@Method` async rule is a favourite question. It also shapes API design: prefer props and events (declarative, framework-friendly) over methods, because imperative calls across a component boundary are awkward for every consuming framework.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-004",
    type: "flashcard",
    topic: "web-components",
    difficulty: "intermediate",
    tags: ["stencil", "lifecycle"],
    front: "What is the Stencil component lifecycle?",
    back: "```\nconnectedCallback          — attached to the DOM (can fire multiple times if moved)\n  componentWillLoad        — once, before first render. Return a Promise to delay render.\n  componentWillRender\n    render()\n  componentDidRender\n  componentDidLoad         — once, after first render. DOM is available.\n[on update]\n  componentWillUpdate\n    render()\n  componentDidUpdate\ndisconnectedCallback       — removed from the DOM. Clean up here.\n```\n\nKey points:\n- **`componentWillLoad`** is where you fetch initial data — returning a promise defers the first render.\n- **`componentDidLoad`** is where you touch the real DOM or measure.\n- **`connectedCallback` can fire more than once** — moving a node in the DOM disconnects and reconnects it, so subscriptions set up there must be torn down in `disconnectedCallback` or you leak.",
    explanation: "The multiple-`connectedCallback` behaviour is the subtle one and a genuine source of leaks — it's the custom-element equivalent of React's remount, and people who assume it's a constructor get duplicate listeners.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-005",
    type: "flashcard",
    topic: "web-components",
    difficulty: "intermediate",
    tags: ["stencil", "output-targets", "bundling"],
    front: "What are Stencil's output targets, and which do you use for Angular?",
    back: "- **`dist`** — lazy-loaded bundle with a loader (`defineCustomElements()`). Components load on demand. Best when a page uses a small, unpredictable subset of a large library.\n- **`dist-custom-elements`** — plain, **tree-shakable** ES modules, one per component. Best when the consumer bundles them; unused components are dropped. **This is the recommended base for framework wrappers.**\n- **`www`** — a runnable dev/demo site.\n- **`docs-readme` / `docs-json`** — generated API docs from the decorators and JSDoc.\n- **`angular` (`@stencil/angular-output-target`)** — generates typed Angular wrapper components.\n\nFor an EAA-style library consumed by Angular apps: `dist-custom-elements` + the Angular output target, published as `@eaa/core` and `@eaa/angular`.",
    explanation: "The tree-shaking argument is the one to lead with: with `dist`'s lazy loader, an app using three components still ships the loader and the manifest for all 80. `dist-custom-elements` lets the app's bundler drop what it doesn't import.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-006",
    type: "flashcard",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["angular", "wrappers", "integration"],
    front: "Why do you need Angular wrapper components for Stencil elements?",
    back: "Raw custom elements work in Angular, but badly. The generated wrappers fix five concrete problems:\n\n| Problem | Without wrappers | With wrappers |\n|---|---|---|\n| Unknown tag | `NG_ERROR: 'eaa-button' is not a known element` unless you add `CUSTOM_ELEMENTS_SCHEMA` | Recognised component |\n| Complex props | Objects/arrays stringify through attributes | Set as DOM **properties** |\n| Type safety | None — `CUSTOM_ELEMENTS_SCHEMA` disables *all* template checking | Typed `@Input()`s |\n| Events | `(eaaClick)` works but payload is in `$event.detail`, untyped | Typed `@Output()` re-emitting `detail` |\n| Forms | `formControlName` silently does nothing | `ControlValueAccessor` provided |\n\nThe type-safety row is the most dangerous: `CUSTOM_ELEMENTS_SCHEMA` turns off unknown-property errors for **every** element in that component, so genuine typos stop being compile errors.",
    explanation: "That last point is the strongest argument and the one most teams discover too late. `CUSTOM_ELEMENTS_SCHEMA` is a blunt, component-wide opt-out of the template type checker, not a targeted allowance for one tag.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-007",
    type: "flashcard",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["stencil", "angular", "setup"],
    front: "How do you set up a Stencil library in an Angular app end to end?",
    back: "**Library side** (`stencil.config.ts`):\n```ts\noutputTargets: [\n  { type: 'dist-custom-elements', customElementsExportBehavior: 'single-export-module' },\n  angularOutputTarget({\n    componentCorePackage: '@eaa/core',\n    outputType: 'standalone',\n    directivesProxyFile: '../angular/src/lib/components.ts',\n    valueAccessorConfigs: [ /* map value/change for form controls */ ],\n  }),\n]\n```\n\n**App side:**\n```ts\n// main.ts — browser only\nimport { defineCustomElements } from '@eaa/core/loader';\nif (typeof window !== 'undefined') defineCustomElements();\n```\n```ts\n@Component({ imports: [EaaButton, EaaInput], template: `<eaa-button (eaaClick)=\"save()\">Save</eaa-button>` })\n```\n\nPublish two packages: `@eaa/core` (the elements) and `@eaa/angular` (the wrappers), versioned together.",
    explanation: "`outputType: 'standalone'` is the modern setting — it generates standalone directives rather than an NgModule, which matches Angular 17+. The browser-only guard on `defineCustomElements()` is what stops SSR from crashing.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-008",
    type: "flashcard",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["angular", "properties", "attributes"],
    front: "Why don't objects and arrays reach a custom element through an Angular binding?",
    back: "HTML **attributes are strings**. `[config]=\"myObject\"` on an element Angular doesn't recognise falls back to attribute binding, so the object becomes `\"[object Object]\"`.\n\nThree fixes:\n1. **Wrapper components** — they set the DOM **property**, which can hold any value. The right answer.\n2. **Explicit property binding** — `[prop.config]` isn't a thing; you need `@ViewChild` plus `el.nativeElement.config = obj`.\n3. **Serialise** — `[attr.config]=\"myObject | json\"` and parse inside the component. Works, but it's lossy, slow, and you lose functions entirely.\n\nPrimitives (string, number via a `@Prop() n: number`, boolean) do survive the attribute round-trip, which is why simple cases appear to work and complex ones mysteriously don't.",
    explanation: "That last sentence explains the confusing bug report: 'strings work, objects don't'. Understanding the attribute/property distinction is the core mental model for using custom elements in any framework.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-009",
    type: "flashcard",
    topic: "web-components",
    difficulty: "intermediate",
    tags: ["events", "shadow-dom", "angular"],
    front: "How do custom events from a Stencil component reach Angular?",
    back: "`@Event()` emits a `CustomEvent`, and its payload lives in **`event.detail`**:\n\n```tsx\n@Event() eaaChange: EventEmitter<{ value: string }>;\nthis.eaaChange.emit({ value: 'abc' });\n```\n\nRaw Angular binding — works, but untyped:\n```html\n<eaa-input (eaaChange)=\"onChange($event.detail)\"></eaa-input>\n```\n\nWith the generated wrapper — typed `@Output()` that already unwraps `detail`:\n```html\n<eaa-input (eaaChange)=\"onChange($event)\"></eaa-input>\n```\n\nEvent config matters: `bubbles` and **`composed: true`** are required for the event to escape the shadow root; `cancelable` if consumers may `preventDefault()`. Stencil defaults to all three true.",
    explanation: "`composed: true` is the shadow-DOM-specific gotcha. Without it the event stops at the shadow boundary and the Angular listener on the host never fires — a bug that looks like 'the event isn't emitted at all'.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-010",
    type: "flashcard",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["forms", "controlvalueaccessor", "angular"],
    front: "Why doesn't `formControlName` work on a custom element, and how do you fix it?",
    back: "Angular forms communicate with a control through the **`ControlValueAccessor`** interface. A custom element doesn't implement it, so `formControlName` binds to nothing — silently, with no error.\n\nTwo fixes:\n1. **The Angular output target's `valueAccessorConfigs`** — declare which event and property represent the value, and it generates a CVA directive:\n```ts\nvalueAccessorConfigs: [{\n  elementSelectors: ['eaa-input'],\n  event: 'eaaChange',\n  targetAttr: 'value',\n  type: 'text',\n}]\n```\n2. **Write a CVA directive yourself** with `NG_VALUE_ACCESSOR`, `multi: true`, implementing `writeValue`, `registerOnChange`, `registerOnTouched`, and `setDisabledState`.\n\nAlso consider **`formAssociated: true`** on the Stencil component, which makes it a form-associated custom element that participates in native form submission — useful for non-Angular consumers.",
    explanation: "The silence is what makes this painful: no error, the input just never updates the form model. `setDisabledState` is the piece people forget, which is why disabling a form group leaves the custom control interactive.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-011",
    type: "flashcard",
    topic: "web-components",
    difficulty: "intermediate",
    tags: ["shadow-dom", "scoped", "styling"],
    front: "`shadow: true` vs `scoped: true` in Stencil — what's the difference?",
    back: "**`shadow: true`** — a real shadow root.\n- True style encapsulation: outer CSS **cannot** reach in, inner CSS cannot leak out.\n- Slotted content stays in the light DOM.\n- Theming must go through CSS custom properties and `::part()`.\n\n**`scoped: true`** — Stencil's own scoping: no shadow root, class-based style scoping, and a slot polyfill.\n- Global CSS **can** reach in. This is often required when a legacy Angular app must style the components.\n- No shadow boundary, so `document.querySelector` finds internals and events don't need `composed`.\n\nChoose `shadow` for a clean, well-governed design system; choose `scoped` when consuming apps genuinely need to override styles or you must support tooling that can't pierce shadow roots.",
    explanation: "The honest trade-off is governance versus pragmatism. Shadow DOM is the correct default for a shared library because it prevents one app's CSS from breaking a component everywhere — but that same strength is why app teams file 'I can't style this' tickets.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-012",
    type: "flashcard",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["styling", "css-custom-properties", "shadow-parts"],
    front: "How do you theme a shadow-DOM component from an Angular app?",
    back: "Shadow DOM blocks outer selectors, so expose a deliberate styling API:\n\n**CSS custom properties** — these *do* pierce the shadow boundary:\n```css\n/* component */\n:host { background: var(--eaa-button-bg, #0057b8); }\n/* app */\neaa-button { --eaa-button-bg: #b8003f; }\n```\n\n**`::part()`** — expose internal elements explicitly:\n```tsx\n<span part=\"label\"><slot /></span>\n```\n```css\neaa-button::part(label) { letter-spacing: .04em; }\n```\n\n**`:host`**, **`:host(.compact)`**, and **`:host-context(.dark-theme)`** for host-driven variants.\n\nAngular's `ViewEncapsulation.Emulated` styles **do not** cross into a shadow root — the app's `_ngcontent` attribute selectors simply don't match inside it.",
    explanation: "That last line is the most-filed bug against a web-component design system. The correct response isn't `::ng-deep` (deprecated and it doesn't pierce shadow DOM anyway) — it's to add a custom property or a `part` to the component's public API.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-013",
    type: "flashcard",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["slots", "content-projection", "shadow-dom"],
    front: "How does content projection work across Stencil and Angular?",
    back: "Stencil uses `<slot>`; Angular uses `<ng-content>`. They're equivalent concepts and they nest fine:\n\n```tsx\n// Stencil\n<div class=\"card\">\n  <slot name=\"header\" />\n  <slot />                 {/* default slot */}\n</div>\n```\n```html\n<!-- Angular app -->\n<eaa-card>\n  <h2 slot=\"header\">{{ title() }}</h2>\n  <p>Projected body</p>\n</eaa-card>\n```\n\nCrucial detail: with `shadow: true`, slotted content **stays in the light DOM**. That means:\n- It's styled by the **Angular app's** styles, not the component's shadow styles.\n- Angular's change detection and bindings work on it normally, because Angular still owns those nodes.\n- Inside the component you can only style it via `::slotted(h2)`, which reaches **one level** and can only set outer-level properties.",
    explanation: "The 'slotted content is styled by the consumer' fact resolves a lot of confusion. It's actually a feature — the design system controls layout and structure, the app controls the content it projects.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-014",
    type: "flashcard",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["change-detection", "ngzone", "angular"],
    front: "Why might a Stencil component's updates not trigger Angular change detection?",
    back: "In a **zone-based** Angular app, Zone.js patches `addEventListener`, timers, and XHR so it knows when to re-check. If a custom element updates state through a path Zone.js didn't patch — or emits from inside a `requestAnimationFrame` or a lazily-loaded module boundary — the Angular view can go stale.\n\nFixes:\n1. **Wrapper components** call `NgZone.run()` when re-emitting events — the generated wrappers handle this.\n2. `ChangeDetectorRef.markForCheck()` in the handler.\n3. **Zoneless + signals**, where Angular no longer depends on Zone.js at all and updates are driven by explicit signal writes.\n\nNote the flip side: an `@Listen('window:scroll')` inside a custom element in a zone-based app can trigger change detection on **every scroll event**, which is a performance problem rather than a correctness one.",
    explanation: "Both directions are worth mentioning — stale views and over-triggering. It's also the concrete reason a component library is a common blocker for a team's zoneless migration, which ties this back to modern Angular.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-015",
    type: "flashcard",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["ssr", "hydration", "angular"],
    front: "How do you make custom elements work with Angular SSR?",
    back: "`customElements` doesn't exist on the server, so `defineCustomElements()` throws during SSR.\n\n```ts\n// app.config.ts / main.ts\nif (isPlatformBrowser(inject(PLATFORM_ID))) {\n  defineCustomElements();\n}\n```\n\nOptions for the server-rendered markup itself:\n1. **Client-only** — the tag renders as an empty element server-side and upgrades on hydration. Simple, but causes layout shift.\n2. **Stencil hydrate app** (`dist-hydrate-script`) — Stencil's server-side renderer produces declarative shadow DOM, so the component's markup is present in the initial HTML.\n3. **Declarative Shadow DOM** — `<template shadowrootmode=\"open\">` in the SSR output, which modern browsers attach natively.\n\nAlso guard `window`, `document`, and `localStorage` inside the components themselves — Stencil components run in the hydrate app on the server.",
    explanation: "The hydrate output target is the detail that distinguishes someone who has actually shipped SSR with a web-component design system. Without it you get a flash of unstyled/empty components and a real LCP penalty.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-016",
    type: "flashcard",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["accessibility", "shadow-dom", "aria"],
    front: "What are the accessibility pitfalls of Shadow DOM, and how do you handle them?",
    back: "IDs are **scoped per root**, so ARIA relationships break across the shadow boundary:\n\n```html\n<!-- BROKEN: the label is in the light DOM, the input is inside the shadow root -->\n<label for=\"email\">Email</label>\n<eaa-input id=\"email\"></eaa-input>\n```\n\nFixes:\n- Put the label **inside** the same shadow root as the input (usually via a `label` prop on the component).\n- Use `aria-label` / `aria-labelledby` **within** one root.\n- **ARIA element reflection** (`el.ariaLabelledByElements = [labelEl]`) crosses roots, where supported.\n- **`delegatesFocus: true`** on the shadow root so focusing the host focuses the inner control.\n- Ensure `:focus-visible` styles exist inside the shadow root — the app's global focus styles won't reach.\n\nTest with a screen reader, not just an automated audit — axe can't always see across roots either.",
    explanation: "This is the most common real accessibility defect in web-component design systems. The design implication: a component that wraps a form control should own its own label rather than expecting the consumer to associate one externally.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-017",
    type: "flashcard",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["testing", "stencil", "angular"],
    front: "How do you test Stencil components, and how do Angular apps test against them?",
    back: "**Stencil side:**\n```ts\n// Spec test — fast, jsdom, good for props/rendering logic\nconst page = await newSpecPage({ components: [EaaButton], html: `<eaa-button/>` });\nexpect(page.root).toEqualHtml(`...`);\n\n// E2E test — Puppeteer, real browser: required for Shadow DOM and events\nconst page = await newE2EPage({ html: `<eaa-button></eaa-button>` });\nconst spy = await page.spyOnEvent('eaaClick');\nawait (await page.find('eaa-button')).click();\nexpect(spy).toHaveReceivedEvent();\n```\n\nUse E2E for anything involving the shadow boundary, real event propagation, or CSS — jsdom's shadow support is incomplete.\n\n**Angular side:** with wrappers, they're ordinary components. With raw custom elements, add `CUSTOM_ELEMENTS_SCHEMA` to the `TestBed` and mock the component's behaviour — don't try to load the real definitions in a unit test.",
    explanation: "The spec-vs-e2e split maps directly onto the shadow boundary, which is a clean rule to state. And in Angular unit tests, mocking rather than loading the real elements keeps the test fast and avoids a custom-element registry that persists across tests.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-018",
    type: "flashcard",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["versioning", "governance", "micro-frontends"],
    front: "What happens if two versions of the same custom element load on one page?",
    back: "`customElements.define()` throws on a duplicate tag name, and the **first definition wins** — the second is silently ignored or errors. The registry is **global per document**, with no scoping.\n\nThis is the central governance problem of a shared component library across many apps (micro-frontends, module federation, a monolith with mixed dependency versions).\n\nMitigations:\n1. **Single-version policy** — enforce via peer dependencies and a monorepo. The cleanest answer.\n2. **Version-suffixed tag prefixes** for breaking majors: `eaa-button` → `eaa2-button`. Ugly but explicit.\n3. **Module federation with a shared singleton** so only one copy loads.\n4. **Guard the define** — check `customElements.get(tag)` first, so at least you fail gracefully with a clear warning rather than a confusing runtime error.",
    explanation: "This is the senior-level question for an EAA-style shared library and it has no elegant technical fix — it's a governance problem. Scoped custom element registries are a proposal, not a shipped feature you can rely on.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-019",
    type: "flashcard",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["api-design", "semver", "design-systems"],
    front: "What is the public API contract of a web component, and what counts as a breaking change?",
    back: "The contract is everything a consumer can depend on:\n- **Props** (names, types, defaults) and their **attribute** names\n- **Events** — names and `detail` shapes\n- **Slots** — names and expected content\n- **CSS custom properties** and **`::part()`** names\n- **`@Method()`** signatures\n- The **tag name** itself\n\nBreaking changes therefore include renaming a `part`, removing a CSS custom property, or changing an event's `detail` shape — things that look internal but are consumed externally.\n\nPractices: generate docs from the source (`docs-json`), run visual regression tests, deprecate before removing (keep the old prop and warn), and treat the design system's semver as a real contract, because dozens of apps depend on it.",
    explanation: "The `::part()` and custom-property point is the one people miss — they feel like styling details but they're published API, and removing one silently breaks every app that theming relies on. That's why generated docs from decorators matter.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-fc-020",
    type: "flashcard",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["performance", "bundling", "design-systems"],
    front: "How do you keep a web-component design system's bundle size under control?",
    back: "- **Prefer `dist-custom-elements`** with tree shaking when apps import specific components; `dist`'s lazy loader ships a manifest for everything.\n- **Avoid heavy dependencies** — a date picker that pulls in a full i18n library is paid for by every consuming app.\n- **Externalise peer dependencies** rather than bundling them per component.\n- **Per-component CSS**, not one global stylesheet.\n- **Lazy-load the genuinely heavy components** (rich editors, charts) rather than the whole library.\n- **Budget and monitor** — track the published bundle size in CI and fail on regressions.\n- In Angular, wrap heavy components in **`@defer`** so they're not in the initial chunk.\n\nMeasure the *consumer's* bundle, not the library's — that's the number that affects users.",
    explanation: "That final point is the one that changes behaviour. Library authors optimise their own build output; the metric that matters is what a typical app actually ships, which depends on tree-shakability and how the wrappers are structured.",
    createdAt: "2026-08-26",
  },
  // ── Multiple choice ───────────────────────────────────────────
  {
    id: "wc-mc-001",
    type: "multiple-choice",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["angular", "properties", "integration"],
    prompt: "`<eaa-table [columns]=\"cols\">` renders nothing, and the DOM shows `columns=\"[object Object]\"`. Why?",
    options: [
      { id: "a", text: "The array must be frozen before passing it to a custom element" },
      { id: "b", text: "Stencil props cannot accept arrays" },
      { id: "c", text: "Change detection has not run yet" },
      { id: "d", text: "Angular fell back to attribute binding on an unknown element, and attributes are strings" },
    ],
    correctOptionId: "d",
    timeLimitSeconds: 55,
    explanation: "Without a wrapper (or CUSTOM_ELEMENTS_SCHEMA plus a property binding), Angular treats the binding as an attribute and stringifies the value. The fix is a generated wrapper that assigns the DOM property, which can hold any JavaScript value. Stencil props handle arrays and objects fine once they arrive as properties.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-mc-002",
    type: "multiple-choice",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["forms", "controlvalueaccessor"],
    prompt: "`formControlName=\"email\"` on `<eaa-input>` produces no error, but the form value never updates. Why?",
    options: [
      { id: "a", text: "Shadow DOM prevents the form from reading the input value" },
      { id: "b", text: "The element does not implement ControlValueAccessor, so Angular forms have nothing to bind to" },
      { id: "c", text: "Custom elements cannot be used inside a reactive form" },
      { id: "d", text: "The form must be marked as touched first" },
    ],
    correctOptionId: "b",
    timeLimitSeconds: 55,
    explanation: "Angular forms talk to controls exclusively through the CVA interface. A custom element has no such interface, so the directive binds to nothing and fails silently — which is what makes it hard to diagnose. Generate a value accessor via the Angular output target's valueAccessorConfigs, or write the directive by hand.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-mc-003",
    type: "multiple-choice",
    topic: "web-components",
    difficulty: "intermediate",
    tags: ["styling", "shadow-dom"],
    prompt: "An app team says the component's button ignores their global `.btn { background: red }` rule. What do you tell them?",
    options: [
      { id: "a", text: "They need to set encapsulation: ViewEncapsulation.None" },
      { id: "b", text: "The stylesheet must be imported after the component library" },
      { id: "c", text: "Shadow DOM blocks outer CSS by design — expose a CSS custom property or a ::part() instead" },
      { id: "d", text: "They should use ::ng-deep to pierce the boundary" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 50,
    explanation: "That encapsulation is the point of Shadow DOM — it's what stops one app's CSS from breaking the component everywhere else. `::ng-deep` is deprecated and doesn't cross shadow roots anyway, and ViewEncapsulation affects Angular's own styles, not the shadow boundary. The right answer is to extend the component's public styling API.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-mc-004",
    type: "multiple-choice",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["events", "shadow-dom"],
    prompt: "A `@Event()` fires inside the component but the Angular listener never receives it. Most likely cause?",
    options: [
      { id: "a", text: "The event was configured with composed: false, so it stops at the shadow boundary" },
      { id: "b", text: "Angular cannot listen to CustomEvents at all" },
      { id: "c", text: "The event name contains a hyphen" },
      { id: "d", text: "The component was not lazily loaded" },
    ],
    correctOptionId: "a",
    timeLimitSeconds: 55,
    explanation: "`composed: true` is what allows an event to cross a shadow root and reach ancestors outside it; `bubbles: true` is also needed to travel up. Stencil defaults both to true, so this bites when someone explicitly overrides them. Angular binds to CustomEvents fine — the payload just arrives in `$event.detail`.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-mc-005",
    type: "multiple-choice",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["angular", "type-safety"],
    prompt: "Why is `CUSTOM_ELEMENTS_SCHEMA` a poor substitute for generated wrappers?",
    options: [
      { id: "a", text: "It disables unknown-property checking for every element in that component, so real typos stop failing the build" },
      { id: "b", text: "It increases bundle size significantly" },
      { id: "c", text: "It only works in NgModule-based applications" },
      { id: "d", text: "It prevents the component from rendering in production builds" },
    ],
    correctOptionId: "a",
    timeLimitSeconds: 55,
    explanation: "It's a component-wide opt-out of the template type checker, not a targeted allowance for one tag — so `[rotuer]` on a real Angular component silently becomes a no-op too. It also doesn't solve property binding for objects, typed events, or forms, which is the rest of what wrappers provide.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-mc-006",
    type: "multiple-choice",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["versioning", "micro-frontends"],
    prompt: "Two apps in a module-federation shell load `@eaa/core` v3 and v4, both defining `eaa-button`. What happens?",
    options: [
      { id: "a", text: "The browser uses the newest version automatically" },
      { id: "b", text: "Custom elements are versioned by package, so there is no conflict" },
      { id: "c", text: "The first define wins; the second throws or is ignored, so one app silently gets the wrong version" },
      { id: "d", text: "Both versions coexist, scoped to their own app" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 60,
    explanation: "The custom element registry is global per document with no scoping, so tag names collide. Mitigations are governance rather than technical: a single-version policy enforced by peer dependencies, a shared singleton in the federation config, or version-suffixed tag prefixes for breaking majors.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-mc-007",
    type: "multiple-choice",
    topic: "web-components",
    difficulty: "intermediate",
    tags: ["output-targets", "bundling"],
    prompt: "Which output target should an EAA-style library publish for Angular consumers who bundle the components?",
    options: [
      { id: "a", text: "www, since it includes everything needed" },
      { id: "b", text: "docs-json, which generates the Angular bindings" },
      { id: "c", text: "dist-custom-elements, plus the Angular output target for typed wrappers" },
      { id: "d", text: "dist, so components lazy-load automatically" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 50,
    explanation: "`dist-custom-elements` emits plain tree-shakable modules, so an app importing 3 of 80 components ships only 3. `dist`'s lazy loader is better when a page uses an unpredictable subset at runtime, but it defeats tree shaking. `www` is a demo site and `docs-json` is documentation metadata.",
    createdAt: "2026-08-26",
  },
  {
    id: "wc-mc-008",
    type: "multiple-choice",
    topic: "web-components",
    difficulty: "advanced",
    tags: ["ssr", "angular"],
    prompt: "Setting up SSR, the build fails with `customElements is not defined`. What is the fix?",
    options: [
      { id: "a", text: "Disable Shadow DOM in the component library" },
      { id: "b", text: "Move defineCustomElements() into ngOnInit" },
      { id: "c", text: "Call defineCustomElements() only in the browser, and use Stencil's hydrate app if you need server-rendered markup" },
      { id: "d", text: "Add customElements to the global scope with a polyfill on the server" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 55,
    explanation: "The registry is a browser API that doesn't exist in Node. Guarding with `isPlatformBrowser` fixes the crash, but leaves empty elements in the server HTML and a layout shift — Stencil's `dist-hydrate-script` renders declarative shadow DOM server-side to solve that properly. `ngOnInit` still runs during SSR, so it changes nothing.",
    createdAt: "2026-08-26",
  },
];
