import type { Question } from '@/types/content';

export const asyncPatternsQuestions: Question[] = [
  {
    id: 'async-001',
    type: 'flashcard',
    topic: 'async-patterns',
    difficulty: 'beginner',
    tags: ['Promise', 'states'],
    front: 'What are the three states of a JavaScript Promise?',
    back: '1. **Pending** — initial state, neither fulfilled nor rejected\n2. **Fulfilled** — operation completed successfully (`.then` callbacks run)\n3. **Rejected** — operation failed (`.catch` callbacks run)\n\nA promise is **settled** once it\'s fulfilled or rejected — it cannot change state again.\n\n```js\nconst p = new Promise((resolve, reject) => {\n  setTimeout(() => resolve(42), 100);\n});\n// pending → fulfilled(42)\n```',
    explanation: 'Once settled, a promise is immutable. This is what makes promises safer than callbacks for async control flow.',
    createdAt: '2024-01-01',
  },
  {
    id: 'async-002',
    type: 'multiple-choice',
    topic: 'async-patterns',
    difficulty: 'intermediate',
    tags: ['Promise.all', 'Promise.allSettled', 'concurrency'],
    prompt: 'What is the difference between `Promise.all` and `Promise.allSettled`?',
    options: [
      { id: 'a', text: 'Promise.all rejects immediately on the first rejection; Promise.allSettled waits for all and returns all results' },
      { id: 'b', text: 'Promise.allSettled is faster because it runs sequentially' },
      { id: 'c', text: 'Promise.all can handle more promises than Promise.allSettled' },
      { id: 'd', text: 'They are identical — both wait for all promises' },
    ],
    correctOptionId: 'a',
    timeLimitSeconds: 30,
    explanation: 'Promise.all "fails fast" — if any promise rejects, the whole thing rejects. Promise.allSettled always resolves with an array of {status, value/reason} objects for every promise, regardless of failures.',
    createdAt: '2024-01-01',
  },
  {
    id: 'async-003',
    type: 'flashcard',
    topic: 'async-patterns',
    difficulty: 'intermediate',
    tags: ['async-await', 'error-handling'],
    front: 'What are the best practices for async/await error handling?',
    back: '**try/catch for async code:**\n```js\nasync function fetchUser(id) {\n  try {\n    const res = await fetch(`/users/${id}`);\n    if (!res.ok) throw new Error(`HTTP ${res.status}`);\n    return await res.json();\n  } catch (err) {\n    console.error("Failed:", err);\n    throw err; // re-throw if caller should handle\n  }\n}\n```\n\n**Avoid unhandled promise rejections:**\n- Always await or `.catch()` promises\n- Handle `process.on("unhandledRejection", ...)` in Node.js',
    explanation: 'Forgetting `await` is a common bug — the function returns a pending promise instead of the resolved value.',
    createdAt: '2024-01-01',
  },
  {
    id: 'async-004',
    type: 'coding-challenge',
    topic: 'async-patterns',
    difficulty: 'intermediate',
    tags: ['concurrency', 'Promise.all', 'rate-limiting'],
    mode: 'complete',
    prompt: 'Implement `fetchWithConcurrencyLimit` that fetches multiple URLs but limits to N concurrent requests at a time.',
    starterCode: 'async function fetchWithConcurrencyLimit(\n  urls: string[],\n  limit: number\n): Promise<string[]> {\n  // Your implementation here\n  // Should fetch all URLs but only run `limit` at a time\n}',
    solutionCode: 'async function fetchWithConcurrencyLimit(\n  urls: string[],\n  limit: number\n): Promise<string[]> {\n  const results: string[] = new Array(urls.length);\n  let index = 0;\n\n  async function worker() {\n    while (index < urls.length) {\n      const i = index++;\n      const url = urls[i]!;\n      const res = await fetch(url);\n      results[i] = await res.text();\n    }\n  }\n\n  // Create `limit` workers running concurrently\n  const workers = Array.from({ length: Math.min(limit, urls.length) }, worker);\n  await Promise.all(workers);\n\n  return results;\n}',
    language: 'typescript',
    hints: [
      'Create a shared index counter that workers increment atomically',
      'Spawn `limit` worker functions, each processing URLs until none remain',
      'Use Promise.all to wait for all workers to complete',
      'The "worker pool" pattern avoids external libraries',
    ],
    explanation: 'The worker pool pattern uses a shared counter and multiple concurrent workers, each pulling the next item. This naturally limits concurrency without batching.',
    createdAt: '2024-01-01',
  },
  {
    id: 'async-005',
    type: 'multiple-choice',
    topic: 'async-patterns',
    difficulty: 'advanced',
    tags: ['AbortController', 'cancellation', 'fetch'],
    prompt: 'How do you cancel an in-flight `fetch` request?',
    options: [
      { id: 'a', text: 'Call fetch.cancel()' },
      { id: 'b', text: 'Set a timeout with setTimeout and reject the promise manually' },
      { id: 'c', text: 'Create an AbortController, pass signal to fetch, call abort()' },
      { id: 'd', text: 'Fetch requests cannot be cancelled once started' },
    ],
    correctOptionId: 'c',
    timeLimitSeconds: 30,
    explanation: 'AbortController.signal is passed as an option to fetch. Calling controller.abort() causes the fetch to reject with an AbortError. This is also useful in React useEffect cleanup to cancel requests on unmount.',
    createdAt: '2024-01-01',
  },
  {
    id: 'async-006',
    type: 'flashcard',
    topic: 'async-patterns',
    difficulty: 'advanced',
    tags: ['async-generators', 'async-iterators'],
    front: 'What are async generators and when do you use them?',
    back: 'Async generators (`async function*`) yield values asynchronously — each `yield` can `await`:\n\n```js\nasync function* paginate(url) {\n  let page = 1;\n  while (true) {\n    const res = await fetch(`${url}?page=${page}`);\n    const data = await res.json();\n    if (!data.items.length) return;\n    yield data.items;\n    page++;\n  }\n}\n\n// Consume with for-await-of:\nfor await (const items of paginate("/api/products")) {\n  process(items);\n}\n```\n\nUse cases: paginated APIs, streaming data, lazy sequences.',
    explanation: 'Async generators are the native way to model infinite or lazy async sequences. They replace complex manual Promise-chaining loops.',
    createdAt: '2024-01-01',
  },
  {
    id: 'async-007',
    type: 'multiple-choice',
    topic: 'async-patterns',
    difficulty: 'intermediate',
    tags: ['Promise.race', 'timeout'],
    prompt: 'How would you implement a timeout for a fetch request using `Promise.race`?',
    options: [
      { id: 'a', text: 'fetch(url, { timeout: 5000 })' },
      { id: 'b', text: 'Promise.race([fetch(url), new Promise((_, reject) => setTimeout(() => reject(new Error("timeout")), 5000))])' },
      { id: 'c', text: 'async function timeout() { await sleep(5000); throw Error("timeout") }; await timeout();' },
      { id: 'd', text: 'fetch cannot be given a timeout' },
    ],
    correctOptionId: 'b',
    timeLimitSeconds: 30,
    explanation: 'Promise.race resolves/rejects with the first settled promise. Race between the fetch and a timeout promise. If the timeout wins, the race rejects. The fetch continues in the background (use AbortController to actually cancel it).',
    createdAt: '2024-01-01',
  },
  {
    id: 'async-008',
    type: 'coding-challenge',
    topic: 'async-patterns',
    difficulty: 'advanced',
    tags: ['retry', 'exponential-backoff', 'error-handling'],
    mode: 'complete',
    prompt: 'Implement a `retry` function that retries an async operation up to N times with exponential backoff.',
    starterCode: 'async function retry<T>(\n  fn: () => Promise<T>,\n  maxAttempts: number,\n  baseDelayMs: number = 100\n): Promise<T> {\n  // Your implementation here\n  // Should retry on failure with 100ms, 200ms, 400ms... delays\n}',
    solutionCode: 'async function retry<T>(\n  fn: () => Promise<T>,\n  maxAttempts: number,\n  baseDelayMs: number = 100\n): Promise<T> {\n  let lastError: unknown;\n\n  for (let attempt = 0; attempt < maxAttempts; attempt++) {\n    try {\n      return await fn();\n    } catch (err) {\n      lastError = err;\n      if (attempt < maxAttempts - 1) {\n        const delay = baseDelayMs * Math.pow(2, attempt);\n        await new Promise(resolve => setTimeout(resolve, delay));\n      }\n    }\n  }\n\n  throw lastError;\n}\n\n// Usage:\nconst result = await retry(\n  () => fetch(\'/api/data\').then(r => r.json()),\n  3,\n  200 // 200ms, 400ms, 800ms\n);',
    language: 'typescript',
    hints: [
      'Loop from 0 to maxAttempts - 1',
      'On failure (except the last attempt), wait before retrying',
      'Exponential backoff: delay = baseDelayMs * 2^attempt',
      'Re-throw the last error if all attempts fail',
    ],
    explanation: 'Exponential backoff prevents hammering a failing service. Add jitter (randomize delay slightly) in production to prevent thundering herd.',
    createdAt: '2024-01-01',
  },
  {
    id: 'async-009',
    type: 'flashcard',
    topic: 'async-patterns',
    difficulty: 'intermediate',
    tags: ['microtasks', 'queueMicrotask'],
    front: 'What is the microtask queue in JavaScript?',
    back: 'The microtask queue runs **after the current task but before the next macrotask**:\n\n**Microtasks:** Promise callbacks (`.then`, `.catch`), `queueMicrotask()`, MutationObserver\n**Macrotasks:** setTimeout, setInterval, I/O, UI rendering\n\n```js\nqueueMicrotask(() => console.log("micro"));\nsetTimeout(() => console.log("macro"), 0);\nconsole.log("sync");\n// Output: sync → micro → macro\n```\n\nMicrotasks drain completely before the event loop proceeds to the next macrotask.',
    explanation: 'If microtasks schedule more microtasks, they all run before any macrotask — this can starve the event loop if not careful.',
    createdAt: '2024-01-01',
  },
  {
    id: 'async-010',
    type: 'multiple-choice',
    topic: 'async-patterns',
    difficulty: 'beginner',
    tags: ['async-await', 'promises', 'syntax'],
    prompt: 'What does `async function` always return?',
    options: [
      { id: 'a', text: 'The raw return value' },
      { id: 'b', text: 'A Promise that wraps the return value' },
      { id: 'c', text: 'An Observable' },
      { id: 'd', text: 'undefined' },
    ],
    correctOptionId: 'b',
    timeLimitSeconds: 20,
    explanation: 'Every async function returns a Promise. If you return 42, the caller gets Promise.resolve(42). If you throw, the caller gets a rejected promise. This is why you need await or .then() to get the actual value.',
    createdAt: '2024-01-01',
  },
  {
    id: 'async-011',
    type: 'flashcard',
    topic: 'async-patterns',
    difficulty: 'intermediate',
    tags: ['callback-pattern', 'promisify'],
    front: 'How do you convert a callback-based function to a Promise?',
    back: '**Manual wrapping:**\n```js\nfunction readFilePromise(path) {\n  return new Promise((resolve, reject) => {\n    fs.readFile(path, "utf8", (err, data) => {\n      if (err) reject(err);\n      else resolve(data);\n    });\n  });\n}\n```\n\n**Using Node\'s built-in util.promisify:**\n```js\nconst { promisify } = require("util");\nconst readFileAsync = promisify(fs.readFile);\nconst data = await readFileAsync("file.txt", "utf8");\n```\n\n`fs.promises` already provides promisified versions.',
    explanation: 'util.promisify works with any function following the Node.js error-first callback convention: `callback(err, result)`.',
    createdAt: '2024-01-01',
  },
];
