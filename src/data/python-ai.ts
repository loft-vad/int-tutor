import type { Question } from '@/types/content';

/**
 * Python for AI engineers.
 *
 * Matrix rows: "Python" (installing, poetry/venv/pip, syntax, control flow, functions,
 * modules, file handling, collections, error handling, Jupyter), "Jupiter Notebooks",
 * "Working in your primary language with txt/doc/xaml/html/csv/json/yaml files",
 * "ReGex", and "Data structures: Vectors".
 */

export const pythonAiQuestions: Question[] = [
  // ── Flashcards ────────────────────────────────────────────────
  {
    id: "py-fc-001",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "beginner",
    tags: ["environments", "packaging", "uv"],
    front: "venv, pip, poetry, uv, conda — which do you use and why?",
    back: "- **venv + pip** — in the standard library. Always available; the lowest-friction baseline.\n- **Poetry** — dependency resolution, a lockfile, and packaging in one tool. Good for libraries and reproducible apps.\n- **uv** — a Rust-based installer/resolver that is a drop-in, dramatically faster replacement for pip and venv, with lockfiles and Python version management. Increasingly the default choice.\n- **conda / mamba** — manages **non-Python** binaries too (CUDA, MKL, compilers). Historically necessary for GPU/scientific stacks.\n\n```bash\nuv venv && source .venv/bin/activate\nuv pip install -r requirements.txt\n```\n\nThe one non-negotiable: a **lockfile**. `pip install langchain` twice a month apart gives you two different dependency trees.",
    explanation: "The AI ecosystem moves fast and breaks compatibility often, so reproducibility is not academic — 'it worked last week' is a real and frequent failure. Naming uv also signals you're current rather than working from 2021 habits.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-002",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "beginner",
    tags: ["collections", "complexity", "data-structures"],
    front: "What are Python's core collections and when do you use each?",
    back: "| Type | Mutable | Ordered | Use for |\n|---|---|---|---|\n| `list` | yes | yes | Sequences; the closest thing to a dynamic array/vector |\n| `tuple` | no | yes | Fixed records; hashable, so usable as dict keys |\n| `dict` | yes | yes (3.7+) | Key→value; O(1) lookup |\n| `set` | yes | no | Membership and deduplication; O(1) `in` |\n\nThe performance point that matters: `x in my_list` is **O(n)**, `x in my_set` is **O(1)**. Deduplicating a million document hashes with a list is the classic accidental O(n²).\n\nAlso know `collections.Counter`, `defaultdict`, and `deque` (O(1) appends/pops at both ends).",
    explanation: "The matrix's 'Data structures: Vectors (dynamic arrays)' row maps to `list`. The interview-worthy detail is amortised O(1) `append` via over-allocation, and O(n) `insert(0, x)` — which is exactly what `deque` exists to fix.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-003",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "beginner",
    tags: ["file-formats", "json", "csv", "yaml"],
    front: "Read and write JSON, CSV, and YAML in Python.",
    back: "```python\nimport json, csv, yaml   # yaml = PyYAML\n\n# JSON\ndata = json.loads(text)\njson.dump(obj, open(\"out.json\", \"w\"), indent=2, ensure_ascii=False)\n\n# JSONL — the standard format for LLM datasets and batch APIs\nwith open(\"data.jsonl\") as f:\n    rows = [json.loads(line) for line in f]\n\n# CSV — DictReader gives you dicts keyed by header\nwith open(\"in.csv\", newline=\"\", encoding=\"utf-8\") as f:\n    rows = list(csv.DictReader(f))\n\n# YAML — always safe_load; yaml.load can construct arbitrary objects\nconfig = yaml.safe_load(open(\"config.yaml\"))\n```\n\nGotchas: pass `newline=\"\"` to `open` for CSV or you get blank rows on Windows; always specify `encoding=\"utf-8\"`; and **never** use `yaml.load` on untrusted input.",
    explanation: "`yaml.safe_load` is a genuine security answer, not pedantry — `yaml.load` on untrusted input is arbitrary code execution. JSONL is worth knowing by name because it's the format every batch API and fine-tuning pipeline expects.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-004",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "intermediate",
    tags: ["pydantic", "structured-output", "validation"],
    front: "How do you use Pydantic for LLM structured output?",
    back: "Pydantic models are the bridge between an LLM's JSON and your typed code, and they generate the JSON Schema you send to the model:\n\n```python\nfrom pydantic import BaseModel, Field\nfrom typing import Literal\n\nclass Ticket(BaseModel):\n    category: Literal[\"billing\", \"technical\", \"account\"]\n    severity: int = Field(ge=1, le=5)\n    summary: str = Field(max_length=200)\n\nschema = Ticket.model_json_schema()      # send this to the provider\n\ntry:\n    ticket = Ticket.model_validate_json(raw)\nexcept ValidationError as e:\n    retry(raw, errors=e.errors())        # feed the errors back to the model\n```\n\nOne definition gives you the schema, the runtime validation, and the static type.",
    explanation: "The retry-with-validation-errors loop is the pattern to describe: on a schema mismatch, send the model its own output plus the specific validation failures. Success rates go up sharply compared with a blind retry.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-005",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "intermediate",
    tags: ["asyncio", "concurrency", "performance"],
    front: "How does async work in Python, and why does it matter for LLM apps?",
    back: "LLM calls are **I/O-bound** — the process sits idle waiting on the network. `asyncio` lets one thread handle hundreds of concurrent calls.\n\n```python\nimport asyncio\n\nasync def summarize(doc):\n    return await client.chat.completions.create(...)\n\nasync def main(docs):\n    sem = asyncio.Semaphore(10)              # respect provider concurrency\n    async def guarded(d):\n        async with sem:\n            return await summarize(d)\n    return await asyncio.gather(*(guarded(d) for d in docs),\n                                return_exceptions=True)\n```\n\nKey points: `gather` with `return_exceptions=True` so one failure doesn't kill the batch; a **semaphore** to stay under rate limits; and never call a blocking function inside a coroutine — it stalls the whole loop (`asyncio.to_thread` for those).",
    explanation: "The blocking-call warning is the most common real bug: one synchronous `requests.get` inside an async pipeline serialises everything, and the symptom is 'async made no difference', which people then blame on Python.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-006",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "advanced",
    tags: ["gil", "concurrency", "performance"],
    front: "Explain the GIL and its practical consequences.",
    back: "The **Global Interpreter Lock** allows only one thread to execute Python bytecode at a time in CPython.\n\nConsequences:\n- **I/O-bound** (API calls, disk, network) — threads and asyncio work fine; the GIL is released during I/O.\n- **CPU-bound** pure Python — threads give no speedup. Use `multiprocessing` or a compiled extension.\n- **NumPy / PyTorch / tokenizers** — these release the GIL in their C code, so they do parallelise.\n\nFor AI work this usually means: asyncio for the API calls, and let the native libraries handle the numeric parallelism.\n\nPython 3.13 ships an experimental free-threaded build (PEP 703) that removes the GIL, but it isn't the default yet.",
    explanation: "Mentioning PEP 703 shows you're current. The practical takeaway to state: for a typical LLM pipeline the GIL is a non-issue because you're waiting on the network, not computing — so reach for asyncio, not multiprocessing.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-007",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "intermediate",
    tags: ["error-handling", "retries", "resilience"],
    front: "Write a robust API-call helper with retries and error handling.",
    back: "```python\nimport time, random, logging\n\ndef call_with_retry(fn, *, attempts=5, base=1.0):\n    for i in range(attempts):\n        try:\n            return fn()\n        except RateLimitError as e:\n            wait = float(getattr(e, \"retry_after\", None) or base * 2 ** i)\n        except (APIConnectionError, InternalServerError):\n            wait = base * 2 ** i\n        except (BadRequestError, AuthenticationError):\n            raise                                # never retry these\n        if i == attempts - 1:\n            raise\n        logging.warning(\"retry %d in %.1fs\", i + 1, wait)\n        time.sleep(wait + random.uniform(0, 0.5))   # jitter\n```\n\nPrinciples: catch **specific** exceptions, distinguish retryable from terminal, exponential backoff **with jitter**, honour `Retry-After`, cap the attempts, and log every retry.",
    explanation: "'Basic error handling (try-except)' is a Novice matrix row, but this is the version that matters in practice. The two details interviewers listen for: never retrying 4xx client errors, and jitter — without it your workers synchronise and re-trigger the rate limit together.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-008",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "beginner",
    tags: ["regex", "text-processing"],
    front: "What regex features do you need day to day?",
    back: "```python\nimport re\n\nre.findall(r\"\\b[\\w.+-]+@[\\w-]+\\.[\\w.]+\\b\", text)   # emails\nre.sub(r\"\\s+\", \" \", text).strip()                     # normalise whitespace\nm = re.match(r\"(?P<key>\\w+)=(?P<val>.*)\", line)        # named groups\nre.split(r\"(?<=[.!?])\\s+\", text)                       # naive sentence split\npat = re.compile(r\"...\", re.IGNORECASE | re.MULTILINE)  # compile once, reuse\n```\n\nCore syntax: character classes `[abc]` `\\d` `\\w` `\\s`, quantifiers `* + ? {n,m}`, anchors `^ $ \\b`, groups `( )` and non-capturing `(?: )`, alternation `|`, lookarounds `(?= ) (?<= )`, and **lazy** `*?` versus greedy `*`.\n\nGreedy-vs-lazy is the classic bug: `<.*>` swallows an entire line; `<.*?>` matches one tag.",
    explanation: "The matrix's Novice ReGex row asks for exactly the basics plus knowing the testing tools (regex101). The mature addition: catastrophic backtracking (ReDoS) on nested quantifiers over untrusted input is a real availability bug, so keep patterns simple and consider `re2`.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-009",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "beginner",
    tags: ["jupyter", "notebooks", "workflow"],
    front: "What are Jupyter notebooks good for, and where do they fail?",
    back: "**Good for:** exploring data, iterating on prompts, visualising results, sharing a reproducible analysis, and evaluation runs where you want the outputs preserved alongside the code.\n\n**Fail at:** production code. Hidden execution-order state (cell 5 run after cell 9), poor diffs in git, no straightforward testing, and copy-paste rather than reuse.\n\nPractices that help:\n- **Restart & Run All** before you trust or share a result.\n- Factor real logic into a `.py` module and import it; keep the notebook as the driver.\n- `nbstripout` or Jupytext to make diffs reviewable.\n- Pin the kernel's environment; `%pip install` inside the notebook, not `!pip`.\n\nExport: `.ipynb`, `.html`, `.py`, PDF.",
    explanation: "'Jupiter Notebooks' is a full Novice matrix row. The hidden-state point is the one that shows experience — 'it worked in my notebook' is usually an execution-order artifact, which is why Restart & Run All is the minimum bar before believing a number.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-010",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "intermediate",
    tags: ["numpy", "embeddings", "vectorization"],
    front: "Which numeric operations should you know for embedding work?",
    back: "```python\nimport numpy as np\n\ndef normalize(v):\n    return v / np.linalg.norm(v, axis=-1, keepdims=True)\n\n# Cosine similarity of one query against N documents — vectorised\nsims = normalize(docs) @ normalize(q)          # (N, d) @ (d,) -> (N,)\ntop_k = np.argpartition(-sims, k)[:k]          # O(N), beats a full sort\ntop_k = top_k[np.argsort(-sims[top_k])]        # then sort just the k\n```\n\nKey ideas: **vectorise** instead of looping (orders of magnitude faster), `argpartition` for top-k rather than sorting everything, `float32` not `float64` for embeddings (half the memory, same quality), and broadcasting rules for shape errors.",
    explanation: "This *is* brute-force vector search — for a few thousand vectors it's exact, sub-millisecond, and needs no vector database at all. Being able to write it is both a NumPy check and a signal that you won't over-engineer small-scale retrieval.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-011",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "intermediate",
    tags: ["project-structure", "fastapi", "config"],
    front: "How do you structure a Python AI project?",
    back: "```\nproject/\n├── pyproject.toml          # deps + lockfile, tool config\n├── src/app/\n│   ├── config.py           # pydantic-settings, env-driven\n│   ├── llm/                # provider clients behind one interface\n│   ├── prompts/            # versioned templates, not f-strings in code\n│   ├── retrieval/          # chunking, embedding, search\n│   ├── chains/             # orchestration\n│   └── api/                # FastAPI routes\n├── evals/                  # datasets + scoring, run in CI\n├── notebooks/              # exploration only\n└── tests/\n```\n\nRules that matter: **secrets from the environment** (`pydantic-settings`), **prompts as versioned artifacts** rather than inline strings, provider SDKs behind your own interface, and evals as a first-class directory — not a notebook someone ran once.",
    explanation: "The prompts-as-artifacts and evals-as-code choices are the two that separate a prototype from a maintainable system, and both map to matrix rows on evaluation and optimization.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-012",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "intermediate",
    tags: ["config", "secrets", "pydantic"],
    front: "How do you handle configuration and secrets in a Python AI service?",
    back: "```python\nfrom pydantic_settings import BaseSettings, SettingsConfigDict\n\nclass Settings(BaseSettings):\n    model_config = SettingsConfigDict(env_file=\".env\", extra=\"ignore\")\n\n    openai_api_key: str\n    model_name: str = \"gpt-4o-mini\"\n    max_tokens: int = 1024\n    temperature: float = 0.0\n\nsettings = Settings()   # fails fast at startup if a key is missing\n```\n\nRules:\n- Secrets from env vars or a secret manager — never in code, never in a prompt, never in a notebook you commit.\n- `.env` for local only, and in `.gitignore`; ship a `.env.example`.\n- **Validate at startup**, so a missing key is a boot failure rather than a 3am 401.\n- Keep model name, temperature, and prompt version as *config* so you can change behaviour without a deploy.",
    explanation: "That last bullet is the operationally important one: making the model and prompt version runtime config gives you an instant kill switch and rollback, which is exactly what you want when a model change degrades quality in production.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-013",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "intermediate",
    tags: ["tokens", "cost", "tiktoken"],
    front: "How do you count tokens and estimate cost in Python?",
    back: "```python\nimport tiktoken\nenc = tiktoken.encoding_for_model(\"gpt-4o\")\nn_in = len(enc.encode(prompt))\n\ncost = n_in / 1_000_000 * IN_PRICE + n_out / 1_000_000 * OUT_PRICE\n```\n\nNotes:\n- Use the **provider's** tokenizer; `len(text) / 4` is a rough sanity check, not a budget.\n- Different model families tokenize differently — an Anthropic count is not an OpenAI count.\n- Chat messages carry a few tokens of per-message overhead beyond the content.\n- The authoritative number is the `usage` field in the **response**; use it for billing and log it per request.\n- Count **before** sending when you need to enforce a limit or truncate deliberately.",
    explanation: "The 'trust the response usage field for accounting, use the local tokenizer for pre-flight limits' distinction is the practical answer. Teams that estimate from character counts routinely miscalculate their unit economics.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-014",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "intermediate",
    tags: ["type-hints", "protocol", "mypy"],
    front: "What do you need to know about type hints in modern Python?",
    back: "```python\nfrom typing import Protocol, Literal, TypedDict\n\ntype Vector = list[float]                    # 3.12+ type alias\n\nclass Chunk(TypedDict):\n    id: str\n    text: str\n    score: float\n\nclass Retriever(Protocol):                   # structural typing\n    def search(self, q: str, k: int = 5) -> list[Chunk]: ...\n\ndef rerank(chunks: list[Chunk], q: str) -> list[Chunk]: ...\n```\n\nModern syntax: built-in generics (`list[str]`, not `List[str]`), `X | None` instead of `Optional[X]`, `Protocol` for duck-typed interfaces, `Literal` for closed string sets.\n\nHints are not enforced at runtime — run **mypy** or **pyright** in CI, or they're just comments.",
    explanation: "`Protocol` is the one worth highlighting: it lets you define a `Retriever` or `LLMProvider` interface that third-party classes satisfy structurally, without inheritance — the Pythonic version of the adapter seam.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-015",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "intermediate",
    tags: ["generators", "streaming", "memory"],
    front: "How do you process a very large file without loading it into memory?",
    back: "Stream it — generators are the idiomatic tool:\n\n```python\ndef chunks(path, size=1000):\n    batch = []\n    with open(path, encoding=\"utf-8\") as f:\n        for line in f:                       # lazy, one line at a time\n            batch.append(json.loads(line))\n            if len(batch) == size:\n                yield batch\n                batch = []\n    if batch:\n        yield batch\n\nfor batch in chunks(\"corpus.jsonl\"):\n    embed_and_upsert(batch)                  # constant memory\n```\n\nAlso: `pandas.read_csv(..., chunksize=...)`, `itertools.islice` for taking a slice of a stream, and Polars or PyArrow for larger-than-memory columnar data.\n\nThe rule: never `f.read()` or `json.load` a file whose size you don't control.",
    explanation: "This is the ingestion pipeline pattern in miniature — batching for the embedding API while keeping memory constant. It also connects to the matrix's large-scale document collection row, where the naive load-everything approach simply fails.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-fc-016",
    type: "flashcard",
    topic: "python-ai",
    difficulty: "advanced",
    tags: ["testing", "pytest", "mocking"],
    front: "How do you test LLM-dependent Python code?",
    back: "Separate the deterministic from the probabilistic:\n\n```python\n# Unit: mock the provider entirely\ndef test_parses_tool_call(monkeypatch):\n    monkeypatch.setattr(client.chat.completions, \"create\",\n                        lambda **kw: FAKE_RESPONSE)\n    assert handle(\"...\") == expected\n\n# Integration: record/replay cassettes (vcrpy) for deterministic CI\n# Eval: a separate suite, run on a schedule, scored not asserted\n```\n\n- **Unit tests** cover prompt assembly, parsing, retries, and error handling — all fully deterministic, and where most of your bugs actually live.\n- **Cassettes** give realistic responses without cost or flakiness.\n- **Evals** measure model quality; they belong in a scheduled job with tracked scores, not in a pass/fail CI gate that flakes.",
    explanation: "The dividing line is the answer: assert on your code, measure the model. Teams that put model-quality checks in unit tests end up with a flaky suite everyone ignores — which is worse than having no check at all.",
    createdAt: "2026-08-26",
  },
  // ── Multiple choice ───────────────────────────────────────────
  {
    id: "py-mc-001",
    type: "multiple-choice",
    topic: "python-ai",
    difficulty: "intermediate",
    tags: ["asyncio", "rate-limits"],
    prompt: "You call an embedding API for 10,000 documents with `asyncio.gather` and no limits. What happens?",
    options: [
      { id: "a", text: "The GIL serialises the requests, so concurrency stays at 1" },
      { id: "b", text: "You immediately exceed the provider's concurrency and rate limits and most requests fail with 429s" },
      { id: "c", text: "asyncio automatically throttles to a safe concurrency level" },
      { id: "d", text: "It runs at exactly the provider's limit because of TCP backpressure" },
    ],
    correctOptionId: "b",
    timeLimitSeconds: 50,
    explanation: "`gather` fires everything at once. You need an `asyncio.Semaphore` to cap in-flight requests, plus retry-with-backoff for the 429s that still occur. The GIL is irrelevant here — these are I/O-bound calls, which is exactly where asyncio works.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-mc-002",
    type: "multiple-choice",
    topic: "python-ai",
    difficulty: "beginner",
    tags: ["collections", "complexity"],
    prompt: "Deduplicating 2 million document hashes, `if h in seen` where `seen` is a list, is slow. Why?",
    options: [
      { id: "a", text: "Lists cannot store strings efficiently" },
      { id: "b", text: "The GIL prevents the loop from being optimised" },
      { id: "c", text: "String comparison in Python is unusually slow" },
      { id: "d", text: "List membership is O(n), making the loop O(n²); a set gives O(1) lookups" },
    ],
    correctOptionId: "d",
    timeLimitSeconds: 45,
    explanation: "This is the most common accidental quadratic in data pipelines. Switching `seen` to a set turns hours into seconds. Knowing the complexity of `in` for list versus set versus dict is basic but decisive at ingestion scale.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-mc-003",
    type: "multiple-choice",
    topic: "python-ai",
    difficulty: "beginner",
    tags: ["yaml", "security"],
    prompt: "Which is the correct way to load a YAML config file supplied by a user?",
    options: [
      { id: "a", text: "yaml.load(f)" },
      { id: "b", text: "yaml.full_load(f)" },
      { id: "c", text: "eval(f.read())" },
      { id: "d", text: "yaml.safe_load(f)" },
    ],
    correctOptionId: "d",
    timeLimitSeconds: 40,
    explanation: "`yaml.load` without a safe loader can instantiate arbitrary Python objects, which is remote code execution on untrusted input. `safe_load` restricts construction to plain data types. `eval` on file contents is straightforwardly catastrophic.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-mc-004",
    type: "multiple-choice",
    topic: "python-ai",
    difficulty: "intermediate",
    tags: ["asyncio", "debugging"],
    prompt: "Your async pipeline shows no speedup over the synchronous version. Most likely cause?",
    options: [
      { id: "a", text: "The provider serialises requests from the same API key" },
      { id: "b", text: "A blocking synchronous call inside a coroutine is stalling the event loop" },
      { id: "c", text: "The GIL prevents any concurrency in asyncio" },
      { id: "d", text: "asyncio.gather runs coroutines sequentially" },
    ],
    correctOptionId: "b",
    timeLimitSeconds: 50,
    explanation: "One `requests.get` or a blocking file read inside an `async def` freezes the entire loop, so everything serialises. Use an async client (httpx/aiohttp) or wrap the blocking call in `asyncio.to_thread`. The GIL isn't the issue for I/O-bound work.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-mc-005",
    type: "multiple-choice",
    topic: "python-ai",
    difficulty: "intermediate",
    tags: ["pydantic", "validation", "retries"],
    prompt: "The model returns JSON that fails Pydantic validation. What is the most effective retry?",
    options: [
      { id: "a", text: "Lower the temperature and retry the original request" },
      { id: "b", text: "Send the model its own output plus the specific validation errors and ask it to correct them" },
      { id: "c", text: "Retry the original request unchanged" },
      { id: "d", text: "Fill missing fields with defaults and continue" },
    ],
    correctOptionId: "b",
    timeLimitSeconds: 50,
    explanation: "A blind retry re-rolls the same dice; defaults silently fabricate data. Feeding back the concrete errors — 'severity must be between 1 and 5, you returned 7' — gives the model exactly what it needs to fix, and success rates jump. Better still, use constrained decoding so the failure can't occur.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-mc-006",
    type: "multiple-choice",
    topic: "python-ai",
    difficulty: "beginner",
    tags: ["regex"],
    prompt: "Which regex correctly extracts a single HTML tag rather than an entire line?",
    options: [
      { id: "a", text: "<[^>]>" },
      { id: "b", text: "<.+>" },
      { id: "c", text: "<.*?>" },
      { id: "d", text: "<.*>" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 45,
    explanation: "`.*` is greedy and runs to the last `>` on the line; `.*?` is lazy and stops at the first. (`<[^>]*>` is the more efficient equivalent — note the missing `*` in the third option makes it match exactly one character.) Greedy-versus-lazy is the single most common regex bug.",
    createdAt: "2026-08-26",
  },
  {
    id: "py-mc-007",
    type: "multiple-choice",
    topic: "python-ai",
    difficulty: "advanced",
    tags: ["testing", "evaluation"],
    prompt: "Where do model-quality evaluations belong in your pipeline?",
    options: [
      { id: "a", text: "In the unit test suite, asserting exact expected outputs" },
      { id: "b", text: "In a pre-commit hook so bad prompts never get committed" },
      { id: "c", text: "Nowhere automated — manual review is more reliable" },
      { id: "d", text: "A separate scheduled suite with tracked scores — not a pass/fail unit-test gate" },
    ],
    correctOptionId: "d",
    timeLimitSeconds: 50,
    explanation: "Model outputs vary run to run, so exact-match assertions in CI produce a flaky suite that everyone learns to ignore. Evals are measurements with trends and thresholds; unit tests are for the deterministic code around the model, which is where most bugs actually live.",
    createdAt: "2026-08-26",
  },
];
