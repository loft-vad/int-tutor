import type { Question } from '@/types/content';

export const systemDesignQuestions: Question[] = [
  {
    id: 'sd-001',
    type: 'flashcard',
    topic: 'system-design',
    difficulty: 'intermediate',
    tags: ['REST', 'HTTP', 'API'],
    front: 'What are the key principles of RESTful API design?',
    back: '**REST** (Representational State Transfer) constraints:\n\n1. **Stateless** — each request contains all info needed; server stores no session\n2. **Resource-based URLs** — nouns, not verbs: `/users/123` not `/getUser`\n3. **HTTP verbs** map to CRUD:\n   - `GET` → Read\n   - `POST` → Create\n   - `PUT/PATCH` → Update\n   - `DELETE` → Delete\n4. **Consistent status codes** — 200 OK, 201 Created, 400 Bad Request, 401 Unauthorized, 404 Not Found, 500 Server Error\n5. **JSON responses** with clear resource shapes\n6. **Versioning** — `/api/v1/users`',
    explanation: 'REST is the dominant API style for web services. Know it cold — most fullstack interviews touch it.',
    createdAt: '2024-01-01',
  },
  {
    id: 'sd-002',
    type: 'multiple-choice',
    topic: 'system-design',
    difficulty: 'beginner',
    tags: ['HTTP', 'status-codes'],
    prompt: 'A client sends a `POST /users` request with a malformed JSON body. What HTTP status code should the server return?',
    options: [
      { id: 'a', text: '200 OK' },
      { id: 'b', text: '400 Bad Request' },
      { id: 'c', text: '401 Unauthorized' },
      { id: 'd', text: '500 Internal Server Error' },
    ],
    correctOptionId: 'b',
    timeLimitSeconds: 25,
    explanation: '400 Bad Request indicates the server cannot process the request due to client error (malformed syntax, invalid input). 401 means authentication is required. 500 means the server crashed — inappropriate here since the problem is the client\'s payload.',
    createdAt: '2024-01-01',
  },
  {
    id: 'sd-003',
    type: 'flashcard',
    topic: 'system-design',
    difficulty: 'intermediate',
    tags: ['caching', 'performance'],
    front: 'What is caching and what are the main caching strategies?',
    back: '**Caching** stores computed results to avoid expensive re-computation or network calls.\n\n**Strategies:**\n- **Cache-aside (lazy loading)**: App checks cache → miss → fetch from DB → write to cache\n- **Write-through**: Write to DB and cache simultaneously; cache always fresh\n- **Write-back (write-behind)**: Write to cache first, flush to DB asynchronously (faster writes, risk of data loss)\n- **Read-through**: Cache sits in front of DB; fetches on miss automatically\n\n**Cache invalidation** is hard — stale data is the main risk.\n\n**Common tools:** Redis, Memcached, HTTP cache headers (`Cache-Control`, `ETag`), CDN.',
    explanation: '"There are only two hard problems in CS: cache invalidation and naming things." Know when to cache and how to invalidate.',
    createdAt: '2024-01-01',
  },
  {
    id: 'sd-004',
    type: 'multiple-choice',
    topic: 'system-design',
    difficulty: 'intermediate',
    tags: ['scaling', 'horizontal', 'vertical'],
    prompt: 'What is the difference between horizontal and vertical scaling?',
    options: [
      { id: 'a', text: 'Horizontal = add more CPUs to one machine; Vertical = add more machines' },
      { id: 'b', text: 'Horizontal = add more machines; Vertical = add more resources (CPU/RAM) to existing machines' },
      { id: 'c', text: 'They are the same thing' },
      { id: 'd', text: 'Horizontal scaling only applies to databases' },
    ],
    correctOptionId: 'b',
    timeLimitSeconds: 25,
    explanation: 'Horizontal scaling (scale out) adds more instances and is preferred for stateless services — it\'s cheaper and more resilient. Vertical scaling (scale up) hits hardware limits and creates a single point of failure. Most cloud architectures favor horizontal scaling with load balancers.',
    createdAt: '2024-01-01',
  },
  {
    id: 'sd-005',
    type: 'flashcard',
    topic: 'system-design',
    difficulty: 'intermediate',
    tags: ['database', 'SQL', 'NoSQL'],
    front: 'When would you choose a SQL database over NoSQL, and vice versa?',
    back: '**Choose SQL (PostgreSQL, MySQL) when:**\n- Data has clear relational structure and complex joins\n- ACID transactions are critical (financial, inventory)\n- Schema is stable and well-defined\n- Complex queries with aggregations\n\n**Choose NoSQL (MongoDB, DynamoDB, Redis) when:**\n- Schema is flexible or evolving rapidly\n- Need horizontal scale at massive volume\n- Document, key-value, or graph data models fit better\n- Low-latency lookups by a single key\n- Eventual consistency is acceptable\n\n**Key trade-off**: SQL → consistency + query flexibility; NoSQL → scale + flexibility + speed.',
    explanation: 'Most systems use both. PostgreSQL with JSONB can blur the line. Know the CAP theorem trade-offs.',
    createdAt: '2024-01-01',
  },
  {
    id: 'sd-006',
    type: 'multiple-choice',
    topic: 'system-design',
    difficulty: 'intermediate',
    tags: ['load-balancer', 'availability'],
    prompt: 'What is the primary purpose of a load balancer?',
    options: [
      { id: 'a', text: 'To compress HTTP responses for faster transmission' },
      { id: 'b', text: 'To distribute incoming requests across multiple server instances' },
      { id: 'c', text: 'To encrypt all traffic between client and server' },
      { id: 'd', text: 'To cache static assets at the network edge' },
    ],
    correctOptionId: 'b',
    timeLimitSeconds: 25,
    explanation: 'A load balancer distributes traffic across multiple servers to improve availability, fault tolerance, and throughput. It also performs health checks and reroutes traffic away from unhealthy instances. CDNs handle asset caching; TLS termination can happen at the load balancer but that\'s a secondary concern.',
    createdAt: '2024-01-01',
  },
  {
    id: 'sd-007',
    type: 'flashcard',
    topic: 'system-design',
    difficulty: 'advanced',
    tags: ['CAP-theorem', 'distributed-systems'],
    front: 'What does the CAP theorem state?',
    back: 'The **CAP theorem** states a distributed data store can guarantee at most **two** of:\n\n- **C**onsistency — every read receives the most recent write\n- **A**vailability — every request receives a (non-error) response\n- **P**artition tolerance — the system continues despite network partitions\n\nSince network partitions are unavoidable in distributed systems, you **must sacrifice either C or A**:\n\n- **CP systems** (e.g., HBase, ZooKeeper): return errors when partitioned\n- **AP systems** (e.g., Cassandra, DynamoDB): return potentially stale data\n\n**PACELC** is a more nuanced model that also considers latency.',
    explanation: 'Understanding CAP helps you reason about trade-offs in any distributed system — crucial for system design interviews.',
    createdAt: '2024-01-01',
  },
  {
    id: 'sd-008',
    type: 'multiple-choice',
    topic: 'system-design',
    difficulty: 'intermediate',
    tags: ['message-queue', 'async'],
    prompt: 'Why would you use a message queue (e.g., Kafka, RabbitMQ) between two services?',
    options: [
      { id: 'a', text: 'To make the communication synchronous and strongly consistent' },
      { id: 'b', text: 'To decouple services and handle traffic spikes via buffering' },
      { id: 'c', text: 'To replace the need for a load balancer' },
      { id: 'd', text: 'To store persistent session data' },
    ],
    correctOptionId: 'b',
    timeLimitSeconds: 30,
    explanation: 'Message queues enable async, decoupled communication: the producer sends without waiting for the consumer to process. This absorbs traffic spikes (the queue buffers bursts), improves resilience (consumer can restart without losing messages), and enables independent scaling of producers and consumers.',
    createdAt: '2024-01-01',
  },
  {
    id: 'sd-009',
    type: 'flashcard',
    topic: 'system-design',
    difficulty: 'intermediate',
    tags: ['CDN', 'static-assets'],
    front: 'What is a CDN and how does it improve performance?',
    back: 'A **Content Delivery Network (CDN)** is a globally distributed network of edge servers that cache and serve static content close to users.\n\n**How it works:**\n1. User requests `https://cdn.example.com/app.js`\n2. DNS resolves to the nearest edge server\n3. Edge serves cached file (cache hit) or fetches from origin and caches it\n\n**Benefits:**\n- Reduced latency — data travels shorter distance\n- Reduced origin load — edge absorbs most traffic\n- DDoS protection — distributed infrastructure\n- High availability — multiple PoPs\n\n**Best for:** static assets (JS, CSS, images), video streaming, API responses with long TTLs.',
    explanation: 'For any large-scale web app, a CDN is the first performance optimization to mention in a system design interview.',
    createdAt: '2024-01-01',
  },
  {
    id: 'sd-010',
    type: 'multiple-choice',
    topic: 'system-design',
    difficulty: 'advanced',
    tags: ['rate-limiting', 'API'],
    prompt: 'Which rate-limiting algorithm uses a "token bucket" that refills at a constant rate and allows short bursts above the average rate?',
    options: [
      { id: 'a', text: 'Fixed window counter' },
      { id: 'b', text: 'Sliding window log' },
      { id: 'c', text: 'Token bucket' },
      { id: 'd', text: 'Leaky bucket' },
    ],
    correctOptionId: 'c',
    timeLimitSeconds: 30,
    explanation: 'The token bucket algorithm allows requests as long as tokens are available, with tokens refilling at a constant rate. It permits bursts up to the bucket capacity. Leaky bucket smooths bursts into a constant output rate. Fixed window counts per time window (can allow 2× rate at window boundaries). Sliding window is more accurate but memory-intensive.',
    createdAt: '2024-01-01',
  },
  {
    id: 'sd-011',
    type: 'flashcard',
    topic: 'system-design',
    difficulty: 'beginner',
    tags: ['DNS', 'HTTP'],
    front: 'What happens when you type `https://example.com` in a browser and press Enter?',
    back: '1. **DNS lookup**: Browser resolves `example.com` to an IP address (checks local cache, OS cache, then DNS resolver)\n2. **TCP connection**: 3-way handshake with the server IP on port 443\n3. **TLS handshake**: Negotiate cipher, exchange certificates, establish encrypted session\n4. **HTTP request**: `GET / HTTP/1.1` with headers sent over the encrypted connection\n5. **Server processes**: Routes request, queries DB if needed, builds response\n6. **HTTP response**: HTML document returned with status 200\n7. **Browser renders**: Parse HTML → build DOM → fetch CSS/JS/images → layout → paint\n8. **JS executes**: Hydrates the page, sets up event listeners',
    explanation: 'This question appears in almost every senior fullstack interview. Know each step and be ready to dive deeper into any layer.',
    createdAt: '2024-01-01',
  },
  {
    id: 'sd-012',
    type: 'flashcard',
    topic: 'system-design',
    difficulty: 'advanced',
    tags: ['microservices', 'monolith'],
    front: 'What are the main trade-offs between a monolith and microservices?',
    back: '**Monolith:**\n✅ Simple to develop, test, deploy early on\n✅ Low latency (in-process calls)\n✅ Easier transactions and consistency\n❌ Hard to scale individual components\n❌ Long build/deploy cycles at scale\n❌ Technology lock-in\n\n**Microservices:**\n✅ Independent deployment and scaling\n✅ Technology diversity per service\n✅ Fault isolation\n❌ Network overhead and latency\n❌ Distributed systems complexity (partial failures, consistency)\n❌ Operational overhead (service mesh, distributed tracing)\n\n**Rule of thumb:** Start with a monolith. Extract services when a clear boundary emerges and the monolith becomes a bottleneck.',
    explanation: 'The answer is always "it depends." Show you understand the real costs of microservices, not just the benefits.',
    createdAt: '2024-01-01',
  },
  // ── Flashcards ────────────────────────────────────────────────
  {
    id: "sd-fc-001",
    type: "flashcard",
    topic: "system-design",
    difficulty: "beginner",
    tags: ["scaling", "alex-xu-ch1", "architecture"],
    front: "Walk through the scaling ladder from a single server to millions of users.",
    back: "1. **Single server** — web, DB, cache on one box.\n2. **Separate the database** — web tier and data tier scale independently.\n3. **Load balancer** — public VIP in front of private-IP servers; removes the web-tier SPOF.\n4. **Database replication** — primary takes writes, replicas take reads.\n5. **Cache** — read-through between web tier and DB.\n6. **CDN** — static assets served from the edge.\n7. **Stateless web tier** — session state moves to a shared store, enabling autoscaling.\n8. **Multiple data centres** — geoDNS routes users to the nearest.\n9. **Message queue** — decouple producers and consumers.\n10. **Database sharding** — the last resort, once vertical scaling is exhausted.\n11. **Logging, metrics, automation** throughout.",
    explanation: "This is Chapter 1 of Alex Xu, and it's the skeleton of most system design answers. The valuable part is the *ordering*: each step addresses the bottleneck the previous step created. Jumping straight to sharding is the classic over-engineering red flag.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-002",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["sharding", "alex-xu-ch1", "databases"],
    front: "What problems does database sharding introduce?",
    back: "Sharding splits data across databases by a **shard key** (`hash(user_id) % N`). Three problems follow:\n\n1. **Resharding** — when a shard fills up or data is unevenly distributed, you must move data and update the mapping. Consistent hashing reduces the pain.\n2. **Celebrity / hotspot problem** — if you shard by user and one user has 100M followers, that shard is overwhelmed. May need a dedicated shard, or a different key.\n3. **Joins and denormalization** — cross-shard joins are impractical, so you denormalize and accept the write amplification and consistency cost.\n\nAlso: cross-shard transactions, unique-ID generation, and the fact that every query without the shard key becomes a scatter-gather.",
    explanation: "'Which shard key?' is the follow-up that decides the design. The right answer is 'the one that appears in the majority of my queries and distributes evenly' — and if no such key exists, sharding may be the wrong solution.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-003",
    type: "flashcard",
    topic: "system-design",
    difficulty: "intermediate",
    tags: ["nosql", "alex-xu-ch1", "databases"],
    front: "When would you choose NoSQL over a relational database?",
    back: "Alex Xu's four criteria — choose non-relational when:\n1. Your application requires **super-low latency**.\n2. Your data is **unstructured** or has no relational structure.\n3. You only need to **serialize and deserialize** data (JSON, XML, YAML).\n4. You need to store a **massive amount** of data.\n\nTypes: key-value (Redis, DynamoDB), column (Cassandra, HBase), document (MongoDB), graph (Neo4j).\n\nThe counterweight: relational databases have had 40 years of investment in query optimisation, transactions, and tooling. Start relational unless one of the four criteria clearly applies.",
    explanation: "Interviewers watch for reflexive 'NoSQL because scale'. Postgres handles very large workloads, and choosing NoSQL costs you joins, transactions, and ad-hoc querying — say what you're trading away, not just what you're gaining.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-004",
    type: "flashcard",
    topic: "system-design",
    difficulty: "intermediate",
    tags: ["caching", "alex-xu-ch1"],
    front: "What should you consider when adding a cache?",
    back: "From Chapter 1:\n- **When to use** — data read frequently but modified infrequently. Caches are volatile; never use one as durable storage.\n- **Expiration policy** — mandatory. Too short means constant DB hits; too long means stale data.\n- **Consistency** — keeping the store and cache in sync is hard, especially across regions. Scaling data across regions makes it harder.\n- **Mitigating failures** — a single cache server is a **SPOF**; use multiple nodes across data centres and **over-provision** memory, because filling the cache causes evictions.\n- **Eviction policy** — LRU (most common), LFU, FIFO.\n\nStrategies: cache-aside (most common), read-through, write-through, write-behind, refresh-ahead.",
    explanation: "The over-provisioning tip is subtle and worth quoting: when a cache reaches capacity, adding items evicts existing ones, so a cache running at exactly 100% thrashes. Provision above the expected working set.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-005",
    type: "flashcard",
    topic: "system-design",
    difficulty: "intermediate",
    tags: ["stateless", "alex-xu-ch1", "scaling"],
    front: "Why is a stateless web tier important, and how do you get there?",
    back: "**Stateful** — a user's session lives on one specific server, so requests must be routed back to it (sticky sessions). That makes adding and removing servers hard, and losing a server logs its users out.\n\n**Stateless** — session data lives in a shared store (Redis, a relational DB, NoSQL). Any server can handle any request.\n\nConsequences of going stateless:\n- **Autoscaling** actually works — spin servers up and down freely.\n- Simpler, more robust, and enables multi-data-centre.\n- Cost: an extra network hop per request, and the shared store becomes something you must scale and make highly available.\n\nThe web tier being stateless is the precondition for almost everything else in the ladder.",
    explanation: "Sticky sessions at the load balancer are the tell-tale sign of a stateful tier. They work, but they defeat autoscaling and make deployments disruptive — a good thing to point out when critiquing an existing architecture.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-006",
    type: "flashcard",
    topic: "system-design",
    difficulty: "intermediate",
    tags: ["estimation", "alex-xu-ch2", "latency"],
    front: "Back-of-the-envelope: what latency numbers should you know?",
    back: "Jeff Dean's numbers, rounded:\n\n| Operation | Time |\n|---|---|\n| L1 cache reference | 0.5 ns |\n| Branch mispredict | 5 ns |\n| L2 cache reference | 7 ns |\n| Mutex lock/unlock | 100 ns |\n| Main memory reference | 100 ns |\n| Compress 1 KB with Zippy | 10 µs |\n| Send 2 KB over 1 Gbps | 20 µs |\n| Read 1 MB sequentially from memory | 250 µs |\n| Round trip within the same data centre | 500 µs |\n| Disk seek | 10 ms |\n| Read 1 MB from network | 10 ms |\n| Read 1 MB from disk | 30 ms |\n| Packet CA → Netherlands → CA | 150 ms |\n\nConclusions: memory is fast, disk is slow, avoid disk seeks, compression is cheap, compress before sending over the network, and cross-region traffic is expensive.",
    explanation: "You don't need the exact figures — you need the *ratios*. Memory is ~100× faster than disk; a same-DC round trip is ~300× faster than a cross-Atlantic one. That's what lets you reason about a design without a calculator.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-007",
    type: "flashcard",
    topic: "system-design",
    difficulty: "intermediate",
    tags: ["availability", "alex-xu-ch2", "sla"],
    front: "What do the availability 'nines' mean in downtime?",
    back: "| Availability | Downtime per year | Per day |\n|---|---|---|\n| 99% | 3.65 days | 14.4 min |\n| 99.9% ('three nines') | 8.77 hours | 1.44 min |\n| 99.99% | 52.6 minutes | 8.6 s |\n| 99.999% ('five nines') | 5.26 minutes | 864 ms |\n| 99.9999% | 31.56 seconds | 86.4 ms |\n\nCloud providers (AWS, GCP, Azure) set their SLAs at **99.9% or above**.\n\nThe engineering point: each additional nine costs roughly an order of magnitude more. Five nines means no manual intervention is possible during an incident — every failure must be handled automatically.",
    explanation: "The follow-up is usually 'what availability does your system need?' The mature answer names the business cost of downtime and works backwards, rather than reflexively saying five nines. Also: composed services multiply, so a chain of four 99.9% dependencies is already below 99.6%.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-008",
    type: "flashcard",
    topic: "system-design",
    difficulty: "intermediate",
    tags: ["estimation", "alex-xu-ch2", "qps"],
    front: "Estimate the QPS and storage for a Twitter-like service.",
    back: "Assumptions: 300M MAU, 50% use it daily, 2 tweets/user/day, 10% contain media, stored 5 years.\n\n**QPS**\n```\nDAU        = 300M × 50%                       = 150M\nTweets QPS = 150M × 2 / 24 / 3600             ≈ 3,500\nPeak QPS   = 2 × QPS                          ≈ 7,000\n```\n\n**Storage** (media only)\n```\ntweet_id 64 B, text 140 B, media 1 MB\nPer day  = 150M × 2 × 10% × 1 MB              = 30 TB/day\n5 years  = 30 TB × 365 × 5                    ≈ 55 PB\n```\n\nMethod: state assumptions, round aggressively, label units, and compute peak as a multiple of average.",
    explanation: "Interviewers assess the *process*, not the number. Say your assumptions out loud, use round numbers (100,000 / 10, not 99,987 / 9.1), and always label units — '5' means nothing, '5 MB' means something.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-009",
    type: "flashcard",
    topic: "system-design",
    difficulty: "beginner",
    tags: ["interview-framework", "alex-xu-ch3"],
    front: "What is the four-step framework for a system design interview?",
    back: "**1. Understand the problem and establish scope (3–10 min).** Don't jump to a solution. Ask: which features exactly? how many users? expected scale in 3/6/12 months? what's the existing tech stack? Write assumptions down.\n\n**2. Propose a high-level design and get buy-in (10–15 min).** Draw the boxes: clients, API gateway, services, data stores, cache, CDN, queue. Do back-of-the-envelope maths on the blueprint. Treat the interviewer as a collaborator.\n\n**3. Design deep dive (10–25 min).** The interviewer steers — go deep where they point, usually the bottleneck or the interesting trade-off. Don't sink into unnecessary detail.\n\n**4. Wrap up (3–5 min).** Identify bottlenecks, recap, discuss error cases (server failure, network loss), operations (metrics, rollout), and the next scale-up step.",
    explanation: "Red flags Alex Xu names explicitly: over-engineering, valuing design purity over trade-offs, narrow-mindedness, and stubbornness. The single most-cited mistake is answering fast without clarifying — the interview is not a trivia contest.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-010",
    type: "flashcard",
    topic: "system-design",
    difficulty: "intermediate",
    tags: ["rate-limiting", "alex-xu-ch4"],
    front: "Compare the five rate-limiting algorithms.",
    back: "| Algorithm | Mechanism | Pros | Cons |\n|---|---|---|---|\n| **Token bucket** | Bucket of N tokens, refilled at a fixed rate | Simple, memory-efficient, allows bursts | Two params hard to tune |\n| **Leaking bucket** | FIFO queue drained at a fixed rate | Fixed memory, stable outflow | Bursts fill the queue with stale requests |\n| **Fixed window counter** | Counter per time window | Memory-efficient, matches quota resets | Edge bursts allow up to 2× the limit |\n| **Sliding window log** | Sorted set of request timestamps | Very accurate | Stores timestamps even for rejected requests |\n| **Sliding window counter** | Blend previous and current windows by overlap | Smooths bursts, memory-efficient | Approximate |\n\nToken bucket is the most common default (used by Stripe, AWS); sliding window counter when you need burst smoothing cheaply.",
    explanation: "The fixed-window edge case is the one to be able to explain concretely: a 5-req/min limit lets 5 requests land at 00:59 and 5 more at 01:01 — 10 requests inside two seconds. That's why sliding variants exist.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-011",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["rate-limiting", "redis", "alex-xu-ch4"],
    front: "How do you build a distributed rate limiter?",
    back: "Store counters in **Redis** — in-memory speed plus `INCR` and `EXPIRE`, which is exactly the primitive set you need. A database would be too slow.\n\nTwo distributed problems:\n1. **Race condition** — read counter, compare, write is not atomic, so concurrent requests both see the old value and both pass. Locks solve it but are slow. Use a **Lua script** (atomic on the Redis server) or a **sorted set** for the sliding-window log.\n2. **Synchronization** — with multiple rate-limiter instances, each having its own state doesn't work. Sticky sessions are not scalable or flexible. Use **centralised Redis**.\n\nRules live in config files on disk, loaded into memory by workers.",
    explanation: "The Lua-script-instead-of-lock detail is the differentiator. It's the standard answer for atomic multi-step Redis operations and generalises well beyond rate limiting.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-012",
    type: "flashcard",
    topic: "system-design",
    difficulty: "intermediate",
    tags: ["rate-limiting", "http", "alex-xu-ch4"],
    front: "What should a rate-limited HTTP response look like?",
    back: "Status **429 Too Many Requests**, with headers so clients can behave well:\n\n```\nHTTP/1.1 429 Too Many Requests\nX-Ratelimit-Limit: 100          # requests allowed per window\nX-Ratelimit-Remaining: 0        # requests left in the current window\nX-Ratelimit-Retry-After: 30     # seconds until you may retry\n```\n\nDesign decisions to raise:\n- **Drop vs queue** — silently dropping is simpler; queueing for later processing is possible for async work.\n- **Hard vs soft limits** — a hard limit cannot be exceeded; a soft limit allows brief overage.\n- **Where** — client-side (unreliable, easily bypassed), server-side, or in an **API gateway / middleware**.\n- **Which layer** — Iptables at L3 for IP-level, application middleware for user-level.",
    explanation: "Client best practices are part of a complete answer: cache responses to avoid unnecessary calls, understand the limit and don't send too many requests, catch 429s gracefully, and add enough back-off with jitter.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-013",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["consistent-hashing", "alex-xu-ch5"],
    front: "What problem does consistent hashing solve, and how?",
    back: "**Problem:** `serverIndex = hash(key) % N`. Add or remove a server and N changes, so almost *every* key remaps — a storm of cache misses that can take down the origin.\n\n**Solution:** map servers **and** keys onto a hash ring (e.g. the SHA-1 space). A key belongs to the first server found going clockwise. Adding or removing a server only remaps `k/n` keys on average.\n\n**Two remaining problems**, both solved by **virtual nodes**: partitions between adjacent servers aren't uniform, and key distribution isn't uniform. Each physical server gets many virtual nodes on the ring, which reduces the standard deviation of load — at the cost of more metadata.\n\nUsed by: Amazon Dynamo, Cassandra, Akamai, Discord, Maglev.",
    explanation: "The number of virtual nodes is a real trade-off worth naming: more virtual nodes means more even distribution but more memory for the ring metadata. It's tunable, and it's the answer to 'how do you handle heterogeneous server capacity?' — give bigger servers more virtual nodes.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-014",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["cap-theorem", "alex-xu-ch6", "consistency"],
    front: "Explain the CAP theorem and how it drives key-value store design.",
    back: "**Consistency, Availability, Partition tolerance — pick two.** Since network partitions are unavoidable in a distributed system, the real choice is between CP and AP when a partition occurs.\n\n- **CP** — refuse writes during a partition to avoid inconsistency. Banking, inventory.\n- **AP** — keep accepting writes on both sides and reconcile later. Dynamo, Cassandra, shopping carts.\n\nIn a three-replica store with a partition isolating one node: a CP system blocks writes; an AP system accepts them and resolves conflicts when the partition heals — typically with **vector clocks** handing resolution to the client.\n\nCA is not a real option in a distributed system.",
    explanation: "The nuance interviewers appreciate: CAP describes behaviour *during a partition*, not steady state. PACELC extends it — else (when there's no partition) you still trade Latency against Consistency, which is what tunable quorums like `W + R > N` are really controlling.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-015",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["quorum", "consistency", "alex-xu-ch6"],
    front: "How do quorum reads and writes work?",
    back: "With `N` replicas, `W` write quorum, `R` read quorum:\n\n- A write succeeds when **W** replicas acknowledge it.\n- A read is served after collecting responses from **R** replicas.\n- **`W + R > N` guarantees strong consistency** — the read and write sets must overlap on at least one node holding the latest value.\n\nTuning:\n| Config | Effect |\n|---|---|\n| `R = 1, W = N` | Fast reads, slow writes |\n| `W = 1, R = N` | Fast writes, slow reads |\n| `N = 3, W = R = 2` | Balanced — the common default |\n| `W + R ≤ N` | Strong consistency not guaranteed |\n\nA **coordinator** node acts as proxy between client and replicas.",
    explanation: "Be ready to demonstrate the overlap argument: with N=3, W=2, R=2, any write set and any read set share at least one node by pigeonhole, so the read always sees the latest write. That's the whole intuition behind quorum systems.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-016",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["failure-detection", "gossip", "merkle-tree", "alex-xu-ch6"],
    front: "How does a distributed key-value store detect and handle failures?",
    back: "**Detection — gossip protocol**, not all-to-all multicasting (which is O(n²)). Each node keeps a membership list with heartbeat counters, periodically sends its list to random nodes, and marks a node offline if its heartbeat hasn't increased for a threshold period. Multiple independent nodes confirming makes it robust.\n\n**Temporary failures — sloppy quorum + hinted handoff.** Rather than enforcing strict quorum, pick the first W healthy nodes on the ring. Another server temporarily accepts writes for the down node and hands them back when it recovers.\n\n**Permanent failures — anti-entropy with Merkle trees.** Compare tree hashes to find exactly which key ranges diverge, so you transfer only the differences instead of the whole dataset.\n\n**Data centre outage** — replicate across data centres.",
    explanation: "Merkle trees are the detail that shows depth: comparing root hashes tells you instantly whether two replicas match, and you only descend into subtrees that differ — so synchronising two nearly-identical 1 TB replicas transfers megabytes.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-017",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["lsm-tree", "sstable", "bloom-filter", "alex-xu-ch6"],
    front: "Describe the read and write paths of an LSM-tree-based key-value store.",
    back: "**Write path**\n1. Append to a **commit log** on disk (durability).\n2. Insert into the in-memory **memtable**.\n3. When the memtable is full, **flush** it to a new **SSTable** (sorted string table) on disk.\n\n**Read path**\n1. Check the **memory cache**. If present, return.\n2. If not, consult a **bloom filter** to determine which SSTables might contain the key.\n3. Read from those SSTables and return.\n\nThe bloom filter is the key optimisation: it can say 'definitely not here' with certainty and 'maybe here' with a tunable false-positive rate, so you avoid reading most SSTables entirely.",
    explanation: "This is the Cassandra/RocksDB/LevelDB design. The trade-off to name: LSM trees give excellent write throughput (sequential appends only) at the cost of read amplification and background compaction — the opposite balance to a B-tree.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-018",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["unique-id", "snowflake", "alex-xu-ch7"],
    front: "How does Twitter Snowflake generate unique IDs?",
    back: "A 64-bit ID, divided into sections:\n\n| Section | Bits | Meaning |\n|---|---|---|\n| Sign bit | 1 | Always 0, reserved |\n| **Timestamp** | 41 | Milliseconds since a custom epoch |\n| Datacenter ID | 5 | 32 data centres |\n| Machine ID | 5 | 32 machines per DC |\n| **Sequence** | 12 | Per-millisecond counter → 4,096 IDs/ms/machine |\n\nProperties: **64-bit numeric**, **sortable by time** (the timestamp occupies the high bits), and generated **without coordination** — each machine works independently.\n\n41 bits of milliseconds ≈ **69 years** from the custom epoch.\n\nDatacenter and machine IDs are fixed at startup; changing them at runtime risks collisions.",
    explanation: "Compare with the alternatives to show why: multi-master `auto_increment` doesn't scale across DCs, UUIDs are 128-bit and not time-sortable, and a ticket server is a SPOF. Time-sortability is the property that makes Snowflake IDs usable as primary keys and cursors.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-019",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["unique-id", "alex-xu-ch7", "clock-skew"],
    front: "What issues do you discuss when designing an ID generator?",
    back: "- **Clock synchronization** — Snowflake assumes machines have the same clock. NTP is the standard answer; a backwards clock jump can cause duplicate IDs, so implementations either wait it out or refuse to generate.\n- **Section length tuning** — fewer sequence bits and more timestamp bits gives a longer lifespan; more sequence bits gives higher per-millisecond throughput. Tune to your workload.\n- **High availability** — the ID generator is on the critical path for every write, so it must be highly available.\n- **Sortability** — is time-ordering a requirement? It enables cursor pagination and time-range scans on the primary key.\n- **Predictability** — sequential IDs leak business volume and enable enumeration attacks. Use an opaque public ID if that matters.",
    explanation: "The clock-skew discussion is where this question usually goes. Also worth raising: exposing a monotonically increasing ID in a URL tells competitors how many orders you process per day — a real reason to separate internal and external identifiers.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-020",
    type: "flashcard",
    topic: "system-design",
    difficulty: "intermediate",
    tags: ["url-shortener", "alex-xu-ch8"],
    front: "How do you design a URL shortener?",
    back: "**API**\n```\nPOST /api/v1/data/shorten  { longUrl }  →  shortUrl\nGET  /api/v1/{shortUrl}                 →  301 or 302 redirect\n```\n\n**301 vs 302** — 301 (permanent) is cached by the browser, reducing server load; **302** (temporary) sends every click to your server, which you want for **analytics**. Choose based on which you value.\n\n**Hashing** — two approaches:\n1. **Hash + collision resolution** — hash the long URL (CRC32/MD5/SHA-1), take the first *n* characters, on collision append a fixed string and rehash. Needs a DB lookup per attempt; a **bloom filter** makes that cheap.\n2. **Base-62 conversion** — convert a unique ID to base 62 (`0-9a-zA-Z`). No collisions by construction, but IDs are guessable and length grows over time.\n\n62^7 ≈ 3.5 trillion — 7 characters is plenty.",
    explanation: "The 301-vs-302 trade-off is the question inside the question, and analytics usually wins in a commercial product. The deep dive then goes to caching hot URLs, the rate limiter, and sharding the mapping table.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-021",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["web-crawler", "url-frontier", "alex-xu-ch9"],
    front: "What is the URL Frontier and what two problems does it solve?",
    back: "The URL Frontier is the component that stores URLs to be downloaded and decides the order.\n\n**Politeness** — a naive BFS floods a single host with parallel requests, which looks like a DoS. Solution: a mapping table from hostname to a dedicated FIFO queue, **one worker thread per queue**, with a delay between downloads to the same host.\n\n**Priority** — not all pages are equal. A prioritizer assigns priority (PageRank, traffic, update frequency); **front queues** handle priority and **back queues** handle politeness, chained together.\n\n**Freshness** — recrawl based on update history, prioritising important pages.\n\nStorage is **hybrid**: the majority on disk (there are too many URLs for memory) with in-memory buffers for enqueue and dequeue.",
    explanation: "The politeness requirement is what makes a crawler hard, and it's the piece candidates skip. It also constrains throughput: you scale by crawling many hosts concurrently, not by hammering one — which shapes the whole queue design.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-022",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["web-crawler", "alex-xu-ch9", "performance"],
    front: "What problems must a web crawler handle?",
    back: "**Robustness** — bad HTML, unresponsive servers, crashes, malicious links. Use consistent hashing to distribute load, save crawl state so you can resume, handle exceptions, and validate data.\n\n**Traps**\n- **Redundant content** — ~30% of the web is duplicated. Compare hashes/checksums rather than full content.\n- **Spider traps** — infinite URL depth (`/a/b/a/b/...`). Set a maximum URL length; some traps require manual exclusion.\n- **Data noise** — ads, spam, code snippets, boilerplate. Filter them out.\n\n**Performance**\n- **DNS is a bottleneck** — resolution is synchronous and can take tens of milliseconds. Cache DNS results.\n- **Locality** — put crawl servers geographically near the sites they crawl.\n- **Short timeouts** — don't let one slow host stall a worker.\n- **Respect `robots.txt`**, cached to avoid re-fetching it.",
    explanation: "The DNS point is the non-obvious performance answer and it's specifically called out in the book. It's also a good general lesson: the bottleneck in a distributed crawler is rarely the bandwidth you were thinking about.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-023",
    type: "flashcard",
    topic: "system-design",
    difficulty: "intermediate",
    tags: ["notifications", "alex-xu-ch10", "queues"],
    front: "How do you design a notification system?",
    back: "**Channels** — mobile push (APNs for iOS, FCM for Android), SMS (Twilio, Nexmo), email (SendGrid, Mailchimp). Gather device tokens, phone numbers, and emails at signup; a user has many devices, so `user` and `device` tables are separate.\n\n**Flow**\n```\nservices → notification servers → cache + DB\n         → message queues (one per channel, as buffers)\n         → workers → third-party services → devices\n```\n\nNotification servers provide the API, validate input, build payloads from cache/DB, and rate-limit.\n\n**Reliability** — persist to a **notification log** with retry so nothing is lost. **Exactly-once is not guaranteed** (APNs/FCM are at-least-once), so **dedupe on an event ID** at the client or before sending.\n\n**Extras** — templates, per-channel user settings, rate limiting, `appKey`/`appSecret` auth, and event tracking (open rate, click rate).",
    explanation: "The at-least-once point is the reliability crux. Third-party push services can deliver duplicates, so idempotency via an event ID is required — otherwise users get the same alert three times, which is the most visible possible bug.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-024",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["news-feed", "fanout", "alex-xu-ch11"],
    front: "Compare fanout-on-write and fanout-on-read for a news feed.",
    back: "**Fanout on write (push)** — when a user posts, push the post ID into every friend's news-feed cache.\n- Feed is precomputed, so reads are very fast and real-time.\n- Bad for users with many friends (**hotkey problem** — one celebrity post fans out to millions).\n- Wastes work on inactive friends who never read their feed.\n\n**Fanout on read (pull)** — build the feed at read time by querying friends' posts.\n- No wasted work for inactive users, no hotkey problem.\n- Reads are slow.\n\n**Hybrid** — push for the majority of users, pull for celebrities. This is what production systems actually do.\n\nThe feed-building read path then hydrates post IDs from a **content cache**, users from a **user cache**, plus social-graph and counter caches.",
    explanation: "The hybrid answer is the expected one, and the reasoning generalises: when a distribution has a heavy tail, handle the head and the body with different strategies rather than forcing one mechanism to cover both.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-025",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["chat-system", "websocket", "alex-xu-ch12"],
    front: "Why does a chat system use WebSocket, and which services are stateful?",
    back: "**Transport evolution:** polling (wasteful) → long polling (better, but servers can't tell if a client disconnected) → **WebSocket** — bidirectional, persistent, and works over ports 80/443 so firewalls allow it.\n\nUse WebSocket only for the **send and receive** path; auth, profile, service discovery, and group management stay ordinary HTTP.\n\n**Three service families:**\n- **Stateless** — auth, user profile, group management, service discovery. Behind a load balancer, scale freely.\n- **Stateful — the chat service.** Each client holds a persistent connection to one specific chat server, so it cannot be freely rerouted. Service discovery tells the client which chat server to connect to.\n- **Third-party** — push notification for offline users.\n\nPresence uses **heartbeats** every *x* seconds; missing them beyond a threshold marks the user offline.",
    explanation: "Identifying the chat service as the *only* stateful piece is the key architectural insight — you isolate statefulness to one tier and keep everything else horizontally scalable, which is a pattern that transfers to any real-time system.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-026",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["chat-system", "key-value-store", "alex-xu-ch12"],
    front: "Why store chat history in a key-value store rather than a relational database?",
    back: "Alex Xu's four reasons:\n1. The data is **enormous** — chat volume dwarfs most other data.\n2. **Only recent messages are accessed** frequently; the long tail is rarely read.\n3. Most of it is **never read again**, so index maintenance is wasted work.\n4. KV stores scale **horizontally** with low latency and predictable performance.\n\nReal-world: Facebook Messenger uses HBase, Discord uses Cassandra.\n\n**Schema**\n- 1-on-1: primary key `message_id`.\n- Group chat: composite key `(channel_id, message_id)`, with **`channel_id` as the partition key** so a channel's messages live together.\n\n`message_id` must be unique **and sortable by time** — use Snowflake, or a per-channel local sequence.",
    explanation: "The 'sortable by time' requirement is what links this back to the ID-generation chapter, and it's the reason you can't just use a UUID. Message ordering within a channel *is* the product.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-027",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["autocomplete", "trie", "alex-xu-ch13"],
    front: "How does a trie-based search autocomplete work at scale?",
    back: "A **trie** stores prefixes; each node caches the **top k** most-queried strings for its prefix.\n\nWithout that optimisation: find the prefix node `O(p)`, traverse the whole subtree `O(c)`, sort `O(c log c)` — far too slow.\nWith top-k cached at each node: the query is effectively **O(1)** after the prefix walk.\n\nSecond optimisation: **limit the maximum prefix length** — nobody types a 100-character prefix — so even the prefix walk is bounded.\n\n**Two services:**\n- **Data gathering** — analytics logs → aggregators → weekly workers → trie DB.\n- **Query** — read from a trie cache (Redis) with a DB fallback.\n\nScaling: the trie is too large for one server, so **shard by prefix**, with a shard-map manager compensating for the uneven distribution of first letters ('s' has far more words than 'x').",
    explanation: "The 24,000 QPS / 48,000 peak estimate comes from 10M DAU × 10 searches × 4 requests per word — note the ×4, because autocomplete fires on *every keystroke*. That multiplier is the whole reason the design has to be this aggressive about caching.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-028",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["video-streaming", "youtube", "cdn", "alex-xu-ch14"],
    front: "How do you design YouTube's upload and streaming flows?",
    back: "**Upload**\n```\nclient → LB → API servers → metadata DB (sharded) + cache\n                          → original storage (BLOB)\n                          → transcoding servers → transcoded storage\n                          → CDN + completion queue → completion handler\n```\nMetadata update runs in parallel with the file upload.\n\n**Streaming** — videos are served **from CDN edge servers**, not your origin. Streaming protocols: MPEG-DASH, Apple HLS, Microsoft Smooth Streaming, Adobe HDS.\n\n**Why transcode:** raw video is enormous; different devices and bandwidths need different formats; adaptive bitrate requires multiple renditions. Encoding = container (`.mp4`, `.mov`) + codec (H.264, VP9, HEVC).\n\n**Transcoding architecture** — a **DAG model** (preprocessor → DAG scheduler → resource manager → task workers), splitting video into **GOPs** so encoding parallelises.",
    explanation: "The cost framing is worth quoting: at 5M DAU the CDN bill dominates everything. That's what motivates the optimisations — serve only popular videos from CDN, don't pre-encode cold content, and consider your own CDN for the head of the distribution.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-029",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["video-streaming", "cost-optimization", "alex-xu-ch14"],
    front: "How do you optimise a video platform for speed, safety, and cost?",
    back: "**Speed**\n- Split uploads into **GOPs** and upload them in parallel.\n- Place upload centres close to users.\n- Parallelism everywhere in the pipeline, with message queues decoupling stages.\n\n**Safety**\n- **Pre-signed upload URLs** so clients upload directly to storage with scoped permission.\n- **DRM** — Apple FairPlay, Google Widevine, Microsoft PlayReady.\n- **Watermarking** for traceability.\n\n**Cost** — CDN egress is the dominant line item:\n- Serve only **popular** videos from the CDN; serve the long tail from high-capacity origin servers.\n- Don't pre-encode cold content — transcode on demand.\n- Distribute by **regional popularity** rather than globally.\n- Build your own CDN for the most popular content.\n\n**Error handling** — retry recoverable errors a few times; return a proper error code for non-recoverable ones.",
    explanation: "The long-tail insight is the transferable one: the popularity distribution of user-generated content is extremely skewed, so an optimisation that treats the head and tail differently almost always pays. It applies to caching, storage tiering, and precomputation alike.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-030",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["google-drive", "delta-sync", "alex-xu-ch15"],
    front: "How does Google Drive's block-based sync work?",
    back: "Files are split into **blocks** (Dropbox caps them at 4 MB). Then:\n\n- **Delta sync** — when a file changes, only the modified blocks are uploaded, not the whole file.\n- **Compression** — each block is compressed with an algorithm chosen for the file type (gzip/bzip2 for text; different for images and video).\n- **De-duplication** — identical blocks (by hash) are stored once across all users.\n\n**Block servers** handle splitting, compressing, encrypting, and uploading to cloud storage.\n\n**Upload flow:** add file metadata (status *pending*) → upload blocks to cloud storage → callback → set metadata to *uploaded* → notify other clients.\n\n**Consistency must be strong** here — metadata cache and DB must agree, so invalidate on write and use a relational DB with ACID properties.",
    explanation: "The strong-consistency requirement is worth defending: a file sync product where two devices disagree about the current version is broken in a way users immediately notice. This is a case where eventual consistency is the wrong choice.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-fc-031",
    type: "flashcard",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["google-drive", "long-polling", "alex-xu-ch15"],
    front: "Long polling or WebSocket for a file-sync notification service?",
    back: "Alex Xu chooses **long polling** for Google Drive, and the reasoning is what matters:\n\n- Communication is **not bidirectional** — the server tells the client a file changed; the client doesn't stream data back over the same channel.\n- Notifications are **not frequent**, and often arrive in bursts rather than continuously.\n\nWebSocket is designed for real-time bidirectional chat-like traffic, and its persistent connections cost server resources you don't need here.\n\nContrast with the chat system (Ch. 12), where WebSocket **is** the right answer because messages flow constantly in both directions.\n\nEach client keeps a long-polling connection; on a change, the server responds, the client fetches the new metadata, downloads changed blocks, and reconnects.",
    explanation: "This pairing is a great interview answer because it shows you choose transport from requirements rather than by default. 'WebSocket for everything real-time' is the shallow answer; naming the bidirectionality and frequency criteria is the deep one.",
    createdAt: "2026-08-26",
  },
  // ── Multiple choice ───────────────────────────────────────────
  {
    id: "sd-mc-001",
    type: "multiple-choice",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["quorum", "consistency", "alex-xu-ch6"],
    prompt: "With N=3 replicas, which quorum configuration guarantees strong consistency?",
    options: [
      { id: "a", text: "W=2, R=1" },
      { id: "b", text: "W=2, R=2" },
      { id: "c", text: "W=1, R=1" },
      { id: "d", text: "W=1, R=2" },
    ],
    correctOptionId: "b",
    timeLimitSeconds: 50,
    explanation: "Strong consistency requires W + R > N. Only 2 + 2 = 4 > 3 satisfies it; the others give 2, 3, and 3. The intuition is overlap: with W=2 and R=2 out of 3 nodes, the write set and read set must share at least one node, which therefore holds the latest value.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-mc-002",
    type: "multiple-choice",
    topic: "system-design",
    difficulty: "intermediate",
    tags: ["rate-limiting", "alex-xu-ch4"],
    prompt: "A fixed-window rate limiter allows 5 requests per minute. How many can an attacker send in a 2-second span?",
    options: [
      { id: "a", text: "Unlimited, because fixed windows do not track bursts" },
      { id: "b", text: "1, because the window resets on each request" },
      { id: "c", text: "10 — five at the end of one window and five at the start of the next" },
      { id: "d", text: "5 — the limit is enforced strictly" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 50,
    explanation: "The window boundary is the flaw: five requests at 00:59 and five at 01:01 are each within their own window's limit, yet ten arrive in two seconds. Sliding window log or sliding window counter fix this by considering a moving time range instead of a fixed one.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-mc-003",
    type: "multiple-choice",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["consistent-hashing", "alex-xu-ch5"],
    prompt: "Why does consistent hashing use virtual nodes?",
    options: [
      { id: "a", text: "To speed up the hash function" },
      { id: "b", text: "To even out both partition sizes and key distribution across physical servers" },
      { id: "c", text: "To increase the total number of servers the ring can support" },
      { id: "d", text: "To allow keys to be stored on multiple servers for redundancy" },
    ],
    correctOptionId: "b",
    timeLimitSeconds: 50,
    explanation: "With one point per server, ring positions are random, so partition sizes and therefore load are uneven. Many virtual nodes per server averages this out — more virtual nodes means a lower standard deviation of load, at the cost of more ring metadata. Replication is a separate mechanism.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-mc-004",
    type: "multiple-choice",
    topic: "system-design",
    difficulty: "intermediate",
    tags: ["url-shortener", "http", "alex-xu-ch8"],
    prompt: "A URL shortener needs click analytics. Which redirect status code?",
    options: [
      { id: "a", text: "307 — it preserves the HTTP method" },
      { id: "b", text: "200 with a JavaScript redirect for maximum tracking fidelity" },
      { id: "c", text: "302 — every click reaches your server, so you can record it" },
      { id: "d", text: "301 — permanent redirects are cached, reducing server load" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 45,
    explanation: "301 is cached by the browser, so subsequent clicks never touch your server and go uncounted — which is exactly the trade-off: 301 minimises load, 302 enables analytics. A 200-plus-JS redirect adds latency and breaks for clients without JavaScript.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-mc-005",
    type: "multiple-choice",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["unique-id", "snowflake", "alex-xu-ch7"],
    prompt: "Why does a Snowflake ID put the timestamp in the high-order bits?",
    options: [
      { id: "a", text: "To prevent collisions between data centres" },
      { id: "b", text: "So the sign bit stays zero" },
      { id: "c", text: "So IDs sort chronologically as plain integers, enabling time-ordered scans and cursor pagination" },
      { id: "d", text: "Because timestamps require more bits than the other sections" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 50,
    explanation: "Placing the timestamp first means numeric ordering equals chronological ordering, which is why Snowflake IDs work as primary keys and pagination cursors. Collision avoidance comes from the datacenter, machine, and sequence bits, not from the ordering.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-mc-006",
    type: "multiple-choice",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["news-feed", "fanout", "alex-xu-ch11"],
    prompt: "A celebrity with 50 million followers posts. Which news feed strategy handles this best?",
    options: [
      { id: "a", text: "Fanout on write, so followers get it instantly" },
      { id: "b", text: "Fanout on read for all users, to avoid any write amplification" },
      { id: "c", text: "Fanout on write with a larger message queue" },
      { id: "d", text: "Hybrid — push for ordinary users, pull for celebrities" },
    ],
    correctOptionId: "d",
    timeLimitSeconds: 50,
    explanation: "Pure push means 50 million cache writes for one post — the hotkey problem. Pure pull makes every feed read slow for everyone. The hybrid handles the heavy tail separately: most users' posts are pushed, and celebrity posts are merged in at read time.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-mc-007",
    type: "multiple-choice",
    topic: "system-design",
    difficulty: "intermediate",
    tags: ["nosql", "alex-xu-ch1"],
    prompt: "Which is NOT one of Alex Xu's stated reasons to choose a non-relational database?",
    options: [
      { id: "a", text: "You need complex joins across many entities" },
      { id: "b", text: "Your application requires super-low latency" },
      { id: "c", text: "Your data is unstructured or non-relational" },
      { id: "d", text: "You need to store a massive amount of data" },
    ],
    correctOptionId: "a",
    timeLimitSeconds: 45,
    explanation: "Complex joins are a reason to *stay* relational — cross-document and cross-partition joins are exactly what NoSQL stores handle badly, forcing you into denormalization. The other three are the book's criteria, along with only needing to serialize and deserialize data.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-mc-008",
    type: "multiple-choice",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["web-crawler", "dns", "alex-xu-ch9"],
    prompt: "Why is DNS resolution a bottleneck in a web crawler?",
    options: [
      { id: "a", text: "DNS records are too large to cache efficiently" },
      { id: "b", text: "Each crawl requires a fresh DNS lookup by protocol" },
      { id: "c", text: "Resolution is synchronous and can take tens of milliseconds, stalling worker threads on every new host" },
      { id: "d", text: "DNS servers rate-limit crawlers aggressively" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 50,
    explanation: "A crawler touches an enormous number of distinct hosts, and each blocking lookup idles a worker for tens of milliseconds. The fix is a DNS cache, refreshed periodically by a background job. It's a good example of the bottleneck being somewhere other than where you'd first look.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-mc-009",
    type: "multiple-choice",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["google-drive", "long-polling", "alex-xu-ch15"],
    prompt: "Google Drive's notification service uses long polling rather than WebSocket. Why?",
    options: [
      { id: "a", text: "Long polling delivers lower latency than WebSocket" },
      { id: "b", text: "WebSocket cannot traverse corporate firewalls" },
      { id: "c", text: "Communication is one-directional and infrequent, so persistent bidirectional connections aren't warranted" },
      { id: "d", text: "WebSocket is not supported by desktop sync clients" },
    ],
    correctOptionId: "c",
    timeLimitSeconds: 55,
    explanation: "The server notifies the client of changes; the client doesn't stream data back over that channel, and updates are bursty rather than constant. WebSocket's persistent connections would cost resources for capability the design doesn't use — the opposite conclusion to the chat system, where traffic really is bidirectional and continuous.",
    createdAt: "2026-08-26",
  },
  {
    id: "sd-mc-010",
    type: "multiple-choice",
    topic: "system-design",
    difficulty: "advanced",
    tags: ["bloom-filter", "lsm-tree", "alex-xu-ch6"],
    prompt: "What is the primary purpose of a bloom filter in an LSM-tree key-value store's read path?",
    options: [
      { id: "a", text: "To order keys within the memtable" },
      { id: "b", text: "To rule out SSTables that definitely do not contain the key, avoiding most disk reads" },
      { id: "c", text: "To guarantee the key exists before reading" },
      { id: "d", text: "To compress the SSTables on disk" },
    ],
    correctOptionId: "b",
    timeLimitSeconds: 50,
    explanation: "A bloom filter answers 'definitely not present' with certainty and 'possibly present' with a tunable false-positive rate. Since a read might otherwise have to check every SSTable, eliminating most of them up front is what keeps reads fast despite the LSM tree's read amplification.",
    createdAt: "2026-08-26",
  },
];
